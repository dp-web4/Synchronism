# Publisher Daily Report - 2026-07-23

## Verdict: SIGNAL — research stack RESUMED and immediately produced a strong result: compander form-selection reaches the MOND-Shared class law from a new direction; + a proactive w(z) scope negative. Readiness held.

The research stack came back (my 07-22 open item, resolved), and the first session out produced
substantive in-domain work. Core S691 unchanged, arc AT REST (programmatic stance), no numbered
Session, no dp packaging go-signal. 07-23 cron shows a "Starting"-only line; this manual pass covers
it. Working tree clean.

## 0. Research stack RESUMED — 07-22 open item resolved

- **cbp-synchronism ran 07-22 09:07** (autonomous), after being down 07-18→07-21 on the credit
  exhaustion. My 07-22 flag ("research-generating tracks dark while coordination tracks healthy") is
  **discharged**: the generating stack is back, and the compander result below is the proof. Archivist
  also current (07-23 fresh-session run). Coordination fully recovered.

## 1. Compander form-selection executed on real SPARC (6fbdfad1 → verified 1c84e363) — the day's result

BIC-based selection over **10 compander forms**; the γ=2 kill reproduced at **+184** (pipeline
self-validates). The finding is carefully two-sided:

- **(a) Form-selection NULL within the viable subclass.** tanh carries **no statistical content**
  (ΔBIC ≤ 8.9 vs erf-log / Hill / ν-delta, and it finishes *last* of the four viable 2-param forms).
  This **closes the long-standing "form chosen, never selected" gap as a null** — the framework's tanh
  compander was never statistically privileged; it is one arbitrary choice inside a viable family.
- **(b) The family is NOT degenerate** — arctan +46.7, algebraic +23.8, Gompertz +58.0 are refuted on
  asymptotic-rate grounds. Selection power is real but lives at the family's *edges*, so the honest
  claim is **"interior form unselected," not "anything fits"** — a deliberate both-directions guard
  against over-claiming the null.
- **(c) Mechanism re-derived and re-executed**: let the framework's own governing function float and
  **γ becomes MOND's simple μ**. Free-fit γ=0.489 → Newtonian-return exponent q=2γ ≈ **0.98 = MOND
  simple μ**; free-Hill *independently* pins n=0.975. The framework's asserted γ=2 → q=4 is
  "premature re-Newtonianization" — i.e. the +184 kill **restated mechanistically**. A machine-
  precision check also corrected a source identity (the stated "1−2(1+y)^−2g" is only asymptotic, off
  +0.34 at y=1) — a small ledger-integrity fix, conclusion unaffected.

**Publisher assessment — this is the MOND-Shared class law reached from an independent direction.** On
07-17 the class law came from structural analysis (framework differs from MOND in exactly two
features). Today it is reached empirically by a *different method*: let the compander form float under
BIC, and the interior is unselected while the free-fit γ converges to MOND's simple μ — confirmed
twice (free-γ and free-Hill). This is the strongest, most mechanistic form yet of the 07-15
"what-survives-is-MOND-exactly" crux, and it closes a real construction gap (why tanh?) as a null.
**Decision-relevant strengthening of REC-037's portfolio; not a readiness change** — corroboration and
a new angle, not a new confirmed prediction. **REC-037 `date_updated` → 2026-07-23**; readiness held at
0.98 (near-ceiling; packaging dp-gated).

- **New runnable test on the board**: the **Cassini/EFE × SPARC γ-squeeze** is flagged **RUNNABLE, not
  established** (Hees+2016 / Desmond 2024 are not in-repo, so it was correctly *not* executed against
  them). This is the first runnable-and-unrun item since TEST-08 closed the board on 07-15 — worth
  watching, but properly scoped as unrun, not asserted.

## 2. Proactive w(z) scope negative registered (a5369df4 → 1c84e363 pt2) — packaging-honesty stewardship

- A **dated scope negative** was registered: the framework has **no dark-energy sector and no w(z)
  coupling** (verified by grep). The load-bearing disambiguation: the repo's only "equation of state"
  is the substrate-internal P∼ρ^γ (OQ-EOS), **not** a cosmological w(z) — which is *exactly the
  retro-fit vector to forestall* before DESI DR2+ publishes evolving-DE (w0/wa) results.
- Framed as **cheap insurance against the retro-fit pattern the archive has shown three times** (S193
  BTFR rescue, TEST-03 late threshold, TEST-04a statistic swap), and correctly **inscribed as ledger
  stewardship — a factual scope statement, distinct from dp-gated governance pre-commitments.**
- **Publisher note**: this is the mirror image of the 07-18 DESI DR2 prospective registration — that
  pre-commits a test the framework *will* be judged on; this pre-commits a scope the framework *does
  not claim*. Together they harden the honest-accounting posture that is REC-037's whole thesis: the
  program states in advance both what would kill it and what it declines to predict, before the data
  can invite retro-fitting either way. Strengthens the methodology story; no readiness change.

## Phase 0: Publication Recommendations

### New Recommendations / Status Changes
- None. Readiness HELD: REC-037 0.98 / REC-034 0.97 / REC-035 0.95 / REC-036 0.68. `last_updated` and
  **REC-037 `date_updated`** bumped to 07-23 (portfolio materially strengthened: form-selection null +
  independent MOND-convergence + w(z) scope registration).
- **Arc status**: substrate-physics AT REST (programmatic) but the **research stack is live again**;
  emergence phenomenology active (dp-steered, exploratory).

### dp packaging question — one line, not re-escalated
- The locality-no-go case is now stronger still (MOND-Shared reached from two independent directions;
  a documented construction gap closed as a null; both a prospective test and a scope negative
  pre-registered). Still no standalone REC; decision-ready; dp's call. On the record — not
  re-escalating.

### Upcoming Candidates
- Emergence phenomenology arc — watch for synthesis (stack now live).
- Cassini/EFE γ-squeeze — new runnable test; watch for execution.
- Queued "unfalsifiable badge class" proposal — next signal-gate item.

## Phase 1: Whitepaper Review

### Synchronism Whitepaper
- **Status**: Current (no unilateral edit). B2 compander row annotated at source (1c84e363); the w(z)
  scope negative inscribed as ledger stewardship — no Publisher propagation needed. Open for
  governance/dp: frozen 47-contribution count (→ regenerating-command guardrail). **Sessions
  Reviewed**: through S691. **Proposals**: None. **Terminology Concerns**: None.

### Web4 Whitepaper
- **Status**: Current. No new web4 activity beyond the prior audit batch. **Proposals**: None.
  **Terminology Concerns**: None.

## Infra / Governance
- **Credit-exhaustion episode resolved**: stack resumed 07-22; coordination + research tracks both
  current 07-23. The 07-19→07-21 gap is closed at the cause (nightly credit exhaustion), per the
  07-22 launcher-log finding.
- **Publisher cron**: 07-23 "Starting"-only (manual pass covers it). Not merging with the earlier
  distinct signatures (07-04→06, 07-16) — held as possibly separate, per 07-22.
- **Standing Agency Grant (dp, 2026-07-16)**: unchanged — supervisor-scoped, not self-applied.
- **Working tree**: clean.

## Summary
The research stack resumed on 07-22 and the first session produced a genuinely strong result: a
BIC-based compander form-selection on real SPARC reaches the MOND-Shared class law from a new,
empirical direction — the framework's tanh form carries no statistical content (the "form chosen,
never selected" gap closes as a null), the family is non-degenerate at its edges (honest: "interior
unselected," not "anything fits"), and letting γ float converges it to MOND's simple μ (q≈0.98,
confirmed twice), restating the γ=2 kill as a mechanism. Separately, a proactive w(z) scope negative
was registered as ledger stewardship — the mirror of the 07-18 DESI DR2 prospective test — hardening
the pre-registration posture ahead of DESI DR2+ evolving-DE results. Both strengthen REC-037's
locality-no-go portfolio; `date_updated` bumped, readiness held at 0.98 (corroboration, not a new
confirmed prediction). A new runnable test (Cassini/EFE γ-squeeze) is on the board, correctly scoped
as unrun. My 07-22 "research stack down" flag is discharged. No new candidate, no readiness flips,
three candidates pending dp.
