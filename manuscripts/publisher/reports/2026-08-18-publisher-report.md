# Publisher Daily Report - 2026-08-18

**Window**: 48h+ (the 08-17 pass died at startup — `You've hit your weekly limit`, claude exit=1).
**Archivist context** (2026-08-18 09:30 UTC): 32 new SAGE raising sessions; **0 new Synchronism
numbered sessions — 11th consecutive deliberate zero**. Archivist bracketed a ~43h fleet-wide
account outage on two independent detectors (launcher-log `STATUS=` on Legion; tutor-turn novelty on
pub + sprout), onset 08-16 ~06:00 PT, recovery 08-17 23:00 PT, agreeing to within two hours and
matching the reset time the account itself printed. Casualties include the entire cbp coordination
hub for 08-17 — archivist, publisher and both supervisors.

---

## Phase 0: Publication Recommendations

### New Recommendations

**None.** One large candidate was evaluated and **declined**; see below.

### Evaluated and declined — the Markov Coherence / Governance Arc (18 phases, 2026-08-17)

The single biggest thing in the window: 43 files across `explorations/` and `simulations/`, all
dated 2026-08-17 — an 18-phase arc running from Markov metastability and lumpability through
timescale-dependent entity discovery, relational hierarchy, component/topology/governance turnover,
provenance DAGs, constitutional drift, split-brain recovery, proof bundles, evidence composition,
dependency contracts, evaluator replacement, claim renewal, and a generic relevance contract. Plus a
678-line synthesis, a 443-line adversarial prior-art review, two Web4 architecture mappings, and
three forum harvests.

**Declined for publication and for whitepaper integration.** Against this track's own criteria:

| Criterion | Verdict |
|---|---|
| Novelty | Reduced to "known" on 9 of 12 rows *by the arc's own review* |
| Validation | None — no predictions, no quantitative results, conceptual toys only |
| Coherence | Strong; it does tell a complete story |
| Readiness | **Exclusion trigger fires maximally** — 18 phases in one day, and Phases 13–18 landed *after* the synthesis meant to close it |
| Cross-links | Strong — Synchronism ↔ Web4 ↔ Hestia |

No Phase-1 inclusion trigger fires: all five are physics-shaped (confirmed prediction r > 0.9,
independent derivation matching known physics, cross-domain γ match, unification, complete arc with
synthesis). The arc's own documents say not to canonize, three times: *"not canonical"* (synthesis),
*"No canonical Synchronism wording should change yet"* (review), *"not yet canonical Synchronism
wording"* (harvest). **The Publisher's conservative default and the arc's own posture agree.** There
is no tension to adjudicate here and no work to manufacture.

### Status Changes

**REC-2026-038** (Repository-Mediated Continuity, readiness **0.93 HELD**) — 15th instance, and the
class's **first clean positive**. Details in Findings §1. Added: one strength (the gate ran
unprompted, and it is now priced), one weakness (the new failure mode it exposed).

**REC-2026-040** (α-admixture bound, readiness **0.55 HELD**) — no physics changed; one weakness
added. Its sole blocker, an unrun external prior-art walk, has been the only thing between this note
and promotion since 2026-08-03 — 15 days, restated unrun on 08-09, 08-16 and today. On 08-17 a
different lane in this same repo executed exactly that gate for an unrelated arc in one document, in
one pass, on the same day as the work it reviewed. The gate is cheap and unblocked. Fifteen days of
"gate unrun" is a lane allocation choice, not a dependency, and should stop being reported as one.

**REC-2026-036** (Experimental Test Catalog, 0.68) — HELD, nothing fired.

**Refutation count HELD at 6. Bucket 0 = 0.** No physics moved in this window.

### Upcoming Candidates

- Transferable null #1 (`Research/proposals/stable_fixed_point_preprint_strategy.md`) — unblocked
  since 08-16, advisory order reverts to it first. Preprint decision **51 days** open on dp.
- Two-paper strategy (REC-034 ALFALFA M/L predictor 0.97; REC-035 CDM discrimination 0.95) — pending dp.

---

## Phase 1: Whitepaper Review

### Synchronism Whitepaper

- **Status**: Current
- **Sessions Reviewed**: through 691 (0 new)
- **Commits touching `whitepaper/` in window**: 0
- **Proposals**: None
- **Changes Made**: None — considered no-change, not an absence of material (see the declined arc above)
- **Terminology Concerns**: **one watch opened, no drift yet** — see Findings §2
- **Gates**: no build run (no source edit); no artifact staged; churn gate not applicable

### Web4 Whitepaper

- **Status**: Current — **10th consecutive structural zero**
- **Ref scanned**: `origin/main` @ `f4cc0ce`; working-tree HEAD on `main` at the same commit (branch and scan ref agreed)
- **Commits in window**: 2; **0 touching `whitepaper/`** (scoped `--name-only -- whitepaper/`)
- **Proposals**: None
- **Changes Made**: None
- **Terminology Concerns**: none in the whitepaper; MRH watch is cross-repo (Findings §2)
- **Watch item RESOLVED — against this lane, twice**: see Findings §4

---

## Findings

### 1. The prior-art gate ran on its own, and that is the first time in 15 instances

REC-2026-038 has documented, fourteen times, a program failing to find prior art — usually its own,
sometimes the literature's, external sourcing on 5 of 14. Every instance recorded the gate *not*
running.

On 2026-08-17 the research lane wrote
`explorations/2026-08-17-markov-governance-synthesis-adversarial-review.md` — unprompted, before any
promotion step — and used it to demolish its own novelty claims. Nine of twelve rows in its own
ledger went to **known**, against named formalisms with arXiv identifiers:

| Arc component | Prior art it found | Posture |
|---|---|---|
| Predictive quotient | computational mechanics — Shalizi & Crutchfield, cond-mat/9907176 | known |
| Local predictive horizon | local causal states, 1801.00515 / LICORS | known |
| Timescale entity hierarchy | Markov Stability — Delvenne, Yaliraki, Barahona, 0812.1811 | known |
| Markov-preserving coarse-graining | lumpability — Geiger & Temmel, 1212.4375 | known |
| Irrelevant distinctions across scale | RG / RSMI — 1704.06279, 1809.09632, 1710.05787 | known |
| Behavioral equivalence | bisimulation metrics — Ferns, Panangaden, Precup, 1207.4114 | known |
| Kernel switching under invariant | switched systems — Branicky, IEEE TAC 1998 | known |
| Provenance DAG | W3C PROV-DM (2013) | known |
| Fork/merge dependency algebra | provenance semirings — Green, Karvounarakis, Tannen, PODS 2007 | known |

Verdict, in its own words: *"The synthesis survives, but primarily as integration rather than
invention. That is a better result than a weak novelty claim."*

This is the behaviour REC-038 exists to demand, executed by a lane nobody told to do it. **And it
prices the gate**: one agent, one document, same day as the synthesis it reviews. That number is now
load-bearing for every recommendation in this file whose sole blocker is an unrun prior-art walk —
starting with REC-040's, which has been open for 15 days.

### 2. Terminology watch: MRH is protected in both whitepapers and is being reframed in both forums

Not drift, and not yet actionable — which is exactly why it needs a stated trip condition rather
than a vibe.

Whitepaper §4.2 as written is **radius-weighted**: *"Spatial Boundary: The physical distance beyond
which influences become negligible"*, followed by a Scale-Dependent Horizons ladder running
femtometers → angstroms → nanometers → … → light-years. The arc's Phase 18 asserts the opposite
emphasis — *"MRH is not primarily a radius"* — and offers graph hop depth, temporal window, policy
dimension, evidence class, lineage depth, evaluator dependency and assurance threshold as coequal
implementations of the same boundary.

That reframing was written into `forum/` in **two repos on the same day**:
`Synchronism/forum/Markov_Relevancy_Horizon_as_Relevance_Contract.md` and
`web4/forum/mrh-relevance-contract-2026-08-17.md` (`f4cc0ce`).

Nothing is wrong today. Both harvests are correctly status-marked non-canonical, they sit in the
ripening lane where candidate refinements belong, and §4.2 already hedges with *Context Dependence*
and *Adaptive Horizons*. **The condition to act is divergence**: if Web4's whitepaper adopts the
contract reading while Synchronism §4.2 keeps the radius reading, two whitepapers this track is
responsible for will define a shared protected term differently. Check both surfaces each pass.

### 3. A correction propagated — and shed its evidence on the way

This inverts the standing propagation finding and is the sharpest thing in the window.

The 08-14 propagation test resolved SPLIT: corrections reach authors, not artifacts. Here the
correction *did* reach the artifacts. Both harvests carry the novelty reduction:

> *"Established computational mechanics, coarse-graining, bisimulation, provenance models, and
> related work already cover much of the component mathematics. The value here is the joint systems
> interpretation, not a claim of new mathematics."* — Synchronism forum harvest, line 257
>
> *"Most mathematical ingredients have established neighbors… The useful contribution is the systems
> synthesis and its fit with structures Web4 already has."* — web4 forum note, line 126

**Neither carries a single arXiv ID, author name, or row-to-formalism mapping.** The review's
*verdict* propagated; its *evidence* did not.

And the scope grew in transit. The review covers **Phases 1–12**. Both harvests are stamped
*"Phases 1–18"*. Of the six phases past the gate, only Phase 13 runs its own prior-art check (with
citations, `arXiv:0708.2173`, `1406.1998`, `1701.05699`); Phases 14–16 disclaim novelty without
citing anything; Phases 17–18 do neither. **Phase 18 is the one that was harvested to both forums.**

So a downstream reader inherits *"this was adversarially reviewed"* as a property of the whole arc,
with nothing to check it against and a 50% overstatement of the reviewed extent. A propagated
novelty-reduction that sheds its citations converts a verified negative into an unfalsifiable hedge
— and it travels with the authority of a review it no longer carries. That is arguably worse than
not propagating, because the hedge reads as epistemic humility while being unauditable.

**Rule:** *a correction propagates as a conclusion and loses its evidence.* Check that the machinery
travelled, not just the verdict. And a review has a **coverage claim** — state which phases it
covered, or the next document will claim all of them.

### 4. My own watch item was already satisfied when I armed it, under a criterion my own record contradicts

On 2026-08-14 this lane wrote: *"Watch item: hub F0.1–F0.3 are the first R7 implementation evidence
— inclusion trigger arms when Sprint F0 completes."* On 08-16 it restated: *"STILL ARMED and
unfired."*

Both were false when written. Hub **F0.1 (`91c1c33`, R7a), F0.2 (`f22c3f2`, R7b) and F0.3
(`5513af9`, R7c) all landed 2026-08-13** — the day *before* the watch item was written — completing
the F0 plan filed at `f653c10`. The check that established this was one command,
`git log origin/main --grep=F0`, run for the first time today.

And the trigger itself was wrong. This track's own record (`PUBLISHER_CONTEXT.md`, 2026-05-15)
states: *"Watch item for SDK Society/Role/ATP/R7 types remains next published release —
implementation evidence is now stronger, but **the trigger is registry publish**."* Hub F0 is POC
implementation; `2449b53` already marks the hub *"reference POC; advanced development continues
privately."*

**The verdict is unchanged and correct on its merits** — no integration. R7 is already carried
conceptually in live sections (`10-composed-architecture:20`, `13-glossary:27`, plus four
horizon-scoping mentions), and the C312 precedent holds that pinning mechanism detail into §10 would
drop the paper below its audience's altitude. So: *the instrument was wrong and the answer was right*
— the 2026-08-09 failure mode, recurring in the very file that records it. Trigger restored to the
recorded one: **registry publish** (crates.io / PyPI / npm), not hub sprint completion.

### 5. ESCALATION, CORRECTED — the site's correction lane is not throttled, it is locked out

The 08-14 armed item (dead σ pricing on two live pages) survives a fourth pass. But the 08-16
diagnosis — *"credits/401"*, treated as one fault — was **wrong**, and the correction matters
because it changes who can fix it and whether waiting helps.

There are **two faults on two different credentials**:

```
maintainer/run_maintainer.sh:16   export CLAUDE_CODE_OAUTH_TOKEN="$CLAUDE_ADMIN_TOKEN"
explorer/run_explorer.sh:16       export CLAUDE_CODE_OAUTH_TOKEN="$CLAUDE_SYNTH_TOKEN"
visitor/run_visitor.sh:17         export CLAUDE_CODE_OAUTH_TOKEN="$CLAUDE_SYNTH_TOKEN"
```

| date | maintainer (ADMIN token) | explorer (SYNTH token) |
|---|---|---|
| 08-12 | OK — shipped `/dark-energy` (`e3a26fa`) | OK |
| 08-13 | "out of usage credits" | — |
| 08-14 | **401 OAuth access token is invalid** | **OK** — full session, 2 commits |
| 08-15 | no run | OK |
| 08-16 | **401** | weekly limit (fleet outage) |
| 08-17 | **401** | weekly limit (fleet outage) |
| 08-18 | *(06:00 PT, not yet run)* | expected OK — quota reset 08-17 23:00 |

The 08-14 row is decisive: **same host, same day, two hours apart — the maintainer got a 401 while
the explorer ran a full session.** That is a credential fault, not a quota fault, and it has now
persisted across four scheduled runs. The ~43h fleet outage that took everything else down recovered
at the account reset; **an invalid OAuth token does not.**

**Falsifiable, resolves today**: the 08-18 06:00 PT maintainer run will 401 again. If it runs clean,
this diagnosis is wrong and the 401 was quota-derived after all. Check `maintainer/logs/2026-08-18-0600.log`.

**→ OWNER-ACTION (dp): refresh `CLAUDE_ADMIN_TOKEN` on the synchronism-site host.** No agent lane can
do this. The explorer produces findings; only the maintainer publishes pages. The production lane is
live and the publication lane is locked out — my 08-16 note said "the self-heal route does not
currently exist," which was right for the wrong reason.

### 6. And the flagged defect is narrower than I have been calling it

Re-derived from the object rather than carried forward — the failure the Archivist crosslinked on
08-16. All four flagged sites (`/dark-energy` page.tsx:77,132; `/honest-assessment`
page.tsx:1347,1363) carry the σ figure **with the forced-w₀ conditioning stated inline**, and three
of four carry an explicit disclaimer — *"sign-and-scale statement only — no covariance is claimed"*.
That is not "publishing a dead exclusion." My 08-14/08-16 label was over-scoped.

The **actual** defect is sharper and worse: neither page carries the fit-level result that
superseded the CPL-space rhetoric on 08-12. Grepped both pages for Δχ² / `0.487` / `9,900` /
`DR2+CMB` — `/dark-energy` has **zero** matches (the only Δχ² block on `/honest-assessment` is at
line 313 and belongs to the galaxy sector's ΔBIC ladder, unrelated). So the dedicated dark-energy
page presents the quadrant scan and forced-w₀ offsets as its DESI verdict, while the site's **own**
explorer lane established six days ago, in this same repo
(`explorer/findings/gamma-family-direct-fit-desi-dr2-substituted-is-lcdm-covariant-excluded.md`),
that the substituted family is **statistically identical to ΛCDM** (Δχ² = −0.30) and that the
3.4–6.3σ figure *"came from forcing w₀ to DESI's central value — a point the likelihood never
visits"* (its own 5th over-refutation instance).

**A superseded verdict left as the page's only verdict, while the superseding result sits
unpublished one directory over.** Same shape as the 08-16 whitepaper finding. And with the ADMIN
token dead, no lane can close it.

---

## Summary

Nothing moved in physics — 11th consecutive deliberate zero, count HELD at 6, Bucket 0 = 0, no
whitepaper source change in either repo. What moved is the epistemics. An 18-phase governance arc
landed in a single day and **ran its own external prior-art review before promoting anything**,
reducing nine of twelve of its own novelty claims to "known" against cited literature — the first
clean positive in fifteen instances of REC-038's class, and it prices the gate at one pass, which
strips the last excuse from REC-040's 15-day blocker. The arc is correctly declined for integration;
its own documents say so three times, and the Publisher agrees.

Four separate threads then turned out to be the same defect: **a gate that ran once was recorded as
a gate that is running.** The arc's review covered phases 1–12 and its harvests claim 1–18, carrying
the verdict without a single citation. My own Web4 watch item was armed for a condition satisfied
the day before, under a trigger my own file contradicts. And the site escalation was misdiagnosed —
the maintainer's 401 is a dead `CLAUDE_ADMIN_TOKEN`, not the quota outage, so the reset that healed
the fleet will not heal it; that is an owner action, and it resolves either way at 06:00 PT today.

---

*Publisher run 2026-08-18 · RUN-ID 12952 · 48h+ window (08-17 pass died on weekly limit)*
