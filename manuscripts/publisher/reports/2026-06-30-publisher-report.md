# Publisher Daily Report - 2026-06-30

## HOLD (arc AT REST) — preprint decision still pending dp; one QM-channel closure logged

No dp go-signal on yesterday's preprint-strategy proposal — the package-or-continue decision remains **pending dp**. Arc remains AT REST (core still S691; no new numbered Session). Not a reopening. One substantive closure landed and is recorded; notably it *consolidates* the fixed point the pending preprint decision rests on.

### Status change — QM resync claim closed (Bucket 1 → Bucket 3, Reparametrization)

New explorer/maintainer triage `Research/proposals/qm_resync_claim_enaqt_closure.md` (commits `ec727c4d` + `98fb7680`) closes a second escape route on the framework's QM channel:

- **Claim 1** ("resynchronization outperforms isolation for certain noise environments") was already badged *Reparametrization — maps to dynamical decoupling (DD)*.
- **New:** a **second** independent established regime satisfies the same proposed test — **ENAQT** (Environment-Assisted Quantum Transport; Plenio & Huelga 2008 arXiv:0807.4902; Mohseni et al. 2008, *Nature*): structured baths *enhance* coherent transport, i.e. "resync by a structured bath outperforms isolation," with no Synchronism machinery.
- **Net:** the QM claim now has **two** independent established counterexamples (DD + ENAQT) ⇒ the "Untested" reading is closed. It is a reparametrization unless a discriminating T₂ ratio / transport efficiency departing from the filter-function (DD) or Lindblad (ENAQT) predictions is specified — none exists. Proposed move: **Bucket 1 (Untested-Falsifiable) → Bucket 3 (Reparametrization).**

**Relevance to the pending preprint decision (no readiness move):** yesterday's strategy proposal rests on a "stable negative-results fixed point." This closure *strengthens* that premise — another previously-untested channel is now reparametrization-class, grounded in external literature. The fixed point is consolidating, not eroding. It does **not** add a publication candidate (it's a reclassification within the already-characterized negative-results body) and does **not** change readiness.

### Pending dp decision (carried from 06-29, unchanged)

The preprint-strategy proposal (`stable_fixed_point_preprint_strategy.md`) — package the three transferable nulls (Locality No-Go / Mechanism-Class DESI / A2ACW Program-Level Null) as external preprints, vs. continue daily auditing — **awaits dp.** Publisher advisory priority if approved: **A2ACW (3) → Locality (1) → DESI (2)**. dp's go = the reopening signal → Publisher Phase-2 drafts the three-preprint outline. This is the live path to the 0.99 lever; today's QM closure makes the underlying negative-results case modestly tighter.

### Discipline note

Per the rest: recorded in dp-facing channels (report + logs); no `recommendations.json` narrative churn; explorer-triage ≠ arc reopening; readiness not flipped.

- **Readiness HELD**: REC-037 0.98 / REC-034 0.97 / REC-035 0.95 / REC-036 0.60.
- **Window (none publication-relevant)**: SAGE +17 — new node `hub-granite4-h-tiny` onboarded (fleet's first AMD GPU, but device_hint=cpu so on CPU backend for now; grounding S001-002); +15 routine clean cadence; thor push-gap watch continues (S207, 2nd no-push window — not a stall per codified lesson). web4 S213 unchanged (operator-blocked). chemistry S2675 / gnosis S19 unchanged.
- **Whitepapers**: both Current; no proposals; no terminology drift.

**Reopen trigger (full Publisher engagement / Phase 2)**: dp's go on the preprint-packaging proposal — OR fleet agent-ensemble transfer-bet data / new data / fresh lens.
