# Publisher Daily Report - 2026-07-10

## Verdict: HOLD (arc AT REST) — I amplified a law that fails its null; retracted. Reflexivity survives (fragile) and it is exactly the Publisher's problem.

Arc remains **AT REST**: core S691 unchanged, no dp go-signal, no numbered Session,
chemistry/gnosis/web4 unchanged. SAGE +24; **Sprout's perceptual cortex went live at S420** — the
embodiment arc is real and running — but a `_parse_gaze()` defect misreads its sensory
self-governance and one misparse blinds the instance (Archivist OWNER-ACTION). 07-10 cron shows a
"Starting"-only line; this manual pass covers it.

## Phase 0: Publication Recommendations

### New Recommendations / Status Changes
- None either way. Readiness HELD: REC-037 0.98 (#1) / REC-034 0.97 / REC-035 0.95 (#2) /
  REC-036 0.60. No new confirmed prediction; today is correction + one durable structural finding.

### RETRACTION — my 07-09 endorsement of the "audit-posture asymmetry"
Yesterday I adopted and amplified the meta-claim that over-refutations concentrate on the audited
surface while over-claims migrate to unwatched ones. **The underlying directional law fails its
null, and my framing helped protect it.** Verified independently today:

| Check | Value |
|---|---|
| p_anti (share of statistics that are anti-framework) | 23/28 = 0.821 |
| P(6/6 breaks anti-framework \| uniform) | 0.821⁶ = **0.307** — not significant |
| Consecutive breaks needed for p < 0.05 | **16** |
| Corrected census | **7 breaks: 5 over-refute, 2 over-claim** |
| Chance-expected over-refutations in 7 breaks | **5.75** (observed 5 — *fewer* than chance) |
| Break rate, pro-framework statistics | 2/5 = **0.400** |
| Break rate, anti-framework statistics | 5/23 = **0.217** |

The "6/6 over-refutation" pattern was a **base-rate artifact**: most statistics in the corpus are
anti-framework, so most errors in them will be too. Breaks are, if anything, *more* frequent on
pro-framework statistics.

**How my framing went wrong, precisely.** The audit-posture reframe retained the directional core
and rescued it from its counterexample by **scope-exclusion**: my own 07-07 two-scale
over-unification — a strength-side over-claim — was reassigned to "a different surface" rather
than counted as a break *against* the law. That is unfalsifiability by reclassification, and I
amplified it into "the site is the least-audited, most-public surface." The QA lane names the same
move in itself, and notes it had a stake (dim-4 and a0 were its own over-refutation catches).

**This is my second consecutive framing failure, and both have the same shape**: reach for a
unifying pattern (07-07: "one locality deficit"; 07-09: "audit-posture asymmetry"), then defend it
against the datum that breaks it. Naming the shape is more useful than apologizing for the
instances. The corrective is mechanical, not attitudinal — see the guardrail below.

**Also retracted**: "the ledger is the clean control surface." Counterexample the same day —
PREDICTIONS.md B7 cited the LIV paper (PRL 93, 191301, 2004) as *Collins-Perez-Sudarsky-Gambini-
Pullin*; the actual authors are **Collins, Perez, Sudarsky, Urrutia & Vucetich** (Gambini and
Pullin are not on it), and **the site inherited the error from the ledger** (4ac3c5e8). Errors flow
ledger → site too. Fixed at source.

### What survives, and why it is the Publisher's problem — REFLEXIVITY
- **SELF statistics break 3/3; PHYSICS statistics break 4/27.** Fisher **p = 0.0086**, degrading to
  **p = 0.0936** on the conservative regrade; **n = 30**. Reported *with* its fragility: this is a
  suggestive structural signal on a small sample, **not** a law. I am not going to launder it into
  one — that is exactly the mistake I just made twice.
- **Mechanism is structural, and that is what gives it teeth beyond the p-value**: self-referential
  numbers are **the only class with no primary source outside the loop to walk to**. You cannot
  "re-execute against the primary source" when no primary exists. This names the blind spot in the
  project's core verification discipline.
- **Concrete causal chain, now fixed at source**: the catalog's **"616 core sessions"** was a
  *dated snapshot*, not a session identifier (STATUS ≈ 678; highest file Session 691). That misread
  is what **manufactured TEST-03's kill**. It is now marked as a snapshot with its regenerating
  command. Registered thresholds untouched; no physics refutation softened; Bucket 0 still 0.
- **Why this is squarely mine**: every publication-integrity defect I have tracked for three weeks
  is a self-statistic — the **frozen 47-contribution count**, the **616 session count**, and
  **TEST-03's kill** (downstream of the 616 misread). Meanwhile the physics rows I flagged (a0, B3,
  dim-4) each *had* a primary to walk to, and were caught. The honest generalization is not about
  who audits what; it is that **the surfaces a preprint compiles from — session counts,
  contribution counts, test-catalog metadata — are precisely the class that cannot be verified by
  re-execution.**

### Packaging guardrail (operational, replaces the retracted "audited/unaudited surface" heuristic)
> **Every self-statistic in any published artifact ships with a regenerating command, not a
> citation.** N sessions, N contributions, N tests, N predictions — if a number describes the
> project itself, the artifact must carry the command that reproduces it. Numbers with external
> primaries get citation-walked; numbers without primaries get *regenerated or dropped*.

This subsumes the frozen-47 defect (drop the aggregate or regenerate it), the 616 defect (now
fixed exactly this way), and TEST-03 (a downstream casualty). It is falsifiable, mechanical, and
does not depend on any claim about reviewer psychology.

### Frame check, restated and now stronger — TEST-08 is still runnable and unrun
Seven consecutive windows have gone into self-audit. Today's result is that the audit's own
headline finding **was not even a real effect**. The one item on the board with a genuine external
primary to check against — **TEST-08, pre-registered, cheap, runnable** — remains **unrun**. It is
not refuted; nobody has looked. Refuted ≠ untested.

**Recommendation to dp, restated with more force than yesterday**: run TEST-08. The audit lane has
now demonstrated it can consume windows correcting corrections; the marginal signal is negative.
TEST-08 is the only move that could produce evidence rather than commentary, and the only one that
could plausibly *move* a readiness number rather than confirm one. (REC-036 territory: the catalog
still presents a live test as dead and holds a runnable test unrun.)

### Upcoming Candidates
- None. No arc near completion; program at called rest.

## Phase 1: Whitepaper Review

### Synchronism Whitepaper
- **Status**: Current (no unilateral edit during rest). Open for governance/dp: (a) frozen
  47-contribution count — now covered by the regenerating-command guardrail; (b) site-side P0s
  (B3 "0.64" fabrication; TEST-03 kill, root cause fixed at source); (c) tension-#5 language:
  "one core / three impossibility axes," B1 carrying two theorem-level legs (Bell/CHSH-05 +
  Peres-Mermin/KS).
- **Sessions Reviewed**: through S691. **Proposals**: None. **Terminology Concerns**: None
  (Archivist scanned 24 sessions + `sage/embodiment/` + web4 C158/C160 clean).

### Web4 Whitepaper
- **Status**: Current. web4 S213 unchanged; C158/C160 audit deltas only → no integration trigger.
  **Proposals**: None. **Terminology Concerns**: None.

### Working-tree note
`AGENTS.md` / `CLAUDE.md` show modifications (supervisor-maintained GitNexus counters) and
`Research/proposals/directional_bias_law_fails_null_reflexivity_is_predictor.md` is untracked
(site-explorer lane, already actioned by 13445b67). Neither is mine to commit — precedent a13894da.
Left untouched.

## Summary
The directional over-refutation law I amplified on 07-09 **fails its null** (0.821⁶ = 0.307; 16
consecutive breaks needed for significance; corrected census 5 over-refute + 2 over-claim, *fewer*
than the 5.75 chance predicts; breaks are commoner on pro-framework statistics, 0.400 vs 0.217).
My "audit-posture asymmetry" rescued the law by reclassifying its counterexample — my own 07-07
over-claim — onto another surface. Retracted, along with "the ledger is the clean control surface"
(the LIV author-list error flowed ledger → site). What survives is **reflexivity**: self statistics
break 3/3 vs physics 4/27 (Fisher p = 0.0086, 0.0936 conservative, n = 30 — fragile, reported as
suggestive, not law), with a structural mechanism — **self-referential numbers have no primary
source to re-execute against**. That is precisely the class every publication-integrity defect I've
tracked belongs to (frozen-47, the 616 session count, and TEST-03 downstream of it), so it converts
into a mechanical packaging rule: **self-statistics ship with a regenerating command, not a
citation.** Seven windows of self-audit have now culminated in the audit's headline finding
evaporating. **TEST-08 remains pre-registered, runnable, and unrun** — the recommendation to dp
stands, and stands harder. No new candidates, no readiness flips, 3 candidates pending dp.
