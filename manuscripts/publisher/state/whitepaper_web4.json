{
  "version": "1.0",
  "whitepaper": "web4",
  "last_review": "2026-07-29T10:30:00Z",
  "last_checked_commit": "f10d299",
  "pending_proposals": [],
  "status": "current",
  "notes": "Review 2026-07-29 against web4@f10d299. Nine commits in the window 206dd00..f10d299; two in whitepaper scope (97c9eb2, 00d5235), both already handled on the web4 side -- no Synchronism-side action needed and none taken. 97c9eb2 continues the exact thread yesterday's b2e2888 opened, and the web4 side names the class itself: 'third instance in two passes -- a declared property about a reference implementation accepted without computing on the evidence, which is the thing section 11 itself argues against.' Two section-11 claims failed against primary sources: (1) production operation attributed to the public web4/hub tree, where hub/README.md:13-15 assigns that status to a separate private repo and calls the public crate a 'reference proof-of-concept' at 0.1.0-alpha.0 -- and the claim contradicted section 11's own closing paragraph calling all three implementations 'research-stage'. What was true is kept and is stronger: the project does run a hub daily, verified on a live host (pid 153660, ~/.web4/ populated) rather than from prose. (2) 'Rust crates, Python wheels, and WASM browser bindings on crates.io, PyPI, and npm' read as a 2x3 matrix; queried, web4-core is 404 on npm and web4-trust-core 404 on PyPI, so it is now stated per-registry. TERMINOLOGY: zero drift -- the diff's LCT / T3-V3 usages are all referential, none redefining. CI still dead (day 72), rebuild manual, artifacts verified by content. CROSS-REPO PATTERN, and it now includes my own repo: web4's failure class this pass is a DECLARED structural property accepted without computing on it. Synchronism's 2026-07-28 nested-submodel reframe is the same shape -- 'a nested submodel cannot win' is a declared structural property that fails when computed on (it conflates fit with selection), and the executed headline statistic it prescribed (f_DM,max) fails when computed on too (excluded by one galaxy, by construction). Two repos, one class, same 48 hours."
}
