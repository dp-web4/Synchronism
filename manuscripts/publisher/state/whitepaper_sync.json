{
  "version": "1.0",
  "whitepaper": "synchronism",
  "last_review": "2026-08-23T10:30:00Z",
  "last_integration": "2026-08-23",
  "sessions_reviewed_through": "no new numbered sessions (Archivist: 16th consecutive deliberate zero) -- window carried 2 substantive Synchronism commits (298a950e Yukawa self-check + d2ccb960 SESSION_MAP), both author self-retractions. Fourth consecutive pass where a zero session count sat over a non-zero content window, and this is the first one in which the window's content REFUTED live whitepaper text.",
  "pending_proposals": [
    "PROSPECTIVE REGISTRATION GATED ON DP (filed 2026-08-10; TERMS HARDENED 2026-08-11/12 -- restate at adoption, do not adopt the filed text as-is): originally sign(w0+1) = sign(wa) for every gamma (Publisher-verified 0/16), framed kill-or-tie-only on 'gamma=1/2 IS LCDM'. The covariant 00-component was then executed (site, 2026-08-11; Publisher-reverified 2026-08-12, simulations/publisher_20260812_covariant_00_checks.py): the LITERAL sign lock dies in the Brans-Dicke completion and no LCDM member survives there (kill-or-tie is substitution-conditional), but the DESI-quadrant no-go HARDENS to model class -- 0/192 gammas at omega in {0,1,5,50}, forced wa wrong-signed (+0.23..+0.60), escape condition exactly an interior maximum of rho_DE(x), which no completion of C = tanh produces. Registrable discriminant at DR3: the sign of wa. Whitepaper 5.15 carries the class-level statement as of 2026-08-12; registration remains NOT adopted into the prediction set. Also still gated from the 08-10 proposal: adoption of the negative-existence-claims-need-the-primary-layer rule into registration discipline. The Sessions #100/#101 errata are now fully executed in-archive (08-11 errata + 08-12 conditionality-discharge update). TERMS RE-PRICED AGAIN 2026-08-12 (recorded 08-14, the 08-13 cron died on credits): the site explorer's direct likelihood fit (gamma-family-direct-fit-desi-dr2-substituted-is-lcdm-covariant-excluded.md; routed via Research/proposals/gamma_family_direct_fit_desi_dr2_20260812.md) showed the sign-of-wa statistic is LCDM-degenerate on the only surviving branch (substituted best gamma=0.487, Delta-chi2=-0.3 vs LCDM) and both covariant completions are ALREADY excluded by DR2-era data (A: chi2~9900, exact EdS; B: +79..+187 at every omega) -- before DR3 arrives. Currently proposed terms (test26_registration_projection_robustness_20260812.md, dp-gated): class-level (interior-maximum criterion), statistic = Delta-chi2(substituted vs w0waCDM) on DR3 likelihoods, presented as a CONSISTENCY CHECK not a discriminator, with a projection-robustness pre-commitment against the CPL-artifact literature. Whitepaper 5.15 carries the fit-level statement and the corrected registration description as of 2026-08-14.",
    "OPEN, UNRUN, cheap (inherited from the 2026-08-09 site-lane proposal and NOT adjudicated 2026-08-10): Research/Coupling_Coherence_Experiment.md prescribes finding the empirical transition point as p* = argmax |d2C/dp2| and accepting the tanh fit if |p* - p_crit|/p_crit < 0.15. For the ANALYTIC form this is degenerate: C is concave for all x >= 0, so |d2C/dx2| is maximised at the origin and p* -> 0 regardless of p_crit, meaning the criterion can never be met. Whether that bites depends on whether the SIMULATED C(p) inherits the analytic concavity, which nobody has checked. The protocol never states which curve it expects. Owner: unassigned. Recorded here so it does not decay into folklore.",
    "OQ-Coarsening discharge (registered 2026-08-06, UNRUN): find the single coarse-graining length ell that simultaneously serves SPARC disks, the Cassini/Solar-System bound (TEST-11, +17.95sigma), wide binaries (TEST-02) and clusters. x propto ell^2 under the Jeans normalization route, so ell alone moves NGC 3198 from C=0.0003 (ell=100 pc) to C=0.86 (ell=8 kpc) -- the entire range of the galaxy-sector verdict. An order-of-magnitude mismatch between sectors is a parameter-free obstruction on the coarse-graining axis and is estimator-independent, unlike the amplitude and functional-form obstructions already banked. Owner: unassigned. Cheaper than the preprint decision.",
    "DECLINED 2026-08-06, recorded so it is not re-proposed: the 2026-08-05 proposal 'A_calibration_is_a_coarse_graining_scale_644x_resolved' reads the 644x A-calibration gap as a single length rescaling (ell ~ 317 pc ~ disk scale height). Re-executing session66_A_gap_investigation.py refutes it: the gap is exactly (8 kpc/0.07)^2/4.5^2 = 645.0, TWO substitutions, and the single-length reading requires beta_J=1 where the generating code used alpha=4.5 (implied length 70.5 pc, 4.25x from the claimed 300 pc; S53 records alpha varying 1.3-4.5, so it is not single-valued). The proposal's OTHER two halves stand and were acted on: the coarse-graining gap (-> OQ-Coarsening) and the false 'for any value of the calibration constant' quantifier on /key-claims (x propto 1/A exactly; site-lane scope, absent from this whitepaper -- verified).",
    "Reconcile the four in-program numbers for 'how much boost SPARC demands' before any draft cites one: 34x (max point, efe-boost-ceiling-closure.md 2026-06-03), 13.75x (f_DM,max, TEST-10 2026-07-15 / Publisher 2026-07-29), 42% of points above 3.17 (06-03), 69% of galaxies above 3.17 (07-15). All four reproduce; mutually consistent under different cuts, denominators and radial selections; no document says so. Owner: whoever drafts the class-exclusion note.",
    "Index synchronism-site/explorer/findings/ from the Synchronism repo. 40+ executed results with real computation live in a sibling repo and nothing here points at them. COST RE-MEASURED 2026-08-03 and it is now worse than the 06-03 case that motivated this entry: the RAR scatter no-go was executed there on 2026-08-02 at 08:18 and 48 minutes later the research lane wrote, in THIS repo, that the galaxy sector's single substitution would fail 'which is precisely the locality no-go' -- a conditional whose antecedent had just been settled one repository over, with a validated positive control. Neither document cites the other; the finding had reached zero surfaces here before the Publisher back-annotated it. The failure is no longer 'old work goes unseen' but 'the two halves of one argument are written the same morning by authors who cannot see each other.' publisher/CLAUDE.md Phase 0 section 1b instructs the scan and is why it was caught; a durable index would fix it for every reader, not just the Publisher. Owner: dp / research lane.",
    "Migrate the aggregate contribution count into claims/claim_ledger.json so render_claim_surfaces.py regenerates it under the --check gate (durable fix for the 47/48-vs-30 accounting defect; today's change is an in-place annotation, not a single source of truth).",
    "Repo line-ending policy (owner call, still open, but the urgent half is CLOSED as of 2026-07-27). The rationale previously recorded here -- 'building on the WSL /mnt/c mount emits CRLF and produces a 3,177-line content-free diff' -- was a wrong diagnosis. The mount was a bystander. The real cause was preprocess-sections.sh trapping a CRLF record's trailing CR *inside* the bold markers it generates (`**Header<CR>**`) and writing that back into the source; a lone CR makes git classify the file as non-text, which silently disables core.autocrlf=input for it, so its CRLF endings then diff against CI's LF blobs on every line. Script fixed at source and the 11 committed occurrences repaired (8a0fd27f), so the phantom-diff symptom is gone and a local build is no longer expected to churn. What remains genuinely open, and is a much weaker case: 50 of 95 section sources still carry CRLF in the working tree, harmless under autocrlf=input but re-armed the moment any future generator does substring surgery on a record. A narrow `.gitattributes` (`*.md text eol=lf`) would remove the hazard class entirely at the cost of a one-time normalization of those 50 files. dp's call; no longer urgent.",
    "Move the Publisher run log out of whitepaper/PUBLISHER_CONTEXT.md into publisher/. Appending run history to the document the Phase-1 subagent reads as *context* grows the context doc without bound.",
    "Retire log-size and header-presence as Publisher liveness diagnostics, in this track and in the Archivist's. Since the 2026-07-24 launcher fix added `2>&1 | Add-Content`, the agent's output is flushed only at completion, so a live run's log is ALWAYS header-only when the agent inspects it -- the signature that meant 'never started' before 07-24 now means 'currently running', and the detector can only return 'dead'. This produced false failure reports on 08-01 and 08-02 (both runs exit=0) and a spurious OWNER-ACTION to the supervisor requesting a redirect already present. Same trap misread four times in the Archivist track (07-26/27/29/30). The 2026-08-03 launcher change (022ada55: timezone + UTC + RUN-ID in the header) fixes the ambiguity for the Publisher; the correct predicate is the CLOSING banner, never the byte count, and it should be applied to every track's launcher. Owner: supervisor."
  ],
  "status": "current",
  "notes": "2026-08-20 run: TWO in-place corrections propagated into the Synchronism whitepaper, both from verified results that had landed only in PREDICTIONS.md / Research/proposals/ / explorations/ on 2026-08-19 and had not reached this document. (1) Completion-B omega: the clause '0 of 192 gamma values AT EVERY BRANS-DICKE OMEGA TESTED' was an UNDER-refutation -- Cassini forces omega >= 4.0e4 so the published {0,1,5,50} grid was 800x inside the excluded region, and omega is absorbed by the model's own closure (192x1, not 192x4). Corrected in the lead in 15-dark-matter/dark_matter.md; the corrected statement is STRICTLY STRONGER (w0 hardens -1.58 -> -3.18 at the allowed omega). The absorption claim was re-verified here SYMBOLICALLY (simulations/publisher_20260820_completion_b_omega_absorption.py, 11/11) and the verification yielded an identity the source did not have: at the pinned point 1.5*omega*eps_crit^2 = 1 - 3*eps_crit exactly, so the closure fixes the PRODUCT omega*eps_crit^2 -> 2/3 rather than omega. That is a proof of absorption where the source had a numerical trajectory comparison ('<2%'); the 1.22% / 0.24% departures I compute ARE that observation. (2) Kernel symmetry: 'only non-local survives' sharpened to INWARD-CUMULATIVE non-locality in dark_matter.md and, compactly, in BOTH reader-action surfaces (executive summary and conclusion), with the unexecuted 3-D Yukawa falsifier and the not-yet-citable status carried in all three, plus the 73.0%-raw over-refutation guard. Builds: make-md.sh exit 0, make-web.sh exit 0. Artifacts RESTORED not staged (churn gate: content 118 ins / 506 del vs RAW 12,012 / 12,400 -- the documented CRLF mode); CI is the authoritative builder. Also corrected outside the whitepaper: a cross-surface TEST-25 ID collision noted in Research/EXPERIMENTAL_TEST_CATALOG.md. Refutation count HELD at 6; Bucket 0 = 0.\n\n2026-08-21 (Publisher, autonomous). CONTENT CHANGE, four files. Target: 05-quantum-macro/15-dark-matter (equation lead + table row + a dated evidence note) and 06-implications/04-open-questions (one-sentence conditionality marker). Also two SOURCE annotations outside the whitepaper: Research/proposals/rho_crit_asymmetry_saturation_knee.md (its open RQ3/RQ4 answered, its Option B flagged as BTFR-breaking at gamma=2) and Research/Session649_QM_Kill_And_RhoCrit_Asymmetry.md (its '+1 has no physical motivation beyond numerical stability' clause corrected in place).\nWHAT WAS ROUTED vs WHAT WAS THERE. The 08-20 source disposition routed a '+1 creates-not-excludes the power law' WHITEPAPER correction. Searched: neither that clause nor its companion ('the framework's only structural difference from MOND') exists anywhere in live whitepaper/sections/. Both live on the site, whose maintainer has been unreachable since 08-13. The whitepaper's actual defect was SILENCE -- the bullet list under 'The Coherence Function (Derived)' explained tanh, log(rho) and gamma and omitted the one term that sets the deep limit. So the correct edit here was an ADDITION, not a correction: [[a-correction-has-a-landing-site]], and the first instance where the routed site did not exist at all.\nVERIFIED HERE BEFORE EDITING (sympy + numpy, not accepted from source): C_p = tanh(gamma*ln(1+x^p)) -> gamma*x^p for every p (limits at p = 0.7, 1, 1.5 each return gamma); deleting the regulator gives tanh(gamma*ln x) -> -1, a negative saturated constant outside C in (0,1]; the whitepaper's own d2C/drho2 expression reproduced exactly and confirmed negative everywhere at gamma=1/2; inflections reappear at p=1.5 (x=0.54) and p=2 (x=0.82), matching the source's 0.545/0.819.\nNEW HERE, NOT IN THE SOURCE -- the mechanism behind the -1.34 sigma. The regulator keeps gamma OUT of the deep-limit exponent: C -> gamma*x gives index 1 for every gamma, and the deep-limit solution of g_bar = g*C(g/a0) is g = sqrt(a0*g_bar/gamma), so gamma and a0 enter ONLY as a0/gamma -- an EXACT degeneracy, not an approximate one. It predicts without fitting that rescaling a0 by 2*gamma absorbs any gamma; imposed blind, that reproduces the whole registered interval gamma in [0.425, 0.600] to <= 0.013 dex RMS over the SPARC range (10x under sigma_int = 0.122 dex, under the 0.106 dex nuisance floor), matching the fitted optimum. So gamma is not 'unidentified at a factor of two' (08-14) -- across its own confidence interval it is a relabelling of a0, which is why fitting it can only buy noise and out of sample does (-1.34 sigma).\nAND THE ARCHIVE'S OWN PROPOSAL WOULD HAVE BROKEN THE SECTOR. rho_crit_asymmetry_saturation_knee.md Option B (2026-05-08, live, unactioned) recommends dropping the +1 for C = (1+tanh(gamma*ln x))/2. That moves gamma INTO the exponent: deep index 2*gamma, BTFR slope 2*(2*gamma+1). Viable only at gamma = 1/2 (slope 4); at this archive's own derived gamma = 2 it is slope 10 against an observed 3.75 +/- 0.10. Its RQ4 ('is there any physical problem with C < 0.5 at low density?') is answered YES, and not for the reason it anticipated.\nRefutation count HELD at 6; Bucket 0 = 0. No new empirical failure was added -- this is mechanism for an existing degeneracy plus an answer to a standing internal question. Build verified (make-md.sh, 7,747 lines, content present in the monolith, zero lone-CR damage in sources); artifacts restored, CI is the authoritative builder. Churn gate RAW == CONTENT on all six touched files.\nNOT DONE, and named: registering the executed p-null with a TEST-ID (catalog lane, REC-036); the site's /equation-walkthrough Step 5 inversion (site lane, maintainer unreachable day 9); the cross-repo TEST-25 renumber (unchanged).\n\n2026-08-22 (Publisher, autonomous). CONTENT CHANGE, three whitepaper sections + three source annotations. THE ARMED PROBE FIRED A SECOND TIME, ON ITS SECOND CLASS. The 08-20 watch item asked what descriptive claims this whitepaper makes about its OWN repository that no pass has probed. Outing 1 (08-21, manual pass) hit on TEST-IDs. Outing 2 (today) hit on self-counts, and the target it named -- 5.12's 1,873-vs-1,913 divergence -- resolved completely.\nRESULT: THE PHENOMENON-TYPE COUNTER IS `sessions - 137`, IDENTICALLY. Over 824 independent (type, session) pairs in Research/Chemistry/Framework_Summary.md -- 154 milestone banners plus 670 per-session entries, spanning types 560..2,520 and sessions 697..2,657 -- the relation holds with ZERO exceptions. 1,873 is the ordinal at Session #2010 (:18454, 'Automotive Adhesive ... 1873rd type', tagged 'MILESTONE: 2010th session!'); 1,913 is the ordinal at Session #2050 (:18857, 'Candle Combustion ... 1913th type'). The whitepaper's own 5.12 table already encoded the identity and nobody read it that way: '#147-500 ... 363 phenomenon types', and 500 - 137 = 363. Corpus end (#2671) reads 2,534.\nWHAT IT COSTS THE CLAIM. '89% validation across N phenomenon types' reads as breadth -- N distinct phenomena independently landing at gamma~1. N is the session counter with a fixed offset; Era 1 (#1-133) accounts for the offset and from #138 on EVERY session was recorded as contributing exactly one new type. This is the inventory-level counterpart of the audit already in SESSION_MAP.yaml ('Era 2 (#134-2660) 100% by tautological template'): the tautology is in the INVENTORY, not only in the validation rate. Same genus as 08-21's result that gamma is a relabelling of a0 -- a headline number that restates something already known under a name that implies independent content.\nAND NEITHER FIGURE WAS WRONG WHEN WRITTEN, which is the sharper half. git log -S settles it in one command: 064e3f84 (2026-02-07) is titled 'Update session counts: 476 core, 2010 chemistry, 31 arcs' -- the commit that introduced 1,873 is the commit that set chemistry sessions to 2,010. 867217ae (2026-04-07) is titled 'Chemistry (1873->1913 types, Phase 3-4)'. Both were accurate snapshots by this same Publisher track. The defect is PARTIAL UPDATE: the denominator was carried forward to 2,671 / 2,679 and the numerator was left frozen, twice, in two sections, four months apart.\nREC-038 INSTANCE 18, strongest form of the class yet. The 08-10 caveat stated 'Neither figure appears in Framework_Summary.md ... so the corpus grounds neither.' Both appear, four times each, and their appearance is what identifies them. A search was run, reported negative, and the false negative CERTIFIED the absence -- worse than an unrun gate, because a later reader has a documented negative to rely on. Completes the inversion triad: unfound (08-16) / found-and-mis-signed (08-21) / searched-and-falsely-reported-absent (today).\nSECOND ITEM, independent: the TEST-NN namespace is disjoint by construction. Two dense registries -- this repo's EXPERIMENTAL_TEST_CATALOG.md (01-25) and synchronism-site's src/app/test-catalog (01-26, four tiers) -- with no alias table ever. 11 of 12 IDs checked name different tests; site TEST-02 duplicates this catalog's TEST-14 under a different number, so it is a permutation with partial content overlap. The three 'collisions' logged 08-20 / 08-21 / 08-22 are three draws from that condition, not incidents. Consequence: every executed result the program cites lives in the SITE's namespace while Session674_Test_Catalog_Census.md scores the ARCHIVE's. Namespace declaration added at the head of the catalog, per-row alias on TEST-05, scope note on the census at the exact WAKE paragraph that called the divergence 'housekeeping'. No renumber. Headline unaffected in both namespaces: confirmed-discriminating count = 0, by execution. REC-036 0.58 -> 0.45 with the deliverable restated as a reconciliation table, not a catalog.\nTHIRD ITEM, physics: the satellite ambient-density finding's headline uses the statistic the finding itself disqualifies. '~950x TEST-05's ceiling' / 'the regime gap was worth 3 orders' is the SCAN MAXIMUM (L=3.813, one corner: DDO154 x most massive host x most favourable slope x f_ret=0.8), while both the proposal's section 5 and the source's section F say max-lever is the wrong statistic and quoting it is cherry-picking the tail. On the endorsed ensemble statistic the regime gap is 0.28x -- the satellite ensemble median lever 1.104e-3 sits INSIDE TEST-05's own executed span 4e-5..4e-3, and the D=100 kpc median matches TEST-05's ceiling to 7%. That is section 1's tidal identity being RIGHT: L <= k(3-n)/9 caps both regimes, so both must land in the same decade. The title and section 1 contradict each other and section 1 is the verified one. Sharper still: 956x (max / TEST-05 ceiling) and 896x (max / the scan's own D=100 median) agree to 7% BECAUSE the ceiling equals the median, so the 'regime gap' number is measuring the scan's internal dynamic range, not a regime gap. Re-derived the identity symbolically here (rho_local/rho_bar = (3-n)/3 exactly, n<3). What survives: closure-by-citation is invalid as REASONING; the systematic closure (S/S = 4e-3); the verdict. What changes: the power gain (short by 7x in N vs TEST-05's 2-4 orders) is bought by the matched-pair design, stacking N=700 and 0.0334 dex per-object precision -- NOT by a bigger lever, because the levers are equal.\nRefutation count HELD at 6; Bucket 0 = 0. No new empirical failure. Build: make-md.sh exit 0, 7,802 lines, content present in monolith; artifacts restored, CI is the authoritative builder. Churn gate RAW == CONTENT on all 9 touched files (all pure-LF worktree over pure-LF blobs except the 05-quantum-macro CHANGELOG, which is CRLF in both and was appended with matching endings).\nNOT DONE, and named: the session DENOMINATOR divergence (SESSION_MAP.yaml framework_sessions 2671 / chemistry_documented_sessions 2672 vs SESSION_MAP.md 2,679) -- Archivist-owned, referred not changed; the core session count (whitepaper 690 vs SESSION_MAP.md 628) -- same owner, same referral; REC-041 and REC-040 prior-art gates, unrun for the second and third consecutive pass respectively.\n\n2026-08-23 (Publisher, autonomous). A REFUTATION PROPAGATED, AND THE REFUTATION'S OWN BLAST-RADIUS CLAIM WAS FALSE. Three whitepaper body sections corrected, three CHANGELOG entries added, three historical entries marked SUPERSEDED.\nWHAT FIRED. On 2026-08-20 this lane propagated an 08-19 result into executive_summary.md, 15-dark-matter/dark_matter.md (sec 5.15) and conclusion.md: 'the discriminating axis is symmetry, not range', hence every natural repair of a pointwise multiplier is in the dead branch, hence the live branch reconstructs g_bar itself. It was propagated WITH its own hedge -- a 2-parameter radial scan, 3-D conjectured, and one named cheapest falsifier ('project a genuine 3-D Yukawa ... unexecuted ... if a screened LINEAR scalar lands in the live branch the sorting rule is wrong and the escape taxonomy reopens'). That falsifier ran on 2026-08-22 and returned exactly that branch. The whitepaper's own pre-registered consequence fired, so the escape taxonomy reopens.\nVERIFIED HERE BEFORE PROPAGATING, and the analytic half is stronger than the empirical half. The refutation needs no SPARC number: (lap - m^2)h = 4 pi G rho has Green's function e^(-mr)/r, which is symmetric -- two-sided and isotropic -- at EVERY screening length and recovers Newton as m->0. Re-derived in closed form in this lane: F_Y/F_N = 0.736 / 0.995 / 1.000 at lambda = 1 / 10 / 10^3 in units of r (matching the source's figures exactly). The symmetric branch CONTAINS g_bar, so the sorting rule was refutable by construction on the day it was written. Empirically it also fails: the unscreened Yukawa passes the validation gate at 0.1080 dex against g_bar's 0.1107, the symmetric field overlaps g_bar under galaxy-block bootstrap at EVERY lambda_s >= 1 R_d, and at matched range it beats the causal kernel on every row. yukawa_addendum_bootstraps.py re-run in this lane at EXIT 0 with its tables reproduced value-for-value.\nTWO LIMITS CARRIED INTO THE WHITEPAPER THAT THE SOURCE'S TRIAGE NOTE DOES NOT CARRY. (1) The replacement axis -- which-derivative-of-Phi (Joyce, Jain, Khoury & Trodden, Phys. Rep. 568, 2015) -- is written here as RANGE-CONDITIONAL, not as a ladder, because in the source's own bootstrap TWO OF ITS THREE RUNGS SWAP VERDICT between the only two ranges tested: |Phi_Y| OVERLAPS g_bar at lambda_s = 4 R_d (+0.0171 [-0.0023,+0.0376]) and SEPARATES at infinity (+0.0327 [+0.0118,+0.0578]), while the normalised <Sigma>_Y does the reverse (+0.0190 [+0.0046,+0.0330] at 4 R_d; +0.0123 [-0.0094,+0.0415] at infinity). Further, the grad^2-Phi rung's 1.40x is scored at lambda_s = 0 (pointwise Sigma) while the other two rungs are scored at lambda_s = infinity, so the three rows are not a matched comparison. Only grad-Phi is range-robust. This is the SAME failure the document diagnoses -- a conclusion wider than its own test -- committed again in the replacement. (2) The accompanying normalisation explanation ('the 08-19 symmetric family failed because it was a normalised, intensive smoothing of Sigma') is NOT established: the routed proposal says so in its own words ('my going-in hypothesis is NOT established'), because the normalised member at infinity is unresolved from g_bar. That disclaimer is present in Research/proposals/ and ABSENT from the explorations/ triage note this repo committed -- a correction shedding its own caveat in one hop, same author, same day.\nWHAT SURVIVES AND WHAT IS WITHDRAWN. Withdrawn: 'symmetric kernels fail at any range', 'every natural repair is in the dead branch', 'the live branch reconstructs precisely the variable C(rho) was introduced to replace', and 08-19's 'separated at every finite lambda <= 4 R_d' (which does not reproduce: +0.0075 [-0.0040,+0.0223] masked, +0.0096 [-0.0046,+0.0215] unmasked, OVERLAPS both ways). Gained: a constructive bound, RAR scatter limits any screened-scalar escape to lambda_s >~ 1 R_d ~ 2.4 kpc. Unaffected: C(rho) as Synchronism proposes it is POINTWISE and pointwise Sigma remains separated (1.40x, +0.0390 [+0.0179,+0.0592]); the 08-15/16 Buckingham-pi closure rests on its own enumeration; the mutual-exclusivity tension stands. Refutation count HELD at 6; Bucket 0 = 0.\nTHE SOURCE'S BLAST-RADIUS CLAIM WAS FALSE (REC-038 instance 19, new sub-genus). The finding states 'the hedge worked - the rule never propagated as cited', the commit message states 'so it never propagated', and both state the failure was 'three days from a public page'. It had been IN three body sections and in ten built files under docs/whitepaper/ and whitepaper/build/ -- the deployed Pages surface -- for three days. It was ON a public page, not near one. The author inferred the blast radius from the hedge they attached instead of from a grep. A hedge makes a correction CHEAP; it does not make it UNNECESSARY.\nNEW PROBE RUN, CLEAN NEGATIVE (borrowed from web4 62f41005, which found three dead citations in its own paper the same window). Every URL in the built Synchronism monolith resolved: 17 of 19 are self-references checked against git ls-tree origin/main (0 dead), and the remaining 2 return 200. All 8 arXiv identifiers resolve to topically-correct papers. STRUCTURAL FINDING BEHIND THE CLEAN RESULT: the probe cannot fail here, because 0 of the 19 unique URLs leave the dp-web4 org, there are 0 DOIs, and there is no References section in 7,802 lines. The ~40 author-year literature citations are non-resolvable prose. Logged as a publication-readiness defect, not scored under REC-036.\nBUILD. make-md.sh exit 0, make-web-clean.sh exit 0; verified in the monolith that all 11 occurrences of the refuted phrase are inside a refutation or supersession marker (0 unguarded). Artifacts RESTORED, not staged; CI is the authoritative builder. Churn gate RAW == CONTENT on every touched file -- the 05-quantum-macro CHANGELOG (mixed endings, 81 CRLF of 136 lines) tripped the gate on first write (RAW 166 vs CONTENT 4), was restored from the blob and re-appended byte-wise with matching CRLF.\nNOT DONE, and named: REC-041's prior-art gate is now PARTIALLY run (first execution in five passes) with a specific named next step (Famaey & McGaugh 2012, arXiv:1112.3960, mu-function normalisation convention); REC-040's gate remains unrun though its second blocker is discharged; the session denominator divergences remain Archivist-owned and referred.",
  "watch_items": [
    "2026-08-21 -- executive_summary.md:169 cites 'Gnosis neural architecture independently uses gamma = 2 -- same coherence physics appearing without explicit design' as cross-domain validation, while the galaxy sector is now recorded as running at gamma ~= 1/2 and preferring it frozen there. Under gamma = 2/sqrt(N_corr) these are different N_corr and need not agree, so this is NOT flagged as a contradiction -- but the exec summary offers the coincidence as evidence without naming N_corr, and one of the two values is now known to be a relabelling of a0. Re-read next pass; if it still reads as validation, it needs the N_corr qualifier or removal.",
    "2026-08-21 -- synchronism-site/explorer/topics/the-s-curve-is-an-axis-artifact.md is a QUEUED, UNRUN topic whose premise (C is concave everywhere, so the S-curve is a log-axis artifact) is now known to be conditional on p == 1. It will be run against a premise that is true for the archive's equation but not for the family. Flag to the site lane when reachable.",
    "2026-08-22 -- SELF-DESCRIPTIVE PROBE, remaining classes. The probe is 2/2 (TEST-IDs 08-21, self-counts 08-22). Unrun classes named in the 08-20 arming: 'no X exists' negatives and arc-status labels. Concrete first target for the negatives class: the 08-10 caveat's false 'Neither figure appears in Framework_Summary.md' is itself an instance, so sweep the whitepaper for negative-existence claims about the repository and check each with a command. Concrete first target for arc-status: 'All prior research arcs closed as of Session #616' (conclusion.md:81) against SESSION_MAP's complete_arcs: 43 and the post-closure sub-arc extensions the same sections describe running to #666.",
    "2026-08-22 -- SESSION-COUNT DENOMINATORS, referred to the Archivist and unresolved here. Chemistry: SESSION_MAP.yaml carries framework_sessions 2671 AND chemistry_documented_sessions 2672; SESSION_MAP.md carries 2,679; the whitepaper uses 2,671 in 5.12 and 2,679 in 0/7. Core: whitepaper says 690, SESSION_MAP.md says 628, EXPERIMENTAL_TEST_CATALOG.md's own note reproduces 649 files / highest ordinal 691 / STATUS.md ~678. Five values for one quantity. Do NOT resolve these in the whitepaper -- they are the Archivist's to regenerate, and the correct whitepaper fix is a regeneration command, not a chosen number.",
    "2026-08-23 -- SELF-DESCRIPTIVE PROBE, class 3 of 4 RUN AND CLEAN: link/citation resolvability (19/19 URLs, 8/8 arXiv IDs). Probe now 2 hits / 3 outings. REMAINING UNRUN CLASSES: (a) negative-existence claims the whitepaper makes about its own repository; (b) arc-status labels -- 'All prior research arcs closed as of Session #616' (conclusion.md) against SESSION_MAP's complete_arcs: 43 and the post-closure sub-arc extensions running to #666.",
    "2026-08-23 -- HEDGE-VS-CONTAINMENT. Two lanes now infer 'it did not propagate' from 'I hedged it'. Standing check for any incoming retraction: grep the whitepaper sections AND the built docs/whitepaper artifacts for the retracted claim before accepting the source's blast-radius statement. Cost: one grep. This pass it would have been the difference between a corrected whitepaper and a publicly deployed refuted claim.",
    "2026-08-23 -- INSTRUMENT-RETURNS-UNIFORM-NULL. Twice this pass a probe returned an alarming clean sweep that was a bug: the arXiv http:// endpoint silently returned 0 bytes for all 8 IDs (would have read as '8 fabricated citations' in a repo that has an actual fabricated-consensus retraction on record), and a truncated word inside a quoted arXiv phrase query returned 0 hits. Rule: a probe whose result is uniformly extreme is the suspect; validate it on a known-positive before reporting."
  ]
}
