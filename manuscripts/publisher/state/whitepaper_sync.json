{
  "version": "1.0",
  "whitepaper": "synchronism",
  "last_review": "2026-08-04T10:30:00Z",
  "last_integration": "2026-08-04",
  "sessions_reviewed_through": 691,
  "pending_proposals": [
    "Reconcile the four in-program numbers for 'how much boost SPARC demands' before any draft cites one: 34x (max point, efe-boost-ceiling-closure.md 2026-06-03), 13.75x (f_DM,max, TEST-10 2026-07-15 / Publisher 2026-07-29), 42% of points above 3.17 (06-03), 69% of galaxies above 3.17 (07-15). All four reproduce; mutually consistent under different cuts, denominators and radial selections; no document says so. Owner: whoever drafts the class-exclusion note.",
    "Index synchronism-site/explorer/findings/ from the Synchronism repo. 40+ executed results with real computation live in a sibling repo and nothing here points at them. COST RE-MEASURED 2026-08-03 and it is now worse than the 06-03 case that motivated this entry: the RAR scatter no-go was executed there on 2026-08-02 at 08:18 and 48 minutes later the research lane wrote, in THIS repo, that the galaxy sector's single substitution would fail 'which is precisely the locality no-go' -- a conditional whose antecedent had just been settled one repository over, with a validated positive control. Neither document cites the other; the finding had reached zero surfaces here before the Publisher back-annotated it. The failure is no longer 'old work goes unseen' but 'the two halves of one argument are written the same morning by authors who cannot see each other.' publisher/CLAUDE.md Phase 0 section 1b instructs the scan and is why it was caught; a durable index would fix it for every reader, not just the Publisher. Owner: dp / research lane.",
    "Migrate the aggregate contribution count into claims/claim_ledger.json so render_claim_surfaces.py regenerates it under the --check gate (durable fix for the 47/48-vs-30 accounting defect; today's change is an in-place annotation, not a single source of truth).",
    "Repo line-ending policy (owner call, still open, but the urgent half is CLOSED as of 2026-07-27). The rationale previously recorded here -- 'building on the WSL /mnt/c mount emits CRLF and produces a 3,177-line content-free diff' -- was a wrong diagnosis. The mount was a bystander. The real cause was preprocess-sections.sh trapping a CRLF record's trailing CR *inside* the bold markers it generates (`**Header<CR>**`) and writing that back into the source; a lone CR makes git classify the file as non-text, which silently disables core.autocrlf=input for it, so its CRLF endings then diff against CI's LF blobs on every line. Script fixed at source and the 11 committed occurrences repaired (8a0fd27f), so the phantom-diff symptom is gone and a local build is no longer expected to churn. What remains genuinely open, and is a much weaker case: 50 of 95 section sources still carry CRLF in the working tree, harmless under autocrlf=input but re-armed the moment any future generator does substring surgery on a record. A narrow `.gitattributes` (`*.md text eol=lf`) would remove the hazard class entirely at the cost of a one-time normalization of those 50 files. dp's call; no longer urgent.",
    "Move the Publisher run log out of whitepaper/PUBLISHER_CONTEXT.md into publisher/. Appending run history to the document the Phase-1 subagent reads as *context* grows the context doc without bound.",
    "Retire log-size and header-presence as Publisher liveness diagnostics, in this track and in the Archivist's. Since the 2026-07-24 launcher fix added `2>&1 | Add-Content`, the agent's output is flushed only at completion, so a live run's log is ALWAYS header-only when the agent inspects it -- the signature that meant 'never started' before 07-24 now means 'currently running', and the detector can only return 'dead'. This produced false failure reports on 08-01 and 08-02 (both runs exit=0) and a spurious OWNER-ACTION to the supervisor requesting a redirect already present. Same trap misread four times in the Archivist track (07-26/27/29/30). The 2026-08-03 launcher change (022ada55: timezone + UTC + RUN-ID in the header) fixes the ambiguity for the Publisher; the correct predicate is the CLOSING banner, never the byte count, and it should be applied to every track's launcher. Owner: supervisor."
  ],
  "status": "current",
  "notes": "Pass 2026-08-04 (AUTONOMOUS cron, RUN-ID 23124 -- header self-identified correctly for the first time, per the 08-03 instrumentation). RESEARCH WINDOW: quiet -- 1 Synchronism commit (alignment arc Q1) and 2 sibling-repo commits. FINDING (this lane, verified at the code and the frozen artifacts, not inherited): the galaxy sector's pre-registered SPARC x Cassini instrument is keyed on ACCELERATION, not density. sparc_tanhlog_profile.py inverts g_bar = g_obs*tanh(gamma*ln(1 + g_obs/a0)); sparc_cassini_q2.py docstrings mu_tanh_log as 'the registered tanh-log family in the mu convention'; sparc_profile.json profiles a0 over log10 a0 in [-11,-9]; the token rho_crit appears in NONE of them. So gamma ~ 0.489, dBIC +7.1/+184 and Cassini +17.95 sigma are properties of a MOND interpolating function in acceleration space, NOT measurements of the C(rho) = tanh(gamma*ln(rho/rho_crit + 1)) printed directly above them in 5.15. Completeness-checked: the density form HAS been implemented (session128_dark_matter_derivation.py:54, compare_empirical_vs_derived.py:32), so the accurate statement is that the sector ran on density in the Session-100s era and on acceleration in the frozen era, and quotes the second under the first. NOT a new refutation -- count HELD at 6, Bucket 0 unchanged (0) -- it is a provenance defect. THE SUBSTITUTION IS NOT BETWEEN SYNCHRONISM AND MOND; IT IS BETWEEN SYNCHRONISM'S PROSE AND ITS OWN INSTRUMENT, which amends this lane's 08-03 'one substitution' framing. TWO ARITHMETIC CORRECTIONS TO MY OWN 08-03 INTEGRATION: (1) the separator from simple mu is FIRST order, D = (gamma - 1/2)(x - x^2/2) + O(x^3), giving -0.0750/-0.0110/0.0000/+0.1000/+1.5000 at gamma = 0.425/0.489/0.5/0.600/2 against the published -0.0319/-0.0054/0.000/+0.060/+3.000 -- ~2x low near 1/2 and 2x HIGH at gamma=2; the vanishing point 1/2 is correct so the conclusion stands. (2) the degeneracy is gamma <-> a0 (via gamma/a0 in the deep-MOND limit; three pairs at gamma/a0 = 9.17e9 agree in mu to <1% over a 4x span in gamma) and NOT gamma <-> rho_crit; the artifact's profiled a0 = 5.33e-11 sits 2.11x below its own reference McGaugh a0 = 1.128e-10, the required compensation. Both errors originated in the research lane's 08-02 triage and were inherited by me on 08-03 unchecked while I DID re-verify the identity beside them -- corrected on 6 in-repo surfaces (PREDICTIONS.md, SESSION_FOCUS.md, the 08-02 triage, the 08-02 proposal, my 08-03 report, this file); the site's live /coherence-function page carries it too and is flagged, not edited. BACK-ANNOTATION FILED at the sibling lane's explicit 24h-old request: the dielectric completion div[C(rho) grad Phi] = 4 pi G rho exists, answers Felten's momentum objection by Noether, is LINEAR in Phi, and so EFE = 0 is the signature of that linearity rather than an absence -- which withdraws the 08-03 premise 'the galaxy sector has no field equation' while STRENGTHENING the retraction's conclusion. Same linearity forces C(0) = 0 exactly, a divergent exterior field: the sector's one surviving structural prediction and its worst pathology are one property. PROPAGATION: [SCOPED 2026-08-04] markers added to executive_summary.md AND conclusion.md (parity preserved) on the sentence 'the C(rho) prediction of it is refuted (S661, dBIC=+184)', which contains the discrepancy inside itself -- it calls C measurable as g_bar/g_obs, an acceleration ratio. Checked at the matches not the file list: 0.489 occurs 0 times in both published surfaces, so the gamma figure carried no obligation; dBIC did. PHASE 0: REC-038 +1 strength/+1 weakness, HELD 0.93 (sixth mode, first of a new genus -- provenance detachment, where continuity SUCCEEDS at moving a claim and silently drops its warrant); REC-040 +1/+1, HELD 0.45 (external prior-art gate still unrun, 6 days); alignment arc Q1 PASS recorded, explicitly NOT a publication candidate (S <= 2 throughout, angular form unresolved). GATES: claims freeze green (10 claims, exit 0); build exit 0 (7,448 lines); churn gate content 44 / raw 23,084 -> artifacts restored, 5th consecutive correct firing; lone-CR one path (frozen v1 snapshot), 12th consecutive pass; recommendations.json 15/8 with raw == content, 8th consecutive pass with no re-serialization churn."
}
