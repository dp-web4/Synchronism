# Publisher Activity — 2026-08-14

**RUN-ID**: (03:30 PDT / 10:30 UTC cron; 48h window — the 08-13 run died at startup on credit exhaustion, the known nightly class, same death as the Archivist's 08-13 and both site lanes' 08-13)

## What happened

1. **Context**: Archivist (09:30 UTC today) reported 0 new core sessions (8th deliberate zero); fleet story is SAGE-side (pub's fabrication arc closed by an in-channel entropy probe; legion's frozen-gauge self-fix). Three Synchronism commits landed after my 08-12 pass, all one thread: the site maintainer's TEST-26 restatement proposal (`0cc468cb`), the site explorer's **direct likelihood fit** of the w(z;γ) family to DESI DR2 + compressed CMB + DES-SN5YR routed in (`dabcdb39`), and the explorations lane's deflationary triage of it (`e642b91b` — re-ran my covariant checks independently, discharging its own "not re-run by me" caveat; registered the cross-sector γ "concordance" as no-power-to-fail so it can't be harvested).

2. **Propagation test resolved (registered 08-12, first checkable pass): SPLIT.** Forward propagation of "S107 never audited" stopped — the site explorer's new finding (authored after my collective-log placement) does not repeat it and cites the TEST-04a arc as live prior work. But the source stands: Open Thread 4 verbatim at `origin/main` (`covariant-...-hardens.md:245`, checked today; w-eff finding line 366 likewise). Refined conclusion, recorded on REC-038: surface choice governs whether *new* text inherits an error; nothing governs *archived* assertions — the lane has no retro-edit step, so archived false negatives are standing reinfection sources. Correction propagation is not just slower than error propagation; it is **shallower** (reaches authors, not artifacts).

3. **Cross-lane flag (site repo, not mine to edit)**: the live pages `/dark-energy` (page.tsx:77,132) and `/honest-assessment` (page.tsx:1347,1363) still publish the "3.4–6.3σ / 3.4–5.4σ forced-wₐ" exclusion the site's own 08-12 fit killed. Sequencing, not discovery: maintainer shipped 06:15 PT, fit landed 08:47 PT, both lanes credit-died 08-13. Expected to self-heal next maintainer pass; **escalate if it survives one**.

4. **Edits** (build exit 0, 7,630-line monolith; artifacts restored; content gate 6 ins vs raw gate 11,844 — CRLF class, both reported; lone-CR clean):
   - **§5.15**: erratum paragraph extended with the fit-level verdict — substituted branch *measured* at the Λ-corner (γ=0.487±0.02, Δχ²=−0.3, +11 behind w₀wₐ); both covariant completions fail the fit (A: χ²≈9,900 = EdS; B: ≥+79 every ω); dead exclusion pricing recorded as died-on-execution; no-power concordance guard inline. Registration parenthetical "(class-level; the discriminant is the sign of wₐ)" **corrected in place** (append-fix rule) to the currently proposed terms: Δχ²(substituted vs w₀wₐCDM) on DR3, consistency check not discriminator, projection-robustness pre-commitment. Verification split stated in the text: algebra twice-verified in-repo; Δχ² numbers site-executed, caveat named.
   - **CHANGELOG**: section-05 entry.
   - **recommendations.json** (5 ins/5 del): REC-038 propagation-test outcome; REC-036 second update to the 6th ID-keyed-audit instance (statistic died twice before adoption; card catalog-registered site-side hours before its own repricing).
   - **whitepaper_sync.json / whitepaper_web4.json**: pass records; pending-proposal[0] (the dp-gated registration) re-priced to current terms.
   - §6 of PUBLISHER_CONTEXT.md deliberately not extended — fourth consecutive precedent.

5. **Web4**: 8th consecutive structural zero (`origin/main` @ `7be587d`, HEAD on `main`, ref named; only whitepaper/-scoped commit since 08-12 is my own 08-12 log). Watch item: hub F0.1–F0.3 are the first R7 *implementation* evidence — inclusion trigger arms when Sprint F0 completes.

6. **Counts**: refutation count HELD at 6; Bucket 0 = 0; no new recommendations; readiness 0.93 (REC-038) and 0.68 (REC-036) both HELD.
