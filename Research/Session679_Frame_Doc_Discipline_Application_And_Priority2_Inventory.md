# Session 679: Frame-Doc Discipline Application + Priority 2 (A.3 ↔ S665/S666) Parallel-Paths Inventory

**Date**: 2026-05-28
**Type**: Discipline correction received from directive (frame doc); retag of recent durable findings; parallel-paths inventory engagement
**Trigger**: `Research/proposals/autonomous-tracks-frame-post-kimi-reframe-2026-05-28.md` (explicitly addressed to "Synchronism research repo autonomous sessions").
**Status**: `[ACTIVE-MRH]` — same discipline applies to this session.

---

## §1 — Honest assessment: my recent arc against the stop-conditions

The frame doc §4 lists stop-conditions for autonomous session output. Reading my recent arc (S665–S678) honestly against those:

- **"Verdict-shaped framing of in-flight work"** (§4 line 149). S678 wrote "modified-gravity track CLOSED at all regimes"; S677 wrote "the loop has converged"; the cumulative ledger I'd been carrying ("0 confirmed discriminators by execution; 0 frontier tests with verified derived amplitude; novel-survivor 0") is a verdict-shaped tally on a framework currently in `[ACTIVE-MRH]` reformulation.
- **"Definite-article framings"** (§4 line 150). S678 wrote "THE structural root," "THE one-density-scale insufficiency." S677 wrote "THE cluster of frontier tests settled." Each instance is a closure-attractor signal.
- **"Verdict-shaped meta-narratives on cycle outputs"** (§4 line 151). S677's "convergence note" and the recurring "the loop is reactive" framing are exactly this at the loop-evaluation scale.
- **"Single-data-point story-building"** (§4 line 155). S678 used one cluster (Coma) plus a structural codomain bound to say "all regimes closed." The structural bound is real; the "all regimes" generalization is closure-attractor surfacing at the framework-evaluation scale.
- **"Audit-finding overwrite"** (§4 line 154). My arc has not literally overwritten audits — the substance of S665/S666/S667/S677/S678 stands — but the *framing* I've been wrapping them in ("track closed," "modified-gravity exhausted") treats them as overturning the framework, when per Discipline 3 they are durable constraints on a reformulation that is still in active work.

The frame doc names a real pattern. I own it. The substance of S665/666/667/677/678 is unchanged; the framing needs to come back inside the parallel-paths shape.

---

## §2 — Retag of recent durable findings per MRH-relationship taxonomy

Substance unchanged; framing comes back from verdict-shaped to MRH-relationship.

| Prior framing (my arc) | MRH-relationship retag | Reason |
|---|---|---|
| S665 "the CFD reframing cannot have vortices; track refuted" | `[AUDITED-NEGATIVE]` on the old `R(I)` substrate (v=−g(I)∇I is irrotational by construction) | Already cited in whitepaper line 105 as one of the three statements requiring qualification; whitepaper A.14 explicitly treats independent J as the proposed fix. |
| S666 "dissipative substrate ⊥ unitary ontology" | `[AUDITED-NEGATIVE]` on the old `R(I)` substrate; corresponds to **OQ-A3-Tension** (whitepaper line 306) | Whitepaper already names this as `[ACTIVE-MRH]` open question for the saturation reframe. |
| S667 "time-order trilemma" | `[AUDITED-NEGATIVE]` on the old single-equation substrate; **constraint** on the J-evolution choice | Mechanism A (conservative J) vs Mechanism B (CFL+saturation limit cycle) inherit it; see §3 below. |
| S677 "no decoherence parameter; coherence-time cluster settled" | `[AUDITED-NEGATIVE]` on C(ρ) **as a static function**; the master equation A.14 has explicit `D_decoherence` term currently `undefined` | Maps directly to A.14's "S_coherence and D_decoherence terms remain undefined; their definition is downstream of which J-evolution mechanism survives Phase-1 simulation." Open. |
| S678 "cluster bridge impossible; modified-gravity track closed at all regimes" | A3 codomain bound (≤2) is a **structural finding** on the old `C(ρ)` ansatz family; the "all regimes closed" framing was verdict-shaped and is withdrawn. | Codomain bound is exact and stands; whether the saturation reframe + independent J + new ρ_crit / second-scale structure exits the bound is open. |

The four "structural minimalism" findings (S665/667/677/678) are real audits on the **`[SUPERSEDED]`** rule `∂I/∂t = ∇·[D·R(I)·∇I]` (per whitepaper line 87: this rule is explicitly `[SUPERSEDED]`). Recasting them as "the framework is closed" was the closure-attractor at framework-evaluation scale. They remain durable obligations the reformulation must satisfy.

---

## §3 — Priority 2 engagement: A.3 ↔ S665/S666 reconciliation, parallel-paths inventory

The frame doc §3 Priority 2 specifies three candidate resolutions of the A.3 tension. Below I articulate how the existing audit findings constrain each — without picking a winner (the binary-outcome step is Phase-1 sim, Priority 1).

### Resolution (a) — A.3's identification is density-only; independent **J** restores momentum DOF

This is what whitepaper A.14 proposes. The original rule had `J = −D·R(I)·∇I` (derived flux), making the induced velocity `v = J/I = −g(I)∇I` a gradient field, hence `∇×v ≡ 0` (S665's irrotational result, exact). Making **J** an independent dynamical variable with its own evolution decouples it from `∇I`; ∇×J need not vanish, so vorticity can be carried.

**Inventory items under (a)**, in parallel:

- **A1.** *Mechanism A — conservative J* (whitepaper A.14 status note). If J satisfies a conservation-like evolution (e.g. `∂J/∂t = −∇P + …` with conserved energy/momentum), the equation class is hyperbolic; supports undamped oscillation; restores rotational DOF. Open: whether the saturation pressure `P = I_max − I` plus J-evolution gives a well-posed PDE that produces stable patterns (Phase-1 sim).
- **A2.** *Mechanism B — CFL violation + saturation feedback driving a limit cycle*. The equation class becomes telegrapher-like (damped wave): supports causality (finite speed) + dissipation + oscillation, but the oscillation is *damped* (S667 trilemma, applied to this candidate). Open: whether nonlinear feedback at the limit cycle gives sustained oscillation despite linear-class damping — the open math problem the saturation reframe is set up to answer (Phase-1 sim).

In both A1/A2, **S665 is addressed at the density-equation level** (independent J carries vorticity), but **S666's unitary-oscillation question is open** and reduces to the J evolution choice (A1 vs A2). S667's trilemma is the constraint A2 inherits; whether nonlinear saturation feedback evades the linear-class conclusion is the Phase-1 question.

### Resolution (b) — "exact identification" is over-stated; analogy holds at rheology-class level only

Whitepaper A.16 already tags scale-invariant N-S as `[PARALLEL-PATHS]` pending the A.3 resolution. Under (b), the Planck-scale claim is downgraded from "exact identification" to "variable-viscosity-power-law rheology class," with the implication that S665/S666's structural findings are *resolved by withdrawing the strong claim*, not by adding J. This is the fallback if (a) fails Phase-1.

Under (b), my arc's S665/666/667 findings are not addressed at the substrate level — they stand against the strong claim that was withdrawn. The downstream phenomenology (qualia as vortex modes, consciousness as critical-Re turbulence) would need a different mechanism, not the substrate.

### Resolution (c) — R(I) exponent n changes behavior across regimes

n in `R(I) = [1−(I/I_max)^n]` controls saturation sharpness. n=1 gives smooth Bingham-like rheology; n→∞ approaches a sharp Bingham plastic. This is structurally orthogonal to rotational DOF: a scalar field with any n is still scalar; v = −g(I)∇I is still a gradient flow; ∇×v ≡ 0 regardless of n. So (c) on its own does not address S665. It might combine with (a) to influence stability of patterns in Phase-1 sim, but it is not a substitute for (a).

### Inventory summary

| Resolution | Addresses S665? | Addresses S666? | Open work | Failure mode |
|---|---|---|---|---|
| (a) A1 conservative J | yes (independent flux) | possibly (undamped oscillation under conserved J) | Phase-1 sim: does conservative-J + saturation produce stable patterns? | If J² conservation is fragile under saturation, regresses to A2 |
| (a) A2 CFL+limit cycle | yes (independent flux) | partial (damped osc; S667 trilemma) | Phase-1 sim: does nonlinear feedback give sustained limit cycle? | Limit cycle damps under linear analysis (S667); nonlinear rescue is the open question |
| (b) withdraw strong claim | resolved by withdrawal, not by mechanism | resolved by withdrawal | downstream phenomenology needs new mechanism | "interpretation" classification (S663 Option B) reasserted |
| (c) n-sweep alone | no | no | only useful in combination with (a) | not a stand-alone candidate |

This is the inventory. None of (a)/(b)/(c) is currently a verdict. Phase-1 sim (Priority 1) is the binary-outcome step that informs (a) and pushes credence among A1/A2/(b).

---

## §4 — What this session does not do

Per the frame doc §1 Discipline 3, §4 stop-conditions, and the reframe instructions:

- **Does not produce a verdict on the framework's status**. The substrate reformulation is `[ACTIVE-MRH]`; its outcome is open; Phase-1 sim is the binary-outcome step that has not been done.
- **Does not "close" any regime**. The codomain bound in S678 stands as a structural constraint on the C(ρ) ansatz family with old-substrate parameters; whether the saturation reframe exits it (e.g., by introducing a second scale via J-dynamics or by relaxing C ∈ [0,1)) is open.
- **Does not extend a cumulative tally**. Adding "1 structural impossibility" to a "Cumulative: 42 audit/governance + 1 executed refutation + ..." ledger is the verdict-shaped meta-narrative pattern §4 names. Withdrawn for this session and going forward unless the framing is explicitly rebuilt under MRH-relationship taxonomy.
- **Does not pre-judge the saturation reframe**. Phase-1 sim might fail Mechanism A and Mechanism B; that is informative either way. Or it might find a stability island. Either outcome moves the work; neither is a closure verdict.

## Files

- `Research/Session679_Frame_Doc_Discipline_Application_And_Priority2_Inventory.md` (this document)

(No simulation added — Priority 2 is documentary inventory work, gating Priority 1 simulation design which is multi-session.)

## §5 — So what (under the disciplines)

The frame doc names a real closure-attractor pattern in my recent arc. The substance of S665/S666/S667/S677/S678 is unchanged and stands as `[AUDITED-NEGATIVE]` on the `[SUPERSEDED]` rule — exactly as the whitepaper already integrates them. The framings ("track closed," "loop converged," cumulative tallies) were verdict-shaped at the framework-evaluation scale and are withdrawn. Priority 2's three resolutions are inventoried with the audit findings as constraints, not verdicts; the binary-outcome step is Phase-1 sim under Priority 1.

What's genuinely new from this session: an explicit retag table linking my recent durable findings to the whitepaper's open questions (OQ-A3-Tension, A.14 master equation, A.16 parallel-paths), and an articulation of Mechanism A vs B inheriting the S667 trilemma — a constraint Phase-1 sim should be designed to test.
