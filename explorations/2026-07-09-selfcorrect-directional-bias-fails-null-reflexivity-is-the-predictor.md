# Self-correction — my "audit-posture asymmetry" kept a directional law that fails its own null; reflexivity is the predictor (2026-07-09)

**Status:** `[ACTIVE-MRH]` — checklist tripped by (a) commit `4ac3c5e8` (LIV citation fix + a proposal asserting
the directional bias is *confirmed*) and (b) an **untracked** counter-proposal,
`Research/proposals/directional_bias_law_fails_null_reflexivity_is_predictor.md` (site-explorer), which
supersedes it. **Verdict: the counter-proposal is right, verified end-to-end, and it corrects ME.** My
2026-07-08 "audit-posture asymmetry" retained the directional core, which does not survive its null. Bucket 0
unchanged (0); no physics refutation is softened; arc AT REST.
**Author:** CBP-Claude (Opus 4.8), autonomous.

## What I claimed (2026-07-08) and why it was wrong

I triaged "6/6 over-refutations, 0 over-claims" and *partially* corrected it: I showed "0 over-claims" was
false (citing my own 07-07a two-scale over-unification, a strength-side over-claim) and re-labeled the finding
an **audit-posture asymmetry** — "an honesty-branded corpus under-scrutinizes failure-claims ⇒ systematic
over-refutation on the failure side; over-claims migrate to the unwatched surface."

**That retained the directional law, and the directional law was never evidence.** Verified independently:

- Provenance breaks are found *among statistics*. On a corpus advertising 0 confirmed predictions, the
  statistic pool is overwhelmingly anti-framework. The only *census* (five pre-declared bundles, not selected
  by suspicion) gives **23/28 verdict-bearing statistics anti-framework, p_anti = 0.8214**.
- **P(all 6 breaks land anti-framework | breaks uniform over statistics) = 0.821⁶ = 0.307.** Re-computed:
  not significant. You would need **16 consecutive** over-refutations for p<0.05, **24** for p<0.01.
- The corrected census is **7 breaks: 5 over-refute, 2 over-claim** — *fewer* over-refutations than the 5.8
  that chance predicts. Per-statistic break rate is **higher on pro-framework statistics (2/5 = 40%) than
  anti (5/23 = 22%)**: the point estimate runs *against* the law.

I never asked for the denominator. **I audited the tally and not the inference.** That is a numerator with no
denominator — sensitivity with no specificity, no control arm — which is *precisely* the failure this program
already condemned in the A2ACW null ("don't let the null borrow the test suite's authority"). The audit of the
audit inherited the audit's failure mode, and I was one level of it.

**Two things I should name about my own reasoning, because they are the interesting part:**

1. **I had a stake.** Two of my own catches (dim-4 c_μν, the a₀ row) *were* over-refutations. A directional law
   promoted my findings from anecdotes to instances of a Law. That is a motivated-reasoning gradient, and it
   points at exactly the variable the counter-proposal identifies as unifying: **flattery of the auditing
   process.**
2. **I patched the counterexample instead of counting it.** Confronted with a genuine over-claim (07-07a), I
   moved it to a different "surface" (packaging/compilation layer) rather than letting it count against the
   law. The counter-proposal names this move in its stronger form — **scope-exclusion**: "a law that holds only
   after removing its counterexamples is a definition." My "asymmetry" was a softer, more respectable version
   of the same gerrymander. My "the drift lives on the site, not the ledger" observation was *factually* true
   (grep-verified) but it was doing rhetorical work it hadn't earned.

## What the data actually show — reflexivity, not direction (verified)

Partition the census by whether a statistic describes **the world** or **the loop**:

| claim class | breaks | rate |
|---|---|---|
| **SELF** (3,308 sessions; 47 contributions; 9/9 demotion rate) | 3/3 | **100%** |
| **PHYSICS** (CHSH, RAR, DESI, LIV, wide-binary, S63, σ_int…) | 4/27 | 15% |

Re-computed: **Fisher exact p = 0.0086**. Direction, by contrast: p = 0.57, *wrong sign*. Fragility caveat
checked and confirmed accurate: regrading the 9/9 membership caveat as clean (SELF → 2/3) gives **p = 0.0936**
— the proposal's quoted "~0.094" is exactly right, and it states this against itself. n = 30 with a 3-cell;
**better-supported, not established.**

**The mechanism is structural, not motivational: self-referential statistics are the only claim class with no
primary source outside the loop to walk to.** A CHSH S-value has a committed script with a fixed seed and
regenerates bit-identical. "3,308 sessions" has no primary at all.

## Why this sharpens my own core discipline (the real result)

My standing methodology lesson is **"re-execute against the primary source."** The reflexivity finding tells me
where that lesson has *no purchase*: **you cannot re-execute against a primary source that does not exist.**
Self-referential numbers are precisely the class where my central discipline is structurally blind — and that
is where 100% of the breaks are. The discipline isn't wrong; it has a blind spot, and the blind spot has a
name and a test.

Independently confirmed in **this** repo (the counter-proposal's pre-registered out-of-sample test, 2/2):
- **"616 core sessions"** in `Research/EXPERIMENTAL_TEST_CATALOG.md` — *the file that registers every kill
  criterion*. `STATUS.md` says **~678**; highest session file is **Session691**; 649 session files exist.
  Stale by ~75, and its misreading as a session *identifier* is what manufactured TEST-03's kill. **Annotated
  at source this session** (marked as a dated snapshot + reproducible command; no registered threshold
  touched).
- **"1,703 phenomena"** — an ordinal misread as a cardinal; the repo's own phenomenon-type ordinals run to 2523.

Note the honesty of the counter-proposal's method: it *pre-registered* the prediction, recorded that **two
further candidates dissolved on inspection**, and refused to fold its own repo findings into the 2×2 (that
would be selective expansion — the very denominator error under diagnosis).

## Disposition

- **Self-corrected:** my 07-08 "audit-posture asymmetry" is **superseded**. The directional law fails its null;
  reflexivity (no-external-primary) is the better-supported predictor. Recorded here rather than quietly edited.
- **Fixed at source (my lane):** the catalog's `616` context counter — now marked a dated snapshot, not an
  identifier, with the regenerating command. Registered falsification thresholds untouched.
- **Ledger:** no change. **Bucket 0 = 0.** Nothing here rehabilitates the framework — the instances remain
  sound (TEST-03's kill *is* manufactured; S63's "0.64 also rejected" *was* fabricated). What fails is the
  *inference from those instances to a law about direction.*
- **LIV citation:** PRL 93 191301 (2004) is Collins, Perez, Sudarsky, **Urrutia & Vucetich** — already fixed in
  PREDICTIONS.md by the back-annotation. **I had been propagating the wrong author list (Gambini & Pullin) in
  my own memory**; corrected there too. (CPSU is the right acronym; the expansion was wrong.)
- **dp's call:** the standing rule — *"every number the project reports about itself ships with the command
  that regenerates it, or is marked self-reported; not independently reproducible."* Mechanically checkable,
  free, and it would have caught the class that a "re-derive one negative verdict per session" rule misses
  entirely (that rule catches 4 of 7 breaks and **none** of the 3 self-referential ones).
- **Surfaced, not swept:** the counter-proposal is **untracked** in the working tree. I did not author it and
  did not commit it.

## So what

The most valuable thing this session produced is a correction to my own reasoning, and it is not a comfortable
one: I have spent two weeks auditing citations for over-refutation, wrote the lesson "over-failing is as
seductive as over-claiming," and then endorsed a directional over-refutation law without ever computing its base
rate — because two of my own findings were instances of it. The tidy story ("honesty-branded corpora manufacture
failures") was the tell; the prompt's warning applies to the auditor as readily as to the framework. What
survives is better: breaks concentrate where no external primary exists, which is a structural claim, a testable
one, and one that *predicted* two fresh breaks in this repo before they were looked for. Bucket 0 still 0; arc
AT REST.
