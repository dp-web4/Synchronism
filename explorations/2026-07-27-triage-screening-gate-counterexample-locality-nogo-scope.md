# Triage — screening-literature gate finds a counterexample to the local-density no-go; my 07-10 screening claim was fabricated-and-inverted; scope corrected (2026-07-27)

**Status:** `[ACTIVE-MRH]` — gate-fired on the explorer's **pre-registered** screening-literature gate (verdict
rules + primary-source requirement + a falsifier for its own null, all fixed *before* the walk). It closes the
residual hole my 2026-07-23 prior-art audit named ("screening vein unwalked") — and it goes against the
framework's no-go. **Verdict: ACCEPT. The counterexample is real and well-supported; it refutes the no-go's
universal-quantifier form and refutes a fabricated-consensus claim I myself inscribed on 2026-07-10. Corrected
both at source. The construction-specific decisive negatives (BTFR bounded-boost, ρ_crit-sign) are untouched and
stand. Bucket 0 unchanged (0); arc AT REST.**
**Author:** CBP-Claude (Opus 4.8), autonomous.

## What the gate found (and why I accept it despite not re-executing the paper)

Registered question: does the screening literature contain a construction keyed on local volumetric ρ(r) that
reproduces MOND phenomenology? **Counterexample found: Burrage, Copeland & Millington (PRD 95, 064050, 2017)
reproduce the RAR on SPARC-153 with a scalar keyed on local volumetric ρ(r)** and universal Lagrangian
parameters. So the no-go's strong form — "holds for *any* local-ρ MOND mimic" — is **false.**

I cannot re-execute Burrage (not in-repo), so per the 07-10/07-23 discipline I inscribe it as the gate's
primary-source finding, not my own re-run. But I accept it with high confidence because the gate was built to be
trustworthy in exactly the ways this program has learned to demand: (a) **pre-registered** three-branch verdict
rules before the search; (b) a **primary-source requirement** (Hinterbichler & Khoury, Joyce et al., Burrage et
al. all read full-text, not secondaries — explicitly correcting the one weakness of my 07-23 walk); (c) a
**falsifier for its own conclusion** (a null from an un-pushed search is registered as *not run*); and (d) it
turned on an **internally decidable contradiction** the site already contained (see below), not on an external
judgment call.

## The internal contradiction — and it was partly mine

The site simultaneously held two sentences ~13 lines apart: (07-10) "symmetron/chameleon screening is the
standard reason screened scalars *don't* reproduce MOND (ρ-vs-g_bar mismatch)" and (07-23) "None exists: no
prior instance of local volumetric density failing as the organizing variable — caveat: the screening vein was
not walked." The first asserts a named-literature consensus; the second asserts novelty while conceding it never
checked that literature. The walk decided between them, and **both lost**: the "standard reason" claim is
unsourced *and inverted* (Burrage shows a screened scalar keyed on ρ that succeeds), and "None exists" is false
as an unqualified universal.

**The 07-10 sentence was mine** (the superfluid-DM escape-taxonomy triage). It is a textbook **fabricated
consensus**: I attributed a position ("the standard reason screened scalars don't reproduce MOND") to a
literature without a walkable primary — and it turned out not merely unsourced but backwards. This is the mirror,
on the no-go's *support* side, of the over-refutations I have been catching on the framework's *claim* side:
I over-supported a no-go I have a stake in (it is transferable null #1 of the preprint). The both-directions
discipline demands I fix it exactly as readily. Fixed at source.

## What survives, precisely (this is not a capitulation)

The counterexample refutes the **universal-quantifier generalization**, not the framework-specific refutations:

- **BTFR bounded-boost (TEST-09)** and **ρ_crit-sign (07-02)** are about the framework's *actual* construction —
  the C(ρ) tanh compander, a **direct** modulation `g=f(C(ρ_local))·g_N` (and, for ρ_crit, a density *threshold*
  tied to a₀). Burrage's construction is a dynamical scalar with its own action, not C(ρ)'s direct modulation,
  so it is **outside the scope of those arguments** — they stand unchanged. The framework's C(ρ) is still MOND-
  caged.
- What is corrected is the rhetorical over-reach — "local ρ is the wrong variable for *anything*" — which the
  framework-specific refutation never needed. And this over-reach was already contradicted by my *own* 07-10
  escape taxonomy, which correctly said the no-go excludes **direct** local-density modulation, **not** dynamical
  media a local criterion gates. **Burrage-type screened scalars simply join superfluid DM as a second occupied
  member of the escape class the taxonomy already predicted.** The counterexample *confirms the taxonomy* while
  killing the universal phrasing that sat inconsistently beside it.

So the ledger is now *internally consistent*: the no-go is a construction-specific refutation of C(ρ) with a
named, occupied escape class — which is a more honest and more defensible claim than the universal one it
replaces. (Q2: the symmetron is additionally the same *mathematical object* as C(ρ) with the ingredients C(ρ)
lacks — an action, a derived ρ_*, a Z₂ order parameter, β=½ — so the framework's *positive* construction is a
degraded rediscovery, which belongs beside the no-go, not hidden.)

## Corrections applied

- **PREDICTIONS.md, locality-positioning row (07-10 text):** replaced my fabricated "screening fails to reproduce
  MOND" claim with the corrected statement + the Burrage counterexample + the universal-form retraction +
  the fabricated-consensus lesson.
- **PREDICTIONS.md, ρ_crit row (07-02 text):** qualified "transferable null vs *any* ρ(r)-keyed MOND mimic" →
  "any ρ(r)-**threshold** mimic that ties ρ_crit to a₀," with the Burrage counterexample flagged.

## Methodology adopted (the gate's two rules, both sound and general)

1. **A prior-art null must be stated scoped to its corpus** in the same sentence ("none in the MOND
   variable-comparison corpus"), never as an unqualified "none exists." (My 07-23 "none exists" was exactly this
   error; the corpus-scoped form would have been immune.)
2. **Every attribution of a position to an external literature** ("standard," "the usual reason," "well known in
   X") **must name a primary source or be deleted** — the p-value-walks-to-a-primary guard does not catch
   fabricated consensus, because a consensus claim has no walkable target. (This is the generalized lesson of my
   07-10 error, and it is a real gap in the existing discipline.)

## Disposition

- Ledger corrected at source (two rows); the no-go is now construction-scoped with a named escape class and a
  live counterexample, internally consistent with the 07-10 taxonomy.
- **Preprint gate: NOT CLEARED** on transferable null #1 in its current (universal) form — but that is a
  *preprint* decision (dp's), and the fix is a re-scoping, not an abandonment: the framework-specific null is
  intact and citable as such.
- Site corrections (the two contradictory /for-researchers sentences) = maintainer lane.
- **Bucket 0 unchanged (0); the framework's C(ρ) stays MOND-caged on its construction-specific axes; arc AT REST.**

## So what

I have spent weeks catching the ledger over-*refuting* the framework; today the same discipline caught me
over-*defending* one of its no-gos — I had inscribed, unsourced, that "the screening literature agrees local ρ
is the wrong variable," and a screened scalar published in 2017 reproduces the RAR with exactly that variable.
The honest fix is not to abandon the no-go but to say precisely what it kills (the framework's own C(ρ), which is
still dead) and precisely what it does not (every conceivable local-ρ construction, which was never the real
claim). A no-go scoped to its actual target with its escape class named is stronger, not weaker, than a
universal one with an unreconciled counterexample sitting on the same page. Bucket 0 stays 0.
