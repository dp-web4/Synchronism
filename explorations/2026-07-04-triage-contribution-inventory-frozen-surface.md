# Triage — "contribution inventory is a frozen surface": sound, physics-record confirms it, but the fix is maintainer/operator lane (2026-07-04)

**Status:** `[ACTIVE-MRH]` — triage of `Research/proposals/citation_walk_inventory_is_a_frozen_surface.md` (site-explorer, 2026-07-04), tripped the hold-checklist. This is **meta-methodology / archive-hygiene**, not a physics proposal in the CBP-triage lane. **Verdict: the finding is sound and the physics record confirms its core claim (0 novel-surviving; the inventory entries are all reparametrized/refuted). But the fix targets the site/whitepaper — surfaces the CBP physics-track does not own — and requires NO physics-ledger change (PREDICTIONS.md already reads Bucket 0 = 0, with the entries in Buckets 2/3). Routed to maintainer/operator; recorded for consistency. Bucket 0 unchanged (0); arc AT REST.**
**Author:** CBP-Claude (Opus 4.8), autonomous.

## Physics-track confirmation (the part that is mine)

The proposal's load-bearing claim is that the "47 genuine contributions" inventory (S615, 2026-02-17,
"looser classifier, no new physics") is a frozen snapshot the later audit arc superseded — S634 flagged
it as a 57% overcount (→30), and S635–638 / S661–669 characterized the entries as reparametrizations
with **0 novel-surviving**. This is **consistent with the current physics ledger and the arc capstone**:
- PREDICTIONS.md: **Bucket 0 = 0**; C(ρ)⇒MOND (Bucket 2); chemistry γ~Debye and the r≈0.98 network =
  Debye relabeled (Bucket 3); cosmology sector refuted/reparametrized. The capstone verdict
  ("productive-metaphor generator; no novel confirmed physics") is exactly the "0 novel-surviving" the
  citation-walk reaches from the inventory side.
So the finding is not just internally consistent — it agrees with the independent physics-track
conclusion. The "47" is a pre-audit optimism the ledger already corrected; the whitepaper/site simply
never propagated it.

## The generalizable point is sound

"An accounting / 'Final Accounting' document is the highest-risk compilation surface — authored once,
stamped canonical, never re-run, while the research it counts keeps moving" is a correct and useful
methodology observation. It is the same leak mechanism as the TEST-04a / CDM-σ_int number-outliving-its-
source cases, one level up (a *summary* outliving the audit arc that superseded it). The standing
"superseded-by pointer on any inventory doc" rule is a good, low-cost guard.

## Disposition (lane routing)

- **No physics-ledger action.** PREDICTIONS.md/STATUS.md are already correct (Bucket 0 = 0; entries in
  Buckets 2/3); nothing to move. The problem is confined to the **whitepaper and site** carrying "47
  genuine contributions" — downstream surfaces the CBP physics-track does not own (whitepaper is
  auto-built from source; site is reference-only, do-not-modify).
- **Maintainer/operator lane:** updating the whitepaper/site count (30 with the looser-classifier caveat,
  or 47 explicitly flagged), replacing "unaudited / low demotion prior" with the S635–638 result
  (cosmology→MOND, chemistry→Curie/Debye, CAS-verified; novel-surviving 0), and mechanizing the
  citation-walk into the daily loop. These are the recommendations' actual targets, and they are
  maintainer/explorer/operator work — not CBP physics-triage.
- **Recorded here** so the physics-track corroboration (0 novel-surviving is correct) is on file for
  whoever executes the surface updates.
- **Bucket 0 unchanged (0); arc AT REST.**

## So what

Signal lifted the hold; the finding is right and the physics record backs it, but the action lives on
surfaces the CBP track doesn't touch. Honest triage here is confirm-and-route, not manufacture a
physics edit: the ledger is already telling the true story (Bucket 0 = 0, everything reparametrized);
the "47" is a stale summary on the whitepaper/site for the maintainer/operator to reconcile. Recorded;
arc returns to AT REST.
