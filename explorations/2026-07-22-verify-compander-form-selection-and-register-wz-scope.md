# Verify compander form-selection (γ is simple μ in tanh clothing) + register the w(z) scope negative (2026-07-22)

**Status:** `[ACTIVE-MRH]` — gate-fired on two in-lane proposals. **Verdicts: (1) the compander form-selection
run is SOUND — I re-derived and re-executed its load-bearing mechanism, found one precision error (an identity
stated as exact that is only asymptotic) that does not affect the conclusion, and annotated B2. The result is
two-sided and is the MOND-Shared class law reached from a new direction. (2) The w(z) scope negative is
VERIFIED (no dark-energy machinery exists in the framework) and registered, dated, with a disambiguation my
verification surfaced. Bucket 0 unchanged (0); arc AT REST.**
**Author:** CBP-Claude (Opus 4.8), autonomous.

## (1) Compander form selection — executed, verified, two-sided

Ten functional forms were fit to the real SPARC RAR (2,807 points, BIC; the γ=2 kill reproduced at +184 as a
pipeline sanity check — the instrument self-validates, same standard as TEST-09 recovering Lelli+2019).

**(a) Form-selection NULL inside the viable subclass.** tanh is statistically indistinguishable from
erf-log / Hill / ν-δ (mutual ΔBIC ≤ 8.9, under the 10 bar) and finishes *last* of the four viable 2-parameter
members. The framework's specific functional choice carries **no statistical content** — previously an assertion
("the form was chosen, never selected"), now a result. The gap is closed **as a null.**

**(b) The family is NOT degenerate — and this cuts against over-claiming the null.** SPARC refutes three
qualitatively-acceptable companders on **asymptotic-rate** grounds: arctan-log +46.7, algebraic-log +23.8,
Gompertz +58.0. So the data are not indifferent to functional form in general — real selection power exists, but
it lives at the family's **edges** (deep-MOND slope and Newtonian-return rate), not in the interior shape. The
honest statement is "the interior form is unselected," not "anything fits."

**(c) γ is not a constant awaiting derivation — it is MOND's simple μ in tanh clothing.** This is the crux, so I
re-derived and re-executed it:
- **Exact closed form (mine, machine-precision verified):** `tanh(γ·ln(1+y)) = ((1+y)^{2γ} − 1)/((1+y)^{2γ} + 1)`.
- **Precision fix:** the source writes `tanh(γ ln(1+y)) = 1 − 2(1+y)^{−2γ}` as an *identity*. It is not — it is
  the **asymptotic** limit (error +0.54 at y=0.5, +0.34 at y=1, →0 for y≫1). The conclusion is unaffected because
  the claim concerns the large-y Newtonian-return regime, but the "=" should be "→".
- **Mechanism confirmed:** γ sets the Newtonian-return exponent **q = 2γ**. Re-executed:
  `d ln(1−C)/d ln(1+y) = −0.978` vs predicted `−2γ = −0.978`.
- **Simple-μ identification confirmed:** free fit γ = 0.489 ⇒ q ≈ 0.98; MOND's simple μ has ν−1 ∝ 1/y ⇒ q = 1
  (I verified: fitted slope −0.988). Free-Hill independently pins n = 0.975 — two parametrizations converging on
  the same object.
- **And the framework's asserted γ = 2 ⇒ q = 4** — a Newtonian return far faster than simple μ (or even standard
  μ's q=2): *premature re-Newtonianization*, which is the +184 BIC kill restated mechanistically rather than
  statistically.

**Why this matters beyond one more null:** it is the **MOND-Shared class law (2026-07-15) reached from an
entirely new direction.** The class law says a tie with MOND is only possible where the framework *is* MOND.
Here we did not compare observables at all — we let the framework's **own functional form float freely**, and it
converged on MOND's simple μ. The framework's distinctive value (γ=2) is refuted; the data-preferred value is
MOND wearing the framework's notation. Previous routes were observable-by-observable (BTFR slope, DM fraction,
environment); this one is *at the level of the governing function itself.*

**The forward lever, flagged honestly.** The proposal notes that the SPARC-preferred q≈1 (simple-μ) class is what
Cassini excludes for *modified-gravity* MOND via the EFE quadrupole (Hees, Famaey, Angus & Gentile 2016;
sharpened by Desmond 2024), while Cassini-safe fast return (q ≥ 2 ⇒ γ ≥ 1) is SPARC-killed. If no γ passes both,
the tanh-log family closes as modified gravity by a **second route independent of the locality no-go** — a
potential fifth galactic-sector cage, and runnable. **But those papers are not in-repo and I could not
re-execute against them,** so per the 07-10 superfluid-DM discipline it is registered as a **runnable lever, not
a result.** (Note the modified-inertia exemption: the squeeze applies to modified *gravity* readings only.)

## (2) w(z) scope negative — verified, then registered

DESI DR2's BAO preference for evolving dark energy (w₀wₐCDM over ΛCDM at ~2.8–4.2σ) is the live cosmological
anomaly of 2025–26, and the framework says nothing about it. Rather than assert the silence, I **verified** it:
a grep of SPINE / FUNDAMENTALS / PREDICTIONS / STATUS finds no dark-energy sector, no w(z) coupling, no
quintessence machinery. C(ρ) modulates the gravitational response to *matter* density only.

**The verification surfaced the detail that makes the registration load-bearing:** the repo *does* contain an
"equation of state" — but it is the **substrate's internal** `P ∝ ρ^γ` (open question OQ-EOS, replacing the
broken `P = I_max − I` with its negative sound speed). That is a different object from a cosmological w(z). A
future reader grepping "equation of state" could mistake the substrate EOS for dark-energy machinery — which is
*exactly* the retro-fit vector this registration guards against. Recorded the disambiguation explicitly.

Registered as a **dated scope negative**: if DR2+ confirms evolving DE, the framework inherits it passively,
neither having predicted nor forbidden it. Cheap insurance of the same kind as dp's 07-17 three-branch
pre-commitment, against the pattern the archive exhibited three times (S193's BTFR rescue, TEST-03's late
threshold, TEST-04a's statistic swap): **the absence of a prior scope statement is what makes retroactive
accommodation possible.**

**Lane note:** I inscribed this one myself (unlike the DR2 three-branch pre-commitment, which dp gated on
himself) because it is a *factual statement about what the framework contains*, verifiable and verified — ledger
stewardship — rather than a governance pre-commitment binding the program to future verdict rules.

## Disposition

- **PREDICTIONS.md** — B2 annotated with the executed form-selection (null inside the subclass; family not
  degenerate; q=2γ ⇒ simple μ; my precision fix; Cassini lever flagged unverified). Dated w(z) scope negative
  registered with the substrate-EOS disambiguation.
- **Runnable lever for dp/explorer:** the Cassini/EFE × SPARC γ-squeeze — compute EFE Q₂ for tanh-log(γ) across
  γ and overlay the Cassini bound on SPARC ΔBIC(γ). Same character as TEST-08 was ("runnable and unrun").
- **Bucket 0 unchanged (0); arc AT REST.**

## So what

The framework's governing function was finally *selected against* rather than merely asserted, and the answer is
the uncomfortable one the class law predicts: the specific form is statistically empty, and the moment you let
its one shape parameter float, it becomes MOND's simple μ — γ = 0.49 is not a constant awaiting derivation but a
re-encoding of a function MOND has had since 1983. The framework's own value, γ = 2, is refuted as premature
re-Newtonianization. Meanwhile the cheapest useful act available today was not a discovery but a *dated
silence*: writing down, before the data arrive, that there is no dark-energy coupling to retro-fit. Bucket 0
stays 0.
