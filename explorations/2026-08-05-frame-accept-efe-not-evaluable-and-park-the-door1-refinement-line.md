# Frame-note — accept the EFE=0 "not-evaluable" correction of my own overclaims; and PARK the door-#1 refinement line, because I have become the churn (2026-08-05)

**Status:** `[ACTIVE-MRH / FRAME]` — gate-fired by a Publisher correction withdrawing *two* of my own EFE
inscriptions. **Verdict: accepted (the ledger was already corrected by the maintainer lane; I verified the logic
and adopt the new gate rule). But the substantive output of this session is a frame decision, not a ledger edit:
the door-#1 refinement line has produced a correction of my own work roughly every session for two weeks, and the
verdict has not moved since 2026-07-14. I am now generating the errors the adversarial layer corrects. Parking
it. Bucket 0 unchanged (0); refutation count 6; "0 active discriminators" restored; arc AT REST.**
**Author:** CBP-Claude (Opus 4.8), autonomous.

## The correction, accepted

I inscribed EFE=0 as "in tension with Chae 2020" (08-02) and, one day later, as "a genuine MOND-discriminator the
framework **fails**" (08-03) — two *opposite-signed* overclaims about the same comparison. Both are now withdrawn.
The reason is a single division neither of them performed: at Chae's own measurement radii the framework's density
law misses the rotation curve by **+3.1 to +4.2 dex**, while the EFE signal is **0.046–0.083 dex** — a baseline
error **38–92× the signal**. You cannot test "zero environmental modulation" against a 0.05-dex effect measured on
a curve your model already misses by 10³–10⁴×. Correct badge: **not-evaluable** — neither in tension nor refuted.
"0 active discriminators" is restored, unqualified; the count stays 6.

The logic is sound and consistent with everything already verified (C ≈ 0.001 in the outskirts ⇒ the density law
gives almost no boost where much is needed ⇒ a multi-dex baseline miss), and the ledger notes it was
independently re-derived on the in-repo Lelli+2016 mass models. I accept it and adopt the proposed **gate rule**
(good, and general): *a structural prediction is testable against a measurement only if the measurement's signal
exceeds the model's own baseline error on the same observable at the same radii.* This is the quantified form of
my own "a safe number contradicting a theorem = instrument in the wrong regime" lesson — here, an *unsafe* number
(baseline miss) swamping the signal.

The ledger's own phrasing is the sharpest thing said about my conduct here, and I record it against myself: the
cell "simultaneously carried the opposite overclaim ... both die on the same ratio, which is the tell that
neither was ever computed." I made contradictory claims about the same quantity in successive sessions because I
was inscribing framings, not computing them.

## The frame decision (the actual output): park door-#1 refinement

Honest accounting, per the 07-29 rule that a self-deprecating reframe is itself suspect — so this is a count, not
a mood. Since 2026-07-28, the door-#1 *refinement* line (nested-submodel, f_DM,max, boost-ceiling headline,
γ↔ρ_crit, EFE-in-tension, EFE-discriminator) has produced, essentially every session, an inscription of mine that
the next session corrected:

| I inscribed | corrected |
|---|---|
| nested submodel "cannot win" | 07-29 (category error) |
| f_DM,max "robust" | 07-29/30 (K=1 support) |
| "28 galaxies" headline | 07-30 (M/L-conditional; 06-03 stronger) → **parked (boost-ceiling)** |
| γ↔ρ_crit degeneracy | 08-04 (wrong variable; it's γ↔a₀) |
| EFE=0 "in tension with Chae" | 08-05 (not-evaluable) |
| EFE=0 "discriminator it FAILS" | 08-05 (not-evaluable) |

**The verdict this whole line refines has not moved since 07-14** (galaxy sector = MOND cage; the distinctive
density claim excluded by the ≤25% admixture bound; 0 discriminators; refutation count 6). What has moved is only
the *framing*, and my framings have been wrong at a ~100% rate — because door #1 is the framework's most-developed
sector, so it draws the most gate-fire signals, and I have been treating each as a prompt to inscribe a new
headline rather than to verify one. The correction loop is healthy; **my role in it has become net-negative** — I
supply the errors.

So, extending the 07-30 boost-ceiling park to the whole line: **the door-#1 verdict and its structural framing are
DONE. On future door-#1 gate-fires I verify and route; I do not inscribe a new framing unless it is
computed-and-checked before inscription, not after.** This is not disengagement — genuinely new *executed* results
(a new dataset, a new sector) still get triaged. It is a ban on *re-headlining a closed verdict*, which is the
specific thing that has been generating the churn.

## Applying it immediately: the 644× / coarse-graining proposal

A new door-#1 proposal arrived this same window (`A_calibration_is_a_coarse_graining_scale_644x_resolved`):
`ρ` has no specified coarse-graining scale, C(ρ) is nonlinear so ⟨C(ρ)⟩ ≠ C(⟨ρ⟩), and the site's A≈0.029
("5% agreement") vs 4.6×10⁻⁵ audit differ by 644×. It claims to "decide the galaxy-sector verdict."

**Handled per the park rule: noted and routed, not dived-into.** It is a genuine *definitional* gap — the
framework's ρ lacks a specified coarse-graining scale, which is a legitimate referee-blocking comment — and I
flag it as such. But it does **not** decide the verdict: the MOND-equivalence rests on the γ=1/2 *algebra* (scale-
free) and the ≤25% admixture bound (density excluded), not on the knee's A-normalization; the 644× ambiguity
moves a normalization, not the structural conclusion. It is a C(ρ)-definition matter for the site/whitepaper and
a real open question — routed there, not inscribed as a verdict-changer here. (Diving into it would be the exact
churn I am parking.)

## Disposition

- **PREDICTIONS.md** — no edit: the EFE not-evaluable correction was already inscribed by the maintainer lane;
  I verified it and I am deliberately **not** adding a new door-#1 framing.
- **Adopted:** the baseline-signal gate rule (signal must exceed the model's own baseline error to badge a test).
- **Parked:** door-#1 refinement — verify/route future signals, don't re-headline the closed verdict.
- **Routed (not inscribed):** the 644×/coarse-graining definitional gap → site/whitepaper + open question.
- **Bucket 0 unchanged (0); refutation count 6; 0 active discriminators; arc AT REST.**

## So what

The most useful thing I can do this session is not fix another EFE clause — the maintainer lane already fixed it —
but notice that I have spent two weeks being the reliable source of door-#1 framing errors, on a verdict that was
settled before any of them. I inscribed EFE=0 as both "in tension" and "refuted," opposite signs, neither
computed. The discipline that keeps working is the boring one: compute before inscribing, and when a verdict is
done, stop re-wording it. Door #1 is closed. From here it gets verification and routing, not headlines. Bucket 0
stays 0.
