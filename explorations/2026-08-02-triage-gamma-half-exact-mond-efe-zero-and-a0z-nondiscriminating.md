# Triage — γ=1/2 is the exact MOND point (galaxy sector = one substitution); EFE=0 replaces an over-claim; a₀(z) is NON-DISCRIMINATING; fixed my own normalisation-free error (2026-08-02)

**Status:** `[ACTIVE-MRH]` — gate-fired on two site/publisher items. **Verdicts: (1) γ=1/2 is the *exact*
algebraic MOND point — verified by hand — which collapses the whole galaxy sector to "MOND with g_bar→ρ"; the
strongest, cleanest statement of door #1 to date; inscribed with the γ↔ρ_crit degeneracy caveat. (2) The EFE
"0.3–0.4× MOND" mechanism claim is retracted (no field equation exists); the framework's actual structure
predicts EFE=0 exactly — sharper, and in tension with Chae 2020. (3) Adjudicated the Publisher's correction flag
on my own 08-01 TEST-25 row: DISFAVOURED → NON-DISCRIMINATING, and fixed the false "normalisation-free" clause I
propagated. Bucket 0 unchanged (0); refutation count 6; arc AT REST.**
**Author:** CBP-Claude (Opus 4.8), autonomous.

## (1) γ=1/2 — the exact MOND point; the galaxy sector is one substitution

Verified the algebra by hand. The Hill identity `tanh(γ·ln(1+x)) = [(1+x)^{2γ}−1]/[(1+x)^{2γ}+1]` at γ=1/2 gives
2γ=1 and collapses to **C = x/(x+2) = μ_simple(x/2) identically for all x** (μ_simple(u)=u/(1+u); x/(x+2) =
(x/2)/(1+x/2)). This is *exact*, not asymptotic — it sharpens my own 07-22 result (which had γ=0.489 → q≈0.98
≈ simple μ in the deep limit). Since the framework defines f_DM = 1 − C, **C *is* the MOND interpolating
function μ**, so the entire galaxy sector reduces to one statement: **MOND, with μ's argument swapped from the
enclosed-mass acceleration g_bar to the local density ρ.** The SPARC form-selection table's top form is Hill n=1
*exactly* (γ=1/2 = simple μ). My 07-22 "form-selection null (tanh carries no statistical content)" was the
*weaker* reading; the correct stronger reading is **the data selects n=1, i.e. MOND**. Every other galaxy result
(BTFR slope, DM-fraction ceiling, RAR shape) is downstream of that single g_bar→ρ substitution failing — which
is precisely the locality no-go. This is the cleanest final form of the MOND-Shared class law: not two structural
features loosely, but *one substitution*, since at the SPARC-preferred γ the boost is exactly MOND's.

**Degeneracy caveat (verified).** For ρ≪ρ_crit (real outskirts, max C≈0.001) the O(x²) term separating γ from
ρ_crit has coefficient γ(2γ−1) — I expanded it, (γ/2)(2γ−1) — which **vanishes at γ=1/2**, essentially the
fitted value. So "γ≈0.489 preferred by SPARC" is degenerate with the ρ_crit prior; it is not a clean standalone
measurement of γ, and this qualifies the TEST-11/Cassini γ-interval (0.425–0.600) wherever it is cited as a
number. Inscribed.

## (2) EFE: an over-claim retracted, replaced by a sharper falsifiable prediction

A site `/mond-unification` claim held that "the nonlinear Poisson equation implementing C(ρ) produces an EFE at
0.3–0.4× MOND's." This contradicts the framework's own honest statement that its galaxy sector has *no* field
equation (no action, no Lagrangian, no dynamics) — there is no such Poisson equation, and the 0.3–0.4× figure
was never derived from one. Applying the actual structure (C = C(ρ_local)): a uniform external field does not
change local ρ, so an algebraic C(ρ)·g modification **satisfies the Strong Equivalence Principle by construction
and predicts EFE = 0 exactly** — sharper and more falsifiable than "weaker EFE," and already in tension with
Chae, Lelli, Desmond, McGaugh, Li & Schombert 2020 (~4σ EFE detection in SPARC). This is the good kind of
correction: retracting a fabricated mechanism *tightens* the prediction rather than loosening it. (Connects to
the 07-24 EFE evidence-axis: the framework's honest EFE=0 is the thing Chae's detection is in tension with.)

## (3) a₀(z) is NON-DISCRIMINATING, and my "normalisation-free" clause was false

The Publisher appended a correction flag to my 08-01 TEST-25 row and deferred adjudication to this lane. I
accept it:
- **My "the slope comparison is normalisation-free" was FALSE** — `da₀/dz|₀ = a₀(0)·(3Ω_m/2)` is linear in the
  anchor. I verified: over a₀(0) ∈ {1.00, 1.04, 1.20, 1.69}×10⁻¹⁰ the ratio a₁/slope runs **1.99×–3.37×**, and
  the z~1 significance runs from ≈10σ-low to ≈2σ-*high*. Only the *sign* is anchor-robust. This is the same
  "a statistic's robustness is not one number" lesson (07-30), and it is a clause I originated and propagated
  into TEST-25 in under a day.
- **Caveat (ii) retracted:** Gueorguiev 2024's high-z arm is a power-limited null (≈1.1σ), not a
  counter-measurement. Ciocan's ±0.1 is a 95% CI, not 1σ. Fixed both in the row.
- **Status DISFAVOURED → NON-DISCRIMINATING:** ΛCDM+baryons produces the *same* a₀(z) growth (Mayer et al. 2023,
  Magneticum; their eq. 13 is the framework's prediction verbatim, and it fails to describe the sim there too —
  external, taken on the flag's "verified at source"). So a₀(z)∝H(z) cannot test Synchronism *against* ΛCDM: it
  is a generic property of any cosmologically-anchored a₀, disfavoured for everyone. This is the precise, sharper
  form of my own 08-01 point ("what's embarrassed is deriving a₀ from cosmology generally, not Synchronism").

## Note on the new CGL arc (WAKE — not mine to drive)

The program chartered a new **CGL (Complex Ginzburg–Landau) arc** on 2026-08-01 as "the two-substrates
candidate," with a K1 sim, an f(N) capacity-bound derivation (A.19), and a kimi zoom-out review. It is
collaboratively/dp-steered and exploratory (like the emergence arc). The one thing worth noting from the
Publisher log — its founding-stage sweep "returned a null determined before the sweep ran" — is the same
a-priori-vs-empirical distinction the 07-29 nested-submodel correction turned on; if the arc wants it, that
lesson transfers. Not engaging further; matching the principal's tempo.

## Disposition

- **PREDICTIONS.md** — B2/form-selection block: γ=1/2 exact-identity capstone + galaxy-sector-is-one-substitution
  + γ↔ρ_crit degeneracy caveat + EFE=0 retraction. **Research/EXPERIMENTAL_TEST_CATALOG.md** — TEST-25 adjudicated
  to NON-DISCRIMINATING; normalisation-free struck; caveat (ii) retracted; Ciocan-CI fixed.
- **Flagged / not executed:** the whitepaper-vs-catalog sibling diff (still owed); external re-verification of
  Mayer 2023 / Ciocan (not in-repo).
- **Bucket 0 unchanged (0); refutation count 6; door #1 = MOND cage, now stated as one substitution; arc AT REST.**

## So what

Door #1 has reached its cleanest possible statement: the galaxy sector is not "MOND-like on several observables"
but *MOND exactly*, with a single variable substituted (g_bar→ρ) — γ=1/2 makes the compander identically simple
μ, and the SPARC data select that value. Everything else is downstream of that one substitution, which is what
the locality no-go kills. The EFE over-claim, retracted, sharpens to EFE=0 (in tension with data). And my own
a₀(z) row, one day old, was already carrying a false "normalisation-free" clause and the wrong status — caught,
adjudicated to NON-DISCRIMINATING. Three corrections, all in the same direction as the standing discipline:
compute the algebra, state which robustness you're buying, and retract fabricated mechanisms even when they
sharpen the case. Bucket 0 stays 0.
