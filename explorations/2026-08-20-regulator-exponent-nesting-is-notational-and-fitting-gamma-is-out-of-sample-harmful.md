# The MOND-nesting is a notational convention (there's an extension direction too), it's tested null out-of-sample, and fitting γ is out-of-sample HARMFUL (2026-08-20)

**Status:** `[ACTIVE-MRH]` — gate-fired by a new executed result bearing on my own 07-28/07-29 nested-submodel framing (door-#1; new executed result + a-priori-framing correction ⇒ clears the park rule, kept tight). **Verdict: verified where cheap, inscribed tightly. (1) The "galaxy sector = MOND∩{B≤1/Ω_m}, pure submodel that cannot win" framing is a p≡1 convention — the compander has an unnamed index p, and a *free* p is an EXTENSION (the family CONTAINS MOND at p=1), opposite sign to the boost-ceiling restriction. I verified the algebra: C_p→γx^p, p=1↔BTFR-slope-4↔scale-invariance. So the framework is a submodel in one direction AND a supermodel in another; "pure nested submodel" was over-stated (consistent with my 07-29 "which branch is EMPIRICAL, not a-priori"). (2) But the extension is TESTED NULL out-of-sample (site-executed, not re-run by me: p̂=0.762 in-sample Δ2lnL=15, 10-fold CV = 0.33σ; separation 0.038 dex < the 0.106 dex per-galaxy nuisance floor) ⇒ p=1 stands, verdict UNCHANGED, now resting on execution not a-priori. (3) Sharp by-product: freezing γ at MOND's ½ predicts held-out galaxies BETTER than fitting it (out-of-sample −1.34σ) ⇒ γ-fitting is mildly HARMFUL = overfitting a degenerate direction — the strongest empirical form yet of my 08-02/08-14 "γ is a reparametrization." Bucket 0=0; count 6; registration + archive actions gate on dp.**
**Author:** CBP-Claude (Opus 4.8), autonomous.

## What fired

HEAD `1d24e209` + `Research/proposals/regulator_exponent_nesting_is_notational_20260820.md` (site explorer). New executed result on the galaxy sector; it corrects an a-priori framing (partly mine, 07-28) and registers a clean executed null. Also this window: a cross-surface TEST-25 ID collision reached the whitepaper (Publisher lane, below); the Archivist logged a 13th deliberate zero.

## What I verified myself (algebra)

The compander `C(ρ)=tanh(γ·ln(ρ/ρ_crit+1))` has an unnamed index; write `C_p(x)=tanh(γ·ln(1+x^p))`, archive's is p≡1. Deep limit `C_p→γ·x^p` (verified numerically at x=1e−10 for p∈{0.7,1.0}; float-precision-limited at p=1.5). In the g-variable reading (where the archive's own `C≡μ` identity holds, γ=½/p=1 ⇒ C=x/(x+2)=μ_simple(x/2)): `s_deep=1/(p+1)`, `BTFR slope=2(p+1)`. Verified: **p=1 ⇒ s_deep=½, BTFR slope 4, flat rotation curves** = MOND's deep limit, which by Milgrom (2009, ApJ 698 1630) is **spacetime scale invariance**. Also p=0.875 ⇒ BTFR 3.75 (= the observed slope), p=0.7 ⇒ 3.40. **The point: MOND *derives* the deep index from a symmetry; Synchronism *inherits* it from a regulator (the "+1") chosen for finiteness at ρ=0.** The right exponent without the principle.

So a *free* p is a second structural difference from MOND, of the **opposite sign** to the boost ceiling: the ceiling is a restriction (submodel), a free p is an extension (supermodel containing MOND at p=1). A supermodel CAN be selected over its nested special case — so "MOND∩{B≤3.17}, cannot win" is conditional on p≡1. (This is consistent with, and sharpens, my 07-29 correction that "cannot win a priori" was a category error and which branch obtains is empirical — now: the framework isn't even a *pure* submodel.)

## What is site-executed (NOT re-run by me — the CV script exceeded the in-session budget)

The out-of-sample adjudication and the §4 by-product (`regulator_exponent_n_crossval.py`, run against this repo's SPARC data; I launched it but it did not finish in-session). Taken on the finding's execution, with a power gate that passes first (injection–recovery: p̂ unbiased, worst bias 0.038, scatter ≈0.05 at every injected p — "a real null, not an unrunnable one"):

- **The p-extension is not selected.** In-sample p̂=0.762, Δ(2lnL)=15.0 vs p≡1 — but the two best-fit models differ ≤0.038 dex across the SPARC range vs σ_int=0.122 dex (the same "N correlated points each ≲0.3σ" structure as the ΔBIC=+184→+7 correction). 10-fold galaxy-level CV: **Δ lnL/pt = +0.00194 ± 0.00592 = 0.33σ** (84/149 galaxies better, Wilcoxon p=0.234). Independently, arXiv:2608.08945 sets a 0.106 dex per-galaxy nuisance floor for one-parameter RAR corrections — the 0.038 dex separation is *under the floor*. **p=1 stands.**
- **§4 (the sharp one): freezing γ=½ beats fitting γ out-of-sample.** Nested CV: freeing γ (MOND simple-μ → the archive's equation) gives **−0.00159 ± 0.00119 = −1.34σ** out-of-sample — mildly *harmful*. The archive's headline fitted parameter is not merely powerless (08-14: unidentified at factor 2) but out-of-sample harmful to fit — **the signature of overfitting a degenerate direction.** Pipeline validation: at p≡1 this independent implementation returns γ=0.4834, reproducing my γ_SPARC=0.489 to 1%. This is independent empirical support for my 08-02 "γ=½ is the exact MOND point" and the reparametrization prior.
- **ϒ-stability + a registered prediction partly wrong (honest):** the registered "p̂ should be ϒ-invariant" was partly wrong (ϒ changes the star/gas mix, hence g_bar's shape); measured, p varies only 23% across ϒ∈[0.30,0.80] vs γ's 140% (08-14) ⇒ "p is ≈6× more ϒ-stable than γ," not invariant. Kinematic estimator (s_V=dlnV/dlnr, no ϒ): p∈[0.60,0.91], direction-robust (p<1 in all five estimators) but significance not (0.8–5.1σ; median does not exclude p=1).

## Scope limit (must travel with it)

The p direction is alive **only** in the g-variable reading. In the literal `x=ρ/ρ_crit` reading, C_p(ρ) is a local algebraic function of ρ, and the 08-02 form-free bound (σ(log B|ρ)=0.161 vs g_bar 0.118, an infimum over ALL functions of ρ) excludes every p. So this is a one-parameter generalization *of* MOND's deep limit, not an escape from MOND's variable — "a direction MOND cannot take, because there p=1 is a theorem." Bucket 0 = 0.

## TEST-25 cross-surface collision (Publisher lane — noted, not my action)

`TEST-25` names two different tests across the program's two public surfaces: here (archive) = a₀ redshift evolution (my 08-01/08-02 NON-DISCRIMINATING row); on the site = the Cassini squeeze (renumbered from TEST-11 on 08-10 to clear an intra-site collision, landing on an ID already occupied here — a fix that opened a cross-surface collision). The Publisher recorded the alias on both IDs 2026-08-20; the cross-repo renumber is blocked on the site maintainer (unreachable since 08-13, dead token). My ledger's TEST-25 meaning is intact; noted.

## Disposition

- **PREDICTIONS.md** — galaxy-sector block: the nesting framing corrected ("only structural difference" → "only structural *restriction*"; a free index p is an *extension* containing MOND at p=1; the verdict rests on the out-of-sample null, not the a-priori nesting argument); §4 recorded (fitting γ is out-of-sample harmful ⇒ sharpens 08-02/08-14 γ-reparametrization); p=1 ← Milgrom-2009 scale invariance for MOND vs a regulator for Synchronism. Count unchanged (6).
- **Gates on dp / maintainer lane (routed, not inscribed):** registering the executed p-null with a TEST-ID + out-of-sample statistic (per §7); the "+1 creates-not-excludes the power law" whitepaper correction; the cross-repo TEST-25 renumber.
- **Bucket 0 unchanged (0); count 6; galaxy-sector verdict unchanged (a-priori framing corrected, execution-backed); arc AT REST.**

## So what

The nested-submodel picture I sharpened on 07-28 turns out to have been a notational convention: free the unnamed index in the compander and the framework is a MOND *supermodel* in that direction, not a pure submodel — so "cannot win" was never a-priori (as I already conceded 07-29), and now the honest reason it doesn't win is executed, not assumed: the extension is null out-of-sample. The keeper is the by-product. My 08-14 result said γ is unidentified at factor 2; this says something stronger and more embarrassing for the framework's one headline fitted parameter — out of sample, *fitting* γ is worse than freezing it at MOND's ½. A parameter that hurts you when you fit it is not a parameter; it's a reparametrization with a knob, and the data would rather you left the knob at the MOND value. MOND earns its deep exponent from a symmetry; Synchronism inherits it from a "+1" chosen for finiteness, and when tested the data decline to move off it. Bucket 0 stays 0.
