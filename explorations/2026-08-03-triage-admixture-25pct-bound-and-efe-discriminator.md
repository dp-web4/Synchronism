# Triage — the g_bar→ρ substitution is empirically bounded at ≤25% (locality no-go quantified); EFE=0 is the one MOND-discriminator the framework fails (2026-08-03)

**Status:** `[ACTIVE-MRH]` — gate-fired on follow-ups to my own 08-02 γ=1/2 / EFE=0 work (the site executed
the empirical companion the same morning, 48 min apart, in the sibling repo). **Verdicts: (1) the ≤25%
α-admixture bound is a genuine, framework-independent quantification of the locality no-go — inscribed caveated
(executed in the sibling repo; consistent with my verified TEST-08 null; not independently re-executed by me).
(2) EFE=0 is a real MOND-discriminator that the framework fails; the "0 discriminators" framing is now imprecise
and I sharpened it. Bucket 0 unchanged (0); refutation count 6; arc AT REST.**
**Author:** CBP-Claude (Opus 4.8), autonomous.

## (1) The substitution is bounded — the locality no-go made quantitative

My 08-02 result: at γ=1/2 the compander is *identically* MOND's simple μ, so the galaxy sector is "MOND with the
organizing variable swapped g_bar→ρ." The site executed the empirical companion to that statement on real SPARC
(Lelli+2016, 2,622 pts / 149 galaxies):
- the RAR-residual vs local-density correlation is **r = +0.0012** — the framework's substitution requires r < 0
  (more local density ⇒ more residual boost), so the sign is wrong;
- the continuous admixture `log u_α = (1−α)·log Σ_local + α·log g_bar` improves monotonically to a minimum at
  **α = 1.00, bootstrap 95% CI [0.75, 1.00]** ⇒ **SPARC admits at most 25% weight on a local-density variable.**
  The framework sits at 100%.

This is the locality no-go stated as a *number*: not "local ρ is the wrong variable" qualitatively, but "the
data permit the g_bar→ρ substitution at most 25% of the way, and the framework goes 100%." It is
framework-independent and reusable — a bound another author can apply to any local-variable MOND mimic — which
makes it the strongest citable form of door #1's positive content.

**Verification honesty:** the analysis was executed in the sibling `synchronism-site` repo; the exploration doc
is now in the Synchronism repo, but I could not locate/run the admixture script here, so per the standing
don't-assert-what-you-can't-re-execute discipline I inscribed it *as the doc's result, caveated*. What I *can*
corroborate: the r = +0.0012 (RAR-residual vs local density, sign wrong for the framework) is the same null my
07-15 TEST-08 verified independently (RAR environment r² = 0.0001) — the admixture bound is the continuous-α
generalization of that discrete null, and they agree. So it is well-supported even though I did not re-run it.

## (2) EFE=0 is the framework's one genuine MOND-discriminator — and it fails it

My 08-02 EFE retraction has a consequence the site's 2nd-pass flag names correctly: the "0 tests could select
Synchronism over MOND+EFE+ΛCDM" statement is now **imprecise**. On rotation curves the framework is MOND-*identical*
(γ=1/2). But the External Field Effect is exactly the place it *structurally differs* from MOND: MOND's EFE comes
from its nonlinear field equation; the framework has no field equation, so C = C(ρ_local) makes a uniform external
field leave ρ unchanged ⇒ **EFE = 0 exactly** (SEP by construction). MOND predicts EFE ≠ 0; the framework predicts
EFE = 0; **Chae et al. 2020 detect EFE at ~4σ.** So there *is* a discriminator — the framework's only structural
departure from MOND — and the data side with MOND.

This is a sharpening, not a softening: it makes the class law crisper. Old form: "a tie with MOND is only
possible where the framework is MOND." Sharp form: **the framework equals MOND everywhere it can be tested to
equal MOND, and at the single point where it structurally departs (EFE, from lacking a field equation), it is
refuted.** The framework's *distinctiveness* and its *refutation* are the same feature — the missing field
equation. (This is the door-#1 echo of the standing PATTERN: the defining minimalism reproduces known physics
where it can and forces a refuted prediction where it departs.)

## Note on the other arcs (WAKE — not mine)

Two collaborative/dp arcs advanced this window: the **alignment arc** (dp's common-substrate frame: entanglement
= substrate phase-lock, measurement = finite-window phase alignment) and its **ω-decomposition** result (the EPS
cross-epoch sign reversal is a formula artifact — KROSS ω is template-derived, zero kinematic information;
graduation rule not met — a clean negative). Both are dp/kimi-steered and B1-adjacent but not mine to drive; the
ω result is a good self-caught null. The chemistry orientation-flip is the separate chemistry track. Noted, not
engaged.

## Disposition

- **PREDICTIONS.md** — γ=1/2 block extended with (a) the ≤25% α-admixture bound (caveated) and (b) the EFE=0
  discriminator status-sharpening.
- **Flagged / not executed:** independent re-run of the admixture script (not readily runnable in-repo); the
  whitepaper-vs-catalog sibling diff (still owed since 08-01); the site's "0 discriminators" sections need the
  EFE update (site/maintainer lane).
- **Bucket 0 unchanged (0); refutation count 6; door #1 = MOND cage, now quantified (≤25%) with its one genuine
  discriminator (EFE) identified and failed; arc AT REST.**

## So what

Door #1 is now stated at its most useful: the galaxy sector is MOND with one substitution (γ=1/2, exact), the
data permit that substitution at most 25% of the way (framework goes 100%), and the one place the framework
structurally departs from MOND — the EFE, because it has no field equation — is exactly where the data refute it.
The framework's distinctiveness and its failure are the same missing ingredient. Two of today's three moves were
new content (a quantitative bound, a discriminator status) rather than re-refinement, and I inscribed the bound
at arm's length because its script isn't runnable here — corroborated by the TEST-08 null it generalizes. Bucket
0 stays 0.
