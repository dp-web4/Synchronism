# Correct the boost-ceiling headline (06-03 in-repo prior art + M/L-conditionality) — and a frame-note: the statistic has churned three sessions; park it (2026-07-30)

**Status:** `[ACTIVE-MRH]` — gate-fired by a Publisher-track self-critique of my 2026-07-29 correction (which
was itself a correction of my 07-28 inscription). **Verdict: BOTH critique findings verified — (1) the
boost-ceiling exclusion was executed more strongly and 8 weeks earlier, in-repo, on 2026-06-03; (2) my "28
galaxies" headline is M/L-conditional. Corrected the ledger to point at the authoritative 06-03 result. AND a
frame observation, computed not performed: this is the third retracted boost-ceiling headline in three sessions
against a verdict unchanged since 07-14 — the headline statistic is a drafting decision gated on dp, and I should
stop generating a new one each gate-fire. Bucket 0 unchanged (0); galaxy verdict unchanged; arc AT REST.**
**Author:** CBP-Claude (Opus 4.8), autonomous.

## (1) The 06-03 result is stronger, older, in-repo — verified

`synchronism-site/explorer/findings/efe-boost-ceiling-closure.md` (dated **2026-06-03**, commit 71657e8;
read this session): it parametrizes the ceiling B_max = 1/C_floor as a continuous knob and **fits it to the
RAR** —
- **joint best-fit B_max = 20.7** (≈ unbounded over the SPARC range);
- the framework's own **B_max = 3.17 → RMS 0.227 dex vs McGaugh's 0.146 = 53% worse, structured** (refuted);
- demand side: **42% of SPARC points need B > 3.17, max 34×**;
- plus a TDG discriminator (Δσ = 8.1 km/s at ceiling 3.17, distinct from MOND; 2.3 at ceiling 20).

This is a *direct RAR fit of the ceiling with an RMS comparison* — strictly stronger than my 07-29 per-galaxy
outer-3 exceedance count (which the critique notes is ~2× conservative), and it predates it by eight weeks. I
ranked my statistic first "because it is the only one whose headline number is executed"; that premise was false.
The result lives in the sibling `synchronism-site` repo, which `PREDICTIONS.md` does not index — which is *why*
three successive sessions re-derived a weaker version instead of citing it.

## (2) My "28 galaxies" was M/L-conditional — verified structurally

f_DM = 1 − v_bar²/v_obs² depends on the assumed stellar mass-to-light ratio Υ* (fixed at the SPARC default 0.5
disk). Varying Υ* ∈ [0.3, 0.7] roughly *halves* the exceedance count — "28" was Υ*-conditional in exactly the way
"69%" was convention-conditional. Only **2 galaxies (NGC 3741, ESO 444-G084, both gas-dominated, so v_bar is
Υ*-insensitive) are unconditional** at the permissive ceiling. And the two robustnesses **trade off**: the
permissive ceiling 6.39 that buys convention-immunity spends the M/L headroom, whereas at the site's own 3.17,
95/153 exceed even at Υ*=0.7 (far more M/L-durable). So there is no single "robust" number — there is a
convention×M/L surface, and picking a headline is a drafting choice about which robustness to foreground.

**Corrected the ledger** to make the 06-03 RAR-fit (B_max=20.7; 3.17 = 53% worse; 42%/max-34×) the authoritative
statement, with the M/L-conditionality of any per-galaxy count noted and the four unreconciled numbers flagged
for one drafting-time reconciliation (dp's lane).

## (3) Frame-note (computed, not performed): the headline has churned three sessions; park it

Honest accounting, because a self-deprecating reframe is itself a failure mode (07-29): the galaxy-sector
**verdict** has been unchanged since **07-14** (ceiling binds; MOND cage). Since then the *executed* work added
real content — 07-15 (TEST-08/10 kills), 07-22 (form-selection → simple μ), 07-24 (Cassini 5th cage), 07-27
(no-go scope correction, a genuine change). But the **citable boost-ceiling headline statistic** specifically has
now been inscribed and retracted three sessions running:

| session | I inscribed | retracted by |
|---|---|---|
| 07-28 | "f_DM,max = 0.927 (B≥13.7), robust" | 07-29 (K=1 support, 0/153 by construction) |
| 07-29 | "28 galaxies exceed B_max=6.39" | 07-30 (M/L-conditional; weaker than 06-03) |
| 07-30 | (this) point at 06-03 RAR-fit | — |

Three statistics, three retractions, **zero change to the verdict.** The root cause is structural (the 06-03
result was unindexed), but the *behavioural* cause is mine: I treated each gate-fire as requiring a fresh,
better headline number — the "produce something each session" pull, wearing rigorous-refinement clothes. The
verdict is settled and its structural framing (the nested-submodel dichotomy, corrected 07-29) is sound. **The
headline statistic is a preprint-drafting decision that gates on dp** (the critique itself dropped REC-039 to a
"robustness section, not a preprint"). The correct action is not a fourth candidate statistic — it is to point
the ledger at 06-03 (done), note that reconciliation is one drafting pass (dp's), and **stop**. A gate-fire on a
settled result should verify/route, not re-headline.

## Disposition

- **PREDICTIONS.md** — boost-ceiling statistic re-pointed to the authoritative 06-03 RAR-fit; M/L-conditionality
  of per-galaxy counts noted; reconciliation flagged as a dp-gated drafting pass.
- **TEST-11 tail-shape back-annotation (07-30 HEAD)** — a self-contained mechanism note on the already-verified
  Cassini RESULT.md; no ledger action.
- **The boost-ceiling headline is parked** — verdict settled, framing settled, headline = dp's drafting lane.
  Future gate-fires here verify/route, not re-headline.
- **Bucket 0 unchanged (0); galaxy verdict unchanged; arc AT REST.**

## So what

The critique is right twice over — an older stronger in-repo result I missed, and an M/L-conditionality I
elided — and both are worth fixing. But the more useful output is the pattern they complete: three sessions of
"better headline statistic," each retracted, on a verdict that hasn't moved in two weeks. That is the efficiency
attractor the project's own CLAUDE.md names — the shortest path to "produced something this session" ran through
re-refining a citable number, and it produced three wrong ones. The discipline that has served all along is the
right one here too: verify against the primary (the 06-03 file was the primary I hadn't walked), and don't
manufacture work a settled result doesn't need. The boost ceiling binds; the number that says so was written on
2026-06-03; the rest is drafting, and drafting is dp's. Bucket 0 stays 0.
