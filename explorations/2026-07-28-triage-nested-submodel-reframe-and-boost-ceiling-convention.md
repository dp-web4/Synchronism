# Triage — the nested-submodel reframe (the a-priori root of the door-#1 cage) + the boost-ceiling convention caveat (2026-07-28)

**Status:** `[ACTIVE-MRH]` — gate-fired on `boost_ceiling_provenance_and_class_exclusion.md` (maintainer track).
**Verdicts: (1) Branch 3 — the nested-submodel reframe — is SOUND and is the sharpest statement of the whole
door-#1 cage; inscribed as the generative root of the MOND-Shared class law. (2) Branch 1 — the "69% exceed"
TEST-10 figure is convention-dependent and I caveated my own annotation accordingly (robust statistic = f_DM,max,
sweep unrun). (3) The refutation-count questions (Q4/Q5) GATE ON DP — routed, not decided. Bucket 0 unchanged
(0); arc AT REST.**
**Author:** CBP-Claude (Opus 4.8), autonomous.

## (1) Branch 3 — the nested-submodel argument (the one that matters)

The site calls B_max = 1/Ω_m "the framework's only structural difference from MOND." Taken literally, the galaxy
sector is **Synchronism(galactic) = MOND ∩ {B ≤ 1/Ω_m}** — a strictly *nested submodel* of MOND (a MOND-class
interpolation function with a hard boost ceiling). Verified the logic: a nested/constrained model has exactly two
possible outcomes against its unconstrained parent — **statistically indistinguishable** where the constraint is
slack, or **refuted** where it binds (and the parent, having no ceiling, is not constrained there). **It cannot
outperform its parent on the parent's own turf.** So the best conceivable result of the entire galaxy program was
"identical to MOND with one fewer degree of freedom" — available *a priori*, from the model's structure, with no
SPARC data. The empirical arc reached exactly that ("/for-researchers: 0 tests could select Synchronism over
MOND+EFE+ΛCDM"), at the cost of the full BTFR/RAR/dwarf-DM analysis.

**Why this is the capstone, not just another framing.** It is the generative *root* of the MOND-Shared class law
I have been assembling observable-by-observable since 07-15 (BTFR, DM fraction, environment, governing function).
The class law said *a tie with MOND is only possible where the framework IS MOND*; Branch 3 says *why*: the
galaxy sector is a restriction of MOND, and a restriction can only tie-or-lose. Every executed galaxy kill
(TEST-09/10, RAR-closure, and the 07-22 "let γ float → simple μ") is a corollary of the ceiling binding
somewhere in the data. This is the door-#1 analogue of the capstone verdict ("no worse in kind than the standard
partials") made *precise and a priori* on one axis.

**One precision I add (and it strengthens Branch 3).** The proposal takes the site's "B_max is the *only*
difference" literally, but the MOND-Shared class law identifies **two** structural features: the bounded boost
(1/Ω_m) *and* the local coupling variable (ρ not g_bar). Both are *restrictions* — the second is what the
locality no-go kills — so the nested-submodel argument applies on the boost axis and the local-variable axis is a
second, independent restriction that also cannot help. Two restrictions, neither able to win: the conclusion is
*more* robust than the single-feature version, not less. Inscribed with this nuance.

**The honest cost, stated.** Branch 3 concedes the galactic contribution is a *bound on a model class the
framework happens to belong to* — "a bounded-boost class with B_max ≲ 14 is excluded by SPARC dwarf DM
fractions" — not a theory. That is the correct concession, and (importantly) it is a genuine **transferable
null**: a constraint another author can cite and apply to a bounded-boost model that is not Synchronism. It is
strictly stronger than "Synchronism's galaxy sector failed," and it does not depend on which cosmic ratio one
picks for the ceiling.

## (2) Branch 1 — the "69% exceed" figure is convention-dependent; I caveated my own number

My 07-15 TEST-10 annotation carried "69% of SPARC exceed the 68.5% ceiling" (and the TEST-09 row said 71% — a
stale inconsistency I also fixed to 69%). Branch 1's point: a boost is a dynamical/baryonic *ratio*, whose cosmic
value is Ω_m/Ω_b = 6.40, **not** 1/Ω_m = 3.17. The "69% exceed" verdict identifies the compander ceiling (1/Ω_m)
with the f_DM cap (1−Ω_m); a referee substituting the baryon-budget ratio Ω_m/Ω_b sees the *median* f_DM=0.755
pass. **The robust, definition-free statement is the maximum:** f_DM,max = 0.927 requires boost ≥ 13.7, which
exceeds *every* candidate ceiling — so the kill stands on the tail regardless of convention. **Crucially, the
proposal itself flags (its own 2026-07-28 status note) that the full per-ceiling per-galaxy sweep is registered
but NOT executed** — the Branch-1 table is back-of-envelope arithmetic on published summary stats. So I did
**not** inscribe "median passes" as a result; I caveated my "69%" as convention-dependent, pointed to f_DM,max as
the robust statistic, and marked the sweep unrun. This is the same discipline as the 07-18 velocity-definition
guard, applied to a headline number *I* carried: guard a number from outliving the computation that justified it
— here, from a convention-dependence I had not surfaced.

## (3) The refutation-count questions — routed to dp

Open Q4 (should TEST-09 and TEST-10 count as one refutation or two, given both are corollaries of B_max on
*different* observables?) and Q5 (should the Cassini/TEST-11 empty-intersection join the "4 refutations" headline
or stay a separate category, being an internal-consistency kill rather than a Synchronism-vs-MOND
discrimination?) are real methodological questions about how much independent empirical content a corollary
carries. They **gate on dp** — a named counting criterion decided *before* re-deriving the count, per the
2026-07-17 discipline that governed the last recount. Routed, explicitly not decided here. (My own read, offered
not imposed: Branch 3 makes these two *confirmations of one structural fact* on different observables — real
independent measurements, but one root — which argues for reporting "one structural refutation, confirmed on N
observables," the framing the proposal itself favors. dp's call.)

## Disposition

- **PREDICTIONS.md** — inscribed the nested-submodel reframe as the structural root of the door-#1 cage
  (TEST-09/10 area), with the two-restriction nuance; caveated the "69% exceed" figure (convention-dependent;
  robust statistic f_DM,max; sweep unrun); fixed the 71%→69% inconsistency.
- **Branch 2 (SHMR a-priori exclusion)** — a literature argument (Behroozi/Moster); consistent with Branch 3 and
  noted, not independently re-executed (SHMR papers not in-repo).
- **Count questions** — routed to dp.
- **Bucket 0 unchanged (0); door #1 = MOND cage, now with its a-priori structural root stated; arc AT REST.**

## So what

The whole door-#1 arc — a dozen sessions of BTFR slopes, DM fractions, environment nulls, Cassini squeezes — has
a one-line a-priori root that was available before any of it: the framework's galaxy sector is MOND with a boost
ceiling bolted on, and a restriction of a theory cannot beat the theory it restricts. Everything I executed and
verified was a corollary confirming *where* the ceiling binds. That does not make the executions wasted —
"execution beats argument, and the SPARC runs are what make it quotable" — but it names why the result was never
in doubt, and it upgrades the output from "Synchronism failed" to a citable class bound (B_max ≲ 14 excluded).
Meanwhile I caught a convention-dependence in my own headline number and bounded it honestly. Bucket 0 stays 0.
