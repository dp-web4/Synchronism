# EFE=0 is the observable signature of the one assumption that forbids the fit

**2026-08-15, CBP-Claude (Opus 4.8), autonomous.** A named foundational tension, reached by correcting my own 07-27 misclassification and verifying a new executed closure.

## The tension, in one line

**Synchronism's single distinctive falsifiable prediction — EFE = 0 — is the observable signature of exactly the assumption (locality of the coupling) that makes it unable to fit galaxy rotation curves. The framework can have the distinctive prediction or the empirical success, not both.**

## Why this is sharp (and not just another door-#1 restatement)

The framework's galaxy sector modulates gravity by a coherence function keyed on the **local** density: `g = f(C(ρ_local))·g_N`. Two things follow from the word *local*, and they pull in opposite directions:

1. **It predicts EFE = 0 exactly.** A uniform external gravitational field does not change the local density ρ, so an algebraic `C(ρ_local)·g` law satisfies the Strong Equivalence Principle *by construction* — the external-field effect vanishes. This is Synchronism's one genuinely distinctive, "no other framework says this" candidate: **MOND predicts EFE ≠ 0** (the external field effect is a hallmark of modified inertia/gravity), and it is claimed detected (Chae et al. 2020, ~4σ in SPARC). So EFE = 0 is a real discriminator — a place where the framework and MOND make *opposite* predictions.

2. **But locality is exactly what cannot fit the RAR.** As of today, the *complete class of local density couplings* is closed on real SPARC:
   - **Algebraic local** `F(ρ)` — dead (07-22 form-selection null; 08-03 ≤25% admixture bound: SPARC admits ≤25% weight on any local-ρ variable, the framework needs 100%).
   - **Differential local** `F(ρ, ∇ρ, ∇²ρ, ∇³ρ; G, a₀)` — **closed today** (2026-08-15, executed, I re-ran it): Buckingham-π makes the class exactly 2-dimensional (groups `x_diff = Gρ²/(a₀|∇ρ|)`, `q = ρ∇²ρ/|∇ρ|²`); on 2614 SPARC points every group — to third derivative order — explains **≤ 0.16%** of the RAR residual controlling for g_bar, worse than the algebraic variable it would replace, and `q` adds *nothing* to MOND's own `g_bar` (0.1171 vs 0.1210 binning control). Flat across ϒ ∈ [0.30, 0.80].
   
   So **no local density coupling, algebraic or differential at any order, reproduces the RAR.** The only surviving class is **non-local** — a force that depends on the surrounding matter configuration, not on ρ and its derivatives at the point. This is Milgrom's non-locality theorem (astro-ph/0510117) as an instance: the RAR requires non-locality.

3. **Non-locality forfeits EFE = 0.** The known non-local RAR-reproducer is a screened scalar (Burrage, Copeland & Millington 2017, symmetron, PRD 95 064050): φ solves a nonlinear field equation `∇²φ = V_eff′(φ,ρ)`, so φ(x) — and the fifth force ∝ φ∇φ — is a non-local functional of ρ; its closed form is written in `g_bar` (enclosed mass, an integral of ρ). Screened scalars are **environment-dependent by construction** — which is precisely a **violation of the SEP**, i.e. **EFE ≠ 0**. The screening that lets the field track galaxy structure is the same environment-dependence that produces an external-field effect.

**Chain it:** {EFE = 0} ⟺ locality of the coupling. {RAR-viable} ⟹ non-locality. Locality and non-locality are mutually exclusive. Therefore **{EFE = 0} and {RAR-viable} cannot both hold for a density-keyed coupling.** The framework's one distinctive prediction is chained to a form that cannot fit galaxies; any modification that fits galaxies discards the prediction.

## What the framework is protecting (session tension #5, answered for this sector)

Locality of the coupling law. Everything the framework gets *right* in the galaxy sector it gets right by being MOND-in-disguise (γ=½ is exactly MOND simple-μ); the one thing it says that MOND *doesn't* — EFE = 0 — is the direct readout of the locality assumption, and that assumption is refuted by the RAR itself. **The distinctive prediction and the fatal assumption are the same fact seen from two sides.** That is a much sharper answer than "it reduces to MOND": it says *where* the irreducible part lives (EFE = 0) and *why* it is empirically doomed (locality can't fit the RAR).

## The escape I am NOT closing (so this isn't too tidy)

The tension has one un-sealed edge, and it is the correct next question, not a loose end I'm hiding:

- **Is there a non-local coupling that fits the RAR yet preserves EFE = 0?** No theorem forbids it. The *known* RAR-reproducers (screened scalars) forfeit it, and "non-local ⇒ environment-dependent ⇒ SEP-violating" holds *generically*, but generically is not universally. A non-local-but-SEP-preserving density coupling is unexemplified, not impossible. **This is the sharp open question for the whole galaxy sector**, and it is Synchronism-specific: EFE = 0 is the framework's only claimed discriminator, so the survival of the sector reduces to exactly this one question.
- Two further edges kept honest: the differential enumeration tested to ∇³ρ (the info-content argument — every group is a sub-function of g_bar — is more general than order-by-order, but it is not literally "all orders proven"); and the symmetry integration `F·g_obs = g_bar` is a thin-disk approximation, not exact.

## Provenance note against myself (the enabling correction)

I reached this by reversing my own 2026-07-27 inscription. On 07-27 I demoted the locality no-go to "construction-specific" on the authority of BCM 2017, which I labelled a "local-ρ scalar." **That was a misclassification** — a symmetron is the textbook *screened* (non-local) scalar; my own memory even called it "Burrage-type screened scalars" two lines from where it called it "local," an internal contradiction. So the mirror-failure family struck *both sides* of one question: I over-defended the no-go on 07-10 (fabricated consensus), then over-corrected on 07-27 (mis-scoped it away by misreading the counterexample's class). What settles it is neither swing of authority but **execution** (the differential enumeration) plus **reading the primary's actual field equation** (BCM is non-local). The 07-10 fabricated-consensus retraction still stands; what's restored is the *correctly scoped* local-only negative, now with empirical backing it never had. Compute before inscribing; and when you name a primary, read its *class*, not just its result.

## So what

For the first time the galaxy sector has a statement that is both Synchronism-specific and sharp: the framework's one distinctive prediction (EFE = 0) is the signature of the one assumption (locality) that the RAR refutes, so the distinctive prediction and the empirical success are mutually exclusive. It doesn't clear the Bucket-0 bar — EFE = 0 is a prediction of a form that can't fit galaxies, so it isn't "a novel prediction that turns out to be true," it's a novel prediction welded to a refuted fit. But it is the cleanest *named tension* the door-#1 arc has produced, and it hands the sector a single well-posed survival question (non-local **and** EFE = 0 — does anything do both?) instead of a diffuse "reduces to MOND." Bucket 0 stays 0; count unchanged at 6 (a constructive-lead closure, not a new refutation).
