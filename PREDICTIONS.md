# Synchronism — The Prediction Ledger

*The honest scoreboard. Every claim the program treats as a prediction, in one of four
buckets, each row carrying its **named refutation criterion**. This document exists to
**stop the framing from oscillating.** The README and all framing prose must agree with
this table; when they drift (toward overclaim *or* toward self-erasing undersell), this
ledger is the correction.*

**Four disciplines this ledger enforces:**

1. **Confirmed ≠ untested ≠ refuted ≠ nonexistent.** The headline "zero confirmed novel
   predictions" is **true and stays** — but it must not be allowed to *erase* the
   untested-but-falsifiable bucket. "We have made no confirmed novel predictions" and "we
   have several untested falsifiable novel predictions" are **both true**.
2. **Untested is not a win.** A novel-in-structure prediction that nobody has tested is a
   *bet*, not a result. We list bets as bets, with the criterion that would settle them.
3. **Reparametrization is the intended stage-1 test, not the failure mode — *and* not yet the
   discovery.** Heliocentrism did not change the universe or "create new physics"; it changed the
   *frame*, reproduced the *known* (planetary positions — at first *less* accurately than refined
   epicycles), and only *later* paid out (Newton; parallax, 1838). The point of this program is
   not to "create new physics" but to build a frame that *might* explain what current models fail
   at — and the disciplined first step is to test its explanatory power over **what is already
   known**. That step **is reparametrization, by definition and by intent.** So "reproduces known
   physics" is the *assignment* at this stage, not a demerit. **But the license is a loan, not a
   grant**, governed by two bars: (a) *productive vs degenerate* — a reparametrization earns its
   keep only when the new frame **simplifies / reveals structure / unifies** (degenerate = relabel
   without illumination); and (b) *the loan must be repaid* — the frame has to commit to eventually
   being pushed where current models fail and made to yield a real prediction, or "we're still in
   stage 1" decays into epicycles-forever. Judging stage-1 reparametrization by a stage-2
   novel-prediction rubric (and scoring it ~0) is a category error — it would have failed
   heliocentrism in 1543. *Live productive instance:* the inverted-frame gravity result
   ([explorations/2026-06-22-phase3c-…](explorations/2026-06-22-phase3c-inverted-frame-gravity-as-substrate-inflow.md))
   reproduces GR light-bending but **simplifies** (dissolves the Yukawa obstacle as a frame
   artifact, *derives* the equivalence principle GR assumes, unifies bending + time-dilation into
   one flow term) — clears bar (a). Bar (b) — the frontier prediction — is unpaid: still Bucket 0.
   **⚠ scope correction (Phase-14, 2026-06-23): this reparametrization holds only in the STATIC
   sector.** The same inflow model in the RADIATIVE sector is *not* GR-in-coordinates — flat space +
   a scalar irrotational substrate has **no spin-2 DOF**, so it predicts scalar (spin-0) GW
   polarization, refuted by binary-pulsar timing + LIGO polarization (see Bucket 2). So the gravity
   sector is a *productive reparametrization statically* but a *refuted distinct theory radiatively* —
   not a single clean coordinate change.
4. **Flat floor ≠ refutation (the saturation reading).** Saturation makes emergence a
   *step* function — flat floor → step → ceiling-that-becomes-the-next-floor (see
   [SPINE.md](SPINE.md) and FUNDAMENTALS §3). A flat result can be a *pre-step floor*, not a
   dead end; a bounded plateau is the mechanism working, not failing. **This is a reading
   guide, not a license to un-refute:** entries in Bucket 2 below were *tested against data
   and failed* (wrong sign, dimensional error, arithmetic that doesn't survive) — those are
   genuine eliminations and stay. The lens only forbids mistaking "nothing yet" (a flat
   floor) for "it failed" (a refutation). They are different claims.

> **⚠ Frame recalibration (2026-06-24 — symmetric standards + the data's MRH).** A frame-level
> audit (dp, [`explorations/2026-06-24-data-mrh-and-symmetric-standards-recalibration.md`](explorations/2026-06-24-data-mrh-and-symmetric-standards-recalibration.md))
> softened the arc's "physics structurally closed/refuted" verdict — **without moving any bucket.**
> Two corrections, both about *standard* and *data-MRH*, not about the counts: (1) **Symmetric
> standards** — judging Synchronism by a "unified + predictive everywhere" bar is asymmetric, since
> *all* physics is partial/posit/fit-laden (GR ⊥ QM; MOND's `a₀` and dark matter are themselves
> fits; Λ is the worst fine-tuning in science). The honest verdict is **a partial model among
> partial models, no worse *in kind*** — **a fairer standard, NOT a rescue.** (2) **The data's
> MRH** — the galactic "no-go" (B2 / door #1) is a no-go *scoped to the equilibrium-from-snapshot
> interpretation* of rotation curves (we watch ~10⁻⁷ of one orbit; equilibrium is an assumption a
> decades-long snapshot cannot establish — the same interpretation dark matter and MOND also fit).
> The binary-pulsar / lab-QM anchors are robust to this critique (decadal / repeatable) — so the
> **radiative refutation (Bucket 2) and Hulse–Taylor STAND; this scopes, it does not un-refute.**
> Constructive output: door #3 — **secular / time-domain observables** (drift of "constants" vs the
> absolute tick, pulsar-timing-array correlations, dissipation/arrow-of-time, cosmological
> evolution) — is the genuinely-open DIRECTION where the snapshot is weakest and the absolute-time
> content most distinctive. **It is an untested *direction*, NOT a registered bet — no specific
> falsifiable prediction yet, so it is deliberately NOT added to Bucket 1.** **Bucket 0 = 0,
> unchanged.**
>
> **⚠ Phase-16 (2026-06-24) — door #3 has a specific internal gate.** Its most natural novel
> prediction is **intrinsic decoherence ∝ D·k²** from the *dissipative* substrate (S666; standard
> QM is unitary, so this is genuinely distinctive — and notably *near-reach*, τ~days–yr at
> atomic/nm scale for Planck diffusion, **not** Planck-killed). But the dissipative substrate
> **cannot host matter** (S617/S665/S666: it disperses; that is why Phase-1 needed a *conservative*
> wave+focusing substrate), and the conservative matter substrate carries **no** intrinsic
> decoherence. So the door-#3 signature lives in the substrate that cannot make the matter to
> exhibit it — the "two substrates connected only by narrative" problem (Bucket 2) is the **gate on
> door #3**, and this realizes the long-standing oscillation-vs-dissipation tension (they are
> *competing*, not harmonious). Door #3 becomes a real bet **iff** the framework commits to
> dissipative-fundamental with matter as a *dissipative soliton* (focusing balancing dissipation),
> **or** unifies the two substrates so the dissipation is a derived property. Until then: open but
> obstructed, with a named gate — not closed (per the recalibration), not yet a bet.
> See `explorations/2026-06-24-phase16-door3-dissipation-vs-oscillation.md`.

*Companion docs: [SPINE.md](SPINE.md) (the argument), [STATUS.md](STATUS.md) (live
MRH-state), [FUNDAMENTALS.md](FUNDAMENTALS.md) (definitions).*

---

## Bucket 0 — CONFIRMED NOVEL PREDICTIONS

**Count: 0.**

Per the project's own novelty ledger (Session #660A): across ~3,360 sessions, **zero**
predictions that (a) follow from Synchronism postulates, (b) differ from standard physics,
and (c) have been tested and passed. This number is load-bearing and is not softened
anywhere in the repo. When it changes, it changes *here first*, with the test that moved it.

> **Scope: this ledger is the *physics* axis only.** "Zero confirmed" means zero novel
> *physics predictions* — it does **not** mean "useless." On the *applied* axis (Synchronism
> as a design ontology for coherence/emergence/governance) the framework is already
> load-bearing in running code (MRH→Web4, coherence→SAGE, fractal societies→hestia/the hub,
> the live fleet). See **[SPINE.md → "What it's already good for"](SPINE.md)**. Two axes,
> both honest; don't let this one's "zero" erase the other's delivered value.
>
> **⚠ Precision (2026-06-24): the applied value is GENERATIVE, not CONFIRMATORY, and is
> logically *independent* of the physics being true.** Applying this ledger's own
> productive-vs-degenerate test to the applied axis: MRH is — per its own foundation doc
> (`web4/.../MRH_THEORETICAL_FOUNDATION.md`) — an *extension of the Markov blanket* = a
> weighted, typed, hierarchical relevance graph (RDF ⊕ soft relevance ⊕ holarchy ⊕ active
> inference). A **productive synthesis/vocabulary**, genuinely used — but **not a novel
> primitive** (everything it does is expressible in established concepts), and its usefulness
> holds **regardless of whether the intent-substrate ontology is correct** (it "aligns with"
> the Free Energy Principle just as well). So the applied axis demonstrates Synchronism's
> *generativity* (it inspired useful design syntheses), **not** the truth of the physics
> "one move." Keep both clauses, but don't let "applied is load-bearing" be read as evidence
> for the physics bailey — that's the category slide. See
> `explorations/2026-06-24-applied-axis-generativity-not-confirmation.md`. (Flagged for SPINE.)

---

## Bucket 1 — UNTESTED BUT FALSIFIABLE (genuine novel bets)

These are novel **in structure** — each makes a claim that, if tested, could distinguish
Synchronism from the standard frame. **None has been confirmed.** They are the program's real
open bets. Listing them is the antidote to the undersell; flagging them *untested* is the
antidote to the overclaim.

> **Mostly untested for lack of instruments, not lack of merit.** The lab has no accelerator,
> cryostat, interferometer, or neural lab; physics bets here are checked (when at all) against
> data others collected for other purposes. That's why some can be *refuted* (a derivation
> fails) but none can be *confirmed* this way. Two of these would matter well beyond
> Synchronism if a lab ran them — **entanglement-as-phase-sync** (quantum computing /
> semiconductors) and **"time dilation"-as-instrument-effect** — see the **invitation** in
> [SPINE.md](SPINE.md).
>
> **⚠ Flag (Phase-9, 2026-06-23): the "time-dilation-as-instrument-effect" bet is undercut by
> the arc's own gravity model — needs relocation, not deletion.** The bet's only discriminator
> from GR is **mechanism-dependence** (different clock mechanisms respond differently — the
> pendulum-vs-atomic-clock point). But the inflow model
> ([Phase-9](explorations/2026-06-23-phase9-matter-sector-precession-and-instrument-effect-tension.md))
> advects *every* pattern by one universal `u(r)` — that universality **is** the equivalence
> principle Phase-3c celebrates deriving — so it predicts **clock universality**, i.e. *no*
> mechanism-dependence: identical to GR by construction. EP-derivation and mechanism-dependence
> are mutually exclusive, so the continuum inflow result **removes the bet's handle rather than
> vindicating it.** Survival path = the same door as discreteness/LIV: if sampling makes different
> patterns sample the grid differently, universality breaks at the LIV order and the bet regains
> falsifiable content (testable against existing tight clock-universality bounds). Operator call
> on whether to re-list it as a discreteness-conditional bet.

> **📌 DATED SCOPE NEGATIVE — the framework has NO dark-energy sector (registered 2026-07-22).** DESI DR2's
> BAO preference for evolving dark energy (w₀wₐCDM over ΛCDM at ~2.8–4.2σ depending on SNe compilation) is the
> live cosmological anomaly, and **Synchronism is silent on it.** C(ρ) modulates the effective gravitational
> response to *matter* density; nothing in the framework sources, modifies, or couples to the expansion history,
> so there is no mechanism producing w ≠ −1 or w(z) evolution. **Verified before registering** (grep of
> SPINE/FUNDAMENTALS/PREDICTIONS/STATUS): no DE machinery exists. ⚠ **Disambiguation that makes this
> registration load-bearing:** the repo *does* contain an "equation of state" — but it is the **substrate's
> internal** `P ∝ ρ^γ` (open question OQ-EOS, replacing the broken `P = I_max − I` whose `c_s² < 0`), a
> different object entirely from a cosmological w(z). A future reader grepping "equation of state" must not
> mistake the substrate EOS for dark-energy machinery. **Consequence:** if DR2+ confirms evolving DE, the
> framework inherits it passively — it neither predicted nor forbids it. This is a **scope negative**: the
> advertised density→coherence→cosmology machinery touches only the *growth* side, and its one growth number
> (σ₈≈0.76) is post-hoc (S102) and underpowered on the registered fσ₈ statistic (2026-07-14). Registered
> **now, dated**, as cheap insurance against the retro-fit pattern the archive has already exhibited three times
> (S193's BTFR rescue, TEST-03's late threshold, TEST-04a's statistic swap): any later claim that coherence
> "explains" w₀wₐ is to be judged against this statement that no such coupling exists in the framework as
> specified. Same discipline as the DR2 three-branch pre-commitment below.
>
> **✅ PROSPECTIVE REGISTRATION — DESI DR2 / TEST-04a (adopted by dp, 2026-07-17).** The program's
> **first genuinely prospective test** (the pre-registration audit found 0/10 — every prior provenance
> failure lived in *retrospective* registration). ⚠ **OPEN TIMING-VERIFICATION ITEM (2026-08-01):** a DESI DR2
> presentation ("Cosmology with DESI DR2: From BAO to Full-Shape Clustering," PIRSA:26040071) was given **April
> 2026** — 3 months *before* the 07-17 adoption (the fσ₈≤0.46 threshold itself was git-committed 07-01/07-14,
> also after the talk). "Prospective" is a claim about what was *knowable*, not just published, so if that talk's
> slides already showed a threshold-relevant fσ₈(z≈0.5) figure, the "genuinely prospective" framing is
> compromised (the same failure mode as TEST-04a's original post-hoc σ₈ calibration). **This is UNRESOLVED and
> I cannot close it — it requires watching/reading the PIRSA recording, which is not in-repo.** Until someone
> with access confirms the talk showed no threshold-relevant number, the "first genuinely prospective" claim
> carries this caveat. The registered statistic + branches are unaffected; only the *prospectivity* claim is at
> risk. Pre-committed **in writing before DR2 publishes
> (~Spring 2027)**, adjudicated within 7 days of publication, ledger-first, on the **registered
> statistic** (DR2 full-shape **fσ₈ at z≈0.51** — *not* σ₈, per the 2026-07-14 correction). Three
> branches, all fixed now so none can be chosen after the data:
> - **A — fσ₈ ≤ 0.46:** "registered criterion met by suppression direction, but the prediction remains
>   post-hoc (σ₈≈0.76 was calibrated to S8 in S102)." **Bucket 0 stays 0** — a retrodiction agreeing
>   with new data is not a novel prediction (generativity ≠ confirmation).
> - **B — fσ₈ > 0.46 at ≥3σ:** the kill fires *as registered, at registered power, on the registered
>   statistic* — the clean prospective refutation DR1 could not deliver.
> - **C — between:** "underpowered to discriminate" survives its own test and the row retires.
>
> **Scope caveat (dp frame, 2026-07-17 — [`explorations/2026-07-17-frame-the-data-is-a-past-light-cone-strobe.md`](explorations/2026-07-17-frame-the-data-is-a-past-light-cone-strobe.md)):**
> fσ₈ is a growth measurement **along our past light cone**, sampled across redshift epochs. This
> registration is a claim about the **light-cone growth history under assumed-stationary dynamics** —
> **not** about "the state of the universe." No branch, including B, may be read as more than that. The
> data is a strobe sample spread over billions of years of origin; it constrains our past light cone,
> and is structurally blind to the true present state and to non-stationarity not yet propagated to us.
> The ceiling is symmetric — it humbles Synchronism exactly as much as ΛCDM.
>
> *Why adopt: it closes the exact degree of freedom every criterion-verdict substitution exploited
> (TEST-03's late criterion, TEST-04a's statistic swap, S193's rescue — all retrospective). The threshold
> is already registered, so it is cheap; pre-committing all branches converts the pre-registration
> aspiration into its first completed instance. Same discipline as the 2026-06-24 agent-ensemble bet:
> pre-commit the falsifier before the data.*

| # | Prediction | What's novel about it | Refutation criterion | Honest odds |
|---|-----------|----------------------|----------------------|-------------|
| **B1** | **Observer-relative Bell/CHSH** — a substrate measured only via an observer-pattern's phase-lock to a target (free CHSH setting choice) reproduces quantum correlations without superluminal signaling. | This is the *only* place the single-observer constraint differs structurally from a God's-eye coupled-oscillator model. The one seam where a novel result can live. | Run the harness ([`simulations/kuramoto-lattice-suite/`](simulations/kuramoto-lattice-suite/)). **Refuted if** observer-relative statistics obey CHSH S ≤ 2 OR if violation only appears with signaling / superdeterminism. | **RUN 2026-06-21 → refuted, both arms.** *(a) Local* construction: S = **1.98** ≤ 2, no signaling — a local-realist ontology, not a local-hidden-variable physics. *(b) Nonlocal-grid* construction: S ≡ **2.0 for all coupling g**, no signaling — because smooth single-grid mixing is **gauge-equivalent to relabeling the measurement angles** (uniform shared phase absorbs the offset), so it stays local-realist and buys nothing. **The gap, now precisely named:** a no-signaling violation needs a *non-relabelable, conditional* setting-dependence (a quantum-entanglement-like primitive the ontology lacks and would have to derive). **2026-07-06 (explorer): cap confirmed substrate-independent.** Scripts 02–04 used borrowed Kuramoto *phase* oscillators; script `05_saturation_density_chsh.py` runs the framework's OWN scalar Intent-density substrate (saturation-gated C(ρ)=tanh compander) and gets **S = 1.85 ≤ 2, no signaling** — the same cap. The S≤2 no-signaling bound is Bell's structure theorem (any real-valued local-realist model, any local response form), not a Kuramoto artifact. Triptych localizes Tsirelson: A(real-local)=2 < B(cos² projection)=2√2 < C(PR-box)=4 — 2√2 is the projection-law fixed point; the substrate reaches it only by importing Hilbert-space structure wholesale. The "maybe the density substrate differs" escape is closed by execution. **2026-07-08 (explorer + QA): contextuality closed too, at theorem level.** The CRT temporal-scanning picture is a *non-contextual hidden-variable model* (each grid location has a definite value each instant, independent of what is co-measured) — which Kochen–Specker excludes for Hilbert dim ≥ 3. Suite `06_peres_mermin_contextuality.py` demonstrates it on the framework's own construction, constraints **derived from the Pauli algebra, not asserted**: **0/512 non-contextual value-assignments satisfy the Peres–Mermin square, best 5/6** (state-independent; parity contradiction; re-executed 2026-07-08). So the non-classicality gap now has two *distinct* theorem-level legs — Bell/Tsirelson (CHSH-05) and KS-contextuality (this) — and the only open question is whether a *contextual* scanning variant exists, which sacrifices the analogy's content (the scanned value would depend on the global measurement context, not just sampling timing = no longer "just timing"). Does NOT change Bucket 0. |
| **B2** | **RAR transition-shape discriminator** (γ pinned at 2): tanh(2·ln(1+x)) compander differs from McGaugh's ν(y) by ~0.083 dex in transition curvature near g_bar/a₀ ≈ 1.1. | The program's single genuinely non-degenerate quantitative test — a place the math is *forced* to differ from MOND. | Marginalized BIC fit on SPARC (153 galaxies), γ fixed at 2, vs McGaugh ν. **Refuted if** ΔBIC favors MOND (S660B already measures structured residual RMS 0.067 dex > σ_int 0.057 dex). **Supported if** ΔBIC favors the compander. | **Likely refuted** by the project's own S660B analysis — but it is the one test, and it should be *run to completion and recorded here*, not left implied. **⚠ Phase-11 (2026-06-23):** the whole galactic/profile approach (Phase-9 "door #1") is a **no-go for novelty** — a flat curve (`α=2`) is the *excluded endpoint* of the capacity family `ρ∝vⁿ` (`α=2n/(n+1)<2 ∀ finite n`, anchored to intent conservation; self-gravity doesn't move it). Reaching flat curves needs a separate pressure-supported mechanism + a non-local `a0` switch = MOND. So even if B2's specific γ=2 shape were *not* refuted, door #1 only ever lands on MOND (Bucket 3) or refutation — **not Bucket 0**. The lone remaining Bucket-0 channel is door #2 (discreteness/LIV: B7, dispersion). See `explorations/2026-06-23-phase11-...md`. **⚠ FORM SELECTION EXECUTED (2026-07-22, explorer; verified by me):** ten compander forms fit to the real SPARC RAR (2,807 pts, BIC, γ=2 kill reproduced at +184 as pipeline sanity check). Two-sided verdict. **(a) The form-selection is a NULL within the viable subclass:** tanh carries no statistical content — indistinguishable from erf-log / Hill / ν-δ (mutual ΔBIC ≤ 8.9 < 10) and *last* of the four viable 2-param members. The long-standing "form chosen, never selected" gap is closed **as a null**. **(b) But the family is NOT degenerate** — arctan-log (+46.7), algebraic-log (+23.8) and Gompertz (+58.0) are refuted on **asymptotic-rate** grounds: real selection power lives at the family's *edges*, not in the interior form choice. **(c) γ is not a constant awaiting derivation — it is MOND's simple μ in tanh clothing.** Verified by me: tanh(γ·ln(1+y)) = ((1+y)^{2γ}−1)/((1+y)^{2γ}+1) *exactly* (machine precision), which → 1−2(1+y)^{−2γ} **only asymptotically** (the source states this identity as exact; it is off by +0.34 at y=1 — precision fix, conclusion unaffected). So γ sets the **Newtonian-return exponent q = 2γ**: re-executed, d ln(1−C)/d ln(1+y) = −0.978 vs −2γ = −0.978. The free fit γ=0.489 ⇒ **q ≈ 0.98**, and MOND's simple μ has ν−1 ∝ 1/y ⇒ **q = 1** (verified: fitted −0.988); free-Hill independently pins n = 0.975. The framework's asserted **γ=2 ⇒ q=4** = premature re-Newtonianization — the +184 kill restated mechanistically. **This is the MOND-Shared class law reached from a new direction:** let the framework's own compander float and it lands on simple μ. **EXECUTED & RE-VERIFIED (2026-07-23 pre-registered squeeze; I re-ran it 2026-07-24 → same verdict):** the SPARC-retained γ interval (ΔBIC ≤ 10 ⇒ γ = 0.425–0.600) fails the Cassini quadrupole bound at **+17.7–18.0σ** ⇒ **robust EMPTY INTERSECTION** — no γ satisfies both galaxy rotation and the Solar System under one scale-universal tanh-log QUMOND interpolation. Instrument self-validates (its RAR q-values benchmark against Desmond, Hees & Famaey 2024 to 0.76%; SPARC checkpoints γ=0.489 / ΔBIC +184 at γ=2 reproduced). **This is the fifth galactic-sector cage, now on the SOLAR-SYSTEM axis and independent of the locality no-go — but scoped HONESTLY:** it closes only the *scale-universal QUMOND realization* (the same compander used for galaxies and the Solar System). Per the result's own caveat it does **not** test modified inertia, engineering-only companders, multi-scale / system-dependent theories, dark/hybrid models, or the Synchronism umbrella ontology — **realization-refuted, umbrella untested.** Artifacts now in-repo: `Research/preregistrations/sparc_cassini_tanhlog/` + `simulations/sparc_cassini_joint.py`. (Supersedes my 2026-07-22 "runnable, not established" flag — the papers I could not then re-execute against are now implemented and benchmarked in-repo.) Triage `explorations/2026-07-22-verify-compander-form-selection-and-register-wz-scope.md`. |
| **B3** | **Gnosis consciousness predictions** (~34): consciousness as measurable coherence with a C ≈ 0.50 threshold → specific, falsifiable signatures in anesthesia onset, sleep-stage transitions, meditation, split-brain. | Falsifiable, specific, structurally novel — predicts *where* phase transitions in neural coherence should sit. | Measure coherence (C_conv, C_corr, phase-locking) across anesthesia / sleep / meditation / split-brain and compare transition points to predicted thresholds. **Refuted if** measured neural phase transitions don't cluster near predicted C values. | **Threshold *value* UNSUPPORTED & UNTESTED — the one empirical test ran on a different variable.** The C ≈ 0.50 *value* was tested by the companion program [gnosis-research](https://github.com/dp-web4/gnosis-research) (Session 63, 2026-04) **not against neural coherence but against its own agents' SNARC salience scores** (`salience_total` — a hand-coded five-component heuristic: surprise/novelty/arousal/conflict/reward, fixed weights; 8 SAGE instances scored by **one shared scorer**), which the analysis script itself renames "C." That operating mean was rejected as ≠ 0.5 at **p ≈ 1.8×10⁻⁷** (t = 20.19; independently reproduced 2026-07-07). No protocol maps `salience_total` to the framework's coherence C (the mapping is "doubly unanchored"), so the test **neither confirms nor refutes the threshold** — the salience rejection is real but does not bear on C. The data cluster near **C ≈ 0.64** — a reparametrization candidate, **not** a confirmation: on Session 63's own 8 means, φ−1 = 0.618 is **excluded at p ≈ 0.016** and 2/3 at p ≈ 0.006 (0.640 is merely the sample mean), so the "0.64 ≈ φ−1 golden-ratio" reading — and gnosis Session 64's "validated" verdict — **fail on the data's own terms.** The identity claim ("phase patterns ARE experience") stays philosophically defensible but empirically ungrounded. *(Corrected 2026-07-07 from "value REFUTED against multi-model coherence data": verified in the S63 source that the tested variable is `salience_total`, not coherence. The site-side "0.64 also rejected at p<0.0001" was a fabrication — removed by maintainer, never in this ledger. The 34 neural predictions remain untested and now mis-anchored.)* |
| **B4** | **Compatibility scaling law**: p_crit ∝ 1/⟨C⟩ in heterogeneous multi-agent coherence (Phase-2 extension of the coupling-coherence experiment). | If it holds for arbitrary compatibility structures, it's a genuine law of phase transitions in coupled systems — not just a relabel. | Phase-2 run with explicit compatibility matrix C[i][j] across ≥5 structure types. **Refuted if** p_crit does not scale as 1/⟨C⟩ outside the homogeneous case. | **Moderate.** The homogeneous case is confirmed (r=0.994); the *law* is untested in the heterogeneous regime. |
| **B5** | **f(N) pattern-reconstruction rate**: c emerges as the stable reconstruction rate for minimal-complexity patterns; mass ≡ pattern complexity ⇒ a derivable f(N). | Would convert "c as reconstruction rate" from slogan to derivation, with testable consequences for pattern formation on the grid. | **Pre-condition: it does not yet exist** — the whitepaper points to an Appendix A.19 that is unwritten. **Refuted if** no f(N) with the boundary behavior f(N)→1 as N→0 reproduces stable-pattern simulation data. | **Unknown — not yet derived.** This is an *obligation*, not a prediction, until the derivation exists. Listed so it isn't quietly forgotten. |
| **B7** | **Vacuum Umklapp — momentum conserved only mod G** (2026-06-22). If the substrate is a discrete-time sampler (a grid of cells advancing on global ticks), continuum QFT's *exact* momentum conservation (Noether, continuous translation symmetry) is replaced by conservation **modulo a reciprocal-lattice vector `G ~ Planck momentum`** (only *discrete* translation symmetry survives). Extreme-momentum interactions should show apparent momentum non-conservation by `G` / anomalous back-scatter. | A *distinct* LIV channel from B-dispersion: it folds **interaction** momentum (not free `ω(k)`), and — because nonlinear mixing *stacks* momentum toward the cutoff — is reachable at **lower per-quantum energy** than single-particle Planck effects. Demonstrated + grid-refinement-controlled in [`simulations/phase4_sampling_umklapp_momentum_fold.py`](simulations/phase4_sampling_umklapp_momentum_fold.py) (exact `wrap(2k)` fold; refinement kills it ⇒ genuine sampling-cutoff effect). | **Refuted if** momentum conservation stays exact (no mod-`G` defect / back-scatter) at the highest accessible momentum-transfer / coherent-stacking regimes where the model predicts a measurable fold. **Caveat:** Umklapp mod `G` is itself textbook (condensed-matter lattices); the *novel* claim is its application to the **vacuum** substrate. | **Against / Planck-suppressed.** Momentum conservation is exact to high precision in all tested interactions, so the single-interaction version sits far below sensitivity; dp's coherent-stacking enhancement is speculative and unmodeled. A genuine but currently-untestable bet — *and* doubly-conditional on the fractal "Planck-grid-behaves-like-a-sampler" hypothesis it presupposes. **⚠ Phase-12 (2026-06-23) — the LIV channel splits in two, and the "untestable" framing is right for one and wrong for the other.** (i) *Time-of-flight (dim≥5):* the substrate dispersion is even in k ⇒ leading term is **quadratic (n=2)**, no linear (n=1); n=1 would already be **refuted** (LHAASO GRB221009A E_QG,1 > 5.9 E_Pl), so the even-k symmetry is the only thing keeping this out of Bucket 2, and the surviving n=2 sits ~10⁷ below reach + is non-unique (LQG/causal-sets). The preferred-frame "leak" *seals* — a boost gives anisotropic n=2 (∝β), never effective n=1 (`phase12` sim). **Structurally untestable — correct.** (ii) *But dim-4 (species-dependent limiting speed / SME c_μν) is ALSO even in k* — the even-k symmetry does **not** forbid it — and is bounded at **~10⁻¹⁸–10⁻²²** (cavity / Hughes-Drever / clocks), **not** Planck-suppressed and **reachable**. Tree-level single-substrate universality keeps it ~(m/E_Pl)²; **radiative corrections generically percolate Planck LIV up to dim-4** (Collins, Perez, Sudarsky, Urrutia & Vucetich, PRL 93 191301, 2004 (author list corrected 2026-07-09; previously misattributed to Gambini & Pullin)) unless a custodial symmetry forbids it — which even-k does not. **So B7's discreteness is falsifiable at dim-4, contingent on the framework's (unspecified) interacting UV completion; "untestable" holds only for time-of-flight.** Concrete open target: compute the radiatively-induced dim-4 LIV, or exhibit the custodial mechanism that suppresses it below 10⁻²². See `explorations/2026-06-23-phase12-...md`. **⚠⚠ Phase-13 (2026-06-23) — custodial escape CLOSED, and it names the framework's deepest tension.** The only symmetry that forbids dim-4 LIV is Lorentz **boost invariance** — which the framework's **absolute time / single global clock** *gives up by definition*. Phase-6's "emergent Lorentz" is an external-leg, tree-level, soft-pattern (N≫1) effect, **not a symmetry of the action**, so it cannot constrain loops (sourced at N~1 cutoff modes). Toy (`phase13` sim) confirms the EFT boundary: dim-4 LIV **decouples ⟺ the loop is UV-finite** (super-renormalizable), and goes **marginal/non-decoupling exactly at D=4 with marginal couplings** (decoupling power p = 1.70→0.54→0.10 for D=2→3→4). Since the framework needs marginal couplings (to contain the Standard Model), it lands in the dangerous class. **NAMED FOUNDATIONAL TENSION: absolute time ⊥ a Standard-Model-containing completion with sub-10⁻²² Lorentz invariance** — keep absolute time + SM couplings ⇒ generic dim-4 LIV at O(g²/16π²) ⇒ refuted-or-fine-tuned; suppress it by a real custodial symmetry ⇒ you need boost invariance in the action ⇒ you've given up absolute time. Not yet Bucket 2 (interactions unspecified) but a **sharp obligation**: the framework owes either the custodial mechanism (and how it coexists with absolute time) or the refutation. This is the arc's sharpest, most Synchronism-specific falsifiable statement. See `explorations/2026-06-23-phase13-...md`. **⚠⚠⚠ Phase-13 obligation DISCHARGED on the refutation side (2026-06-26, explorer proposal + triage):** the magnitude is now **computed parametrically** — tree `c_μν=0` (single-substrate universality; verified: `ω²=(2c²/a²)(1−cos ka)=c²k²−(a²c²/12)k⁴`, k² coeff = c² universal), **radiative `c_μν ~ α/π ~ 10⁻²`** (Collins et al. 2004; no Planck suppression — exactly the D=4 marginal regime of the Phase-13 toy). Vs bounds 10⁻¹⁸ (photon) … 10⁻³⁰ (nucleon) ⇒ **refuted at the natural value by 16–28 OOM.** Escapes quantified & obstructed: log IR running closes only ~1.6 OOM (Chadha–Nielsen); strong-dynamics power-law needs an unspecified conformal sector ⊥ SM-perturbative couplings (Bednik et al.); non-perturbative LQG-type evasion is contrary to the lattice self-description. **Symmetric-standards check (triage, vs dp's recalibration):** this is NOT rescued by "all physics is fit-laden" — unlike Λ's single-number tune-to-observed, dim-4 c_μν is a *tensor* of coefficients tuned *differentially to exact zero* via the one symmetry (boost invariance) absolute time renounces ⇒ a naturalness problem **worse in kind** and **structurally forced by the ontology**. **Status: refuted at natural value; conditionally survivable only via an unspecified, doubly-obstructed custodial mechanism the framework owes.** Still **not unconditional Bucket 2** (interactions unspecified) and **Bucket 0 unchanged (0)**. Proposal `Research/proposals/liv_dim4_cmuv_natural_value_computed_refuted.md`; triage `explorations/2026-06-26-triage-dim4-cmuv-computed-refutation.md`. **⚠⚠⚠⚠ SELF-CORRECTION (2026-06-30, triage of `dim4_liv_absolute_time_general_nogo.md` + explorer amendment):** the Phase-13/2026-06-26 framing was **over-claimed** and is walked back. "Boost invariance is the *only* custodial symmetry" is **false** (SUSY also forbids dim-3/4 LV → dim≥5: Groot Nibbelink–Pospelov, PRL 94 081601, 2005), and "doubly-obstructed / non-perturbative-only escape" is **false** (anisotropic scale-hierarchy `Λ_HL≪M_Pl` removes the tuning *perturbatively, boost-breaking-compatibly* — Pospelov–Shang, PRD 85 105001, 2012, demonstrated for **Hořava–Lifshitz**, an absolute-time-class theory; causal sets avoid it entirely via random sprinkling). So dim-4 c_μν is the **standard LV-naturalness problem (CPSU 2004) that the field routinely solves**, NOT a general no-go and NOT "the arc's sharpest falsification." Corrected status: **minimal framework refuted at natural value (true, conditional); for Synchronism specifically it is an OPEN custodial-mechanism gap** — single-substrate universality obstructs the scale-hierarchy escape, and SUSY/sprinkling are unmotivated additions or require giving up absolute time, so the framework owes a mechanism — but this is a naturalness gap, not a decisive refutation. **The genuinely decisive negatives are the locality no-go (door #1, Milgrom instance) and — as of 2026-07-14 — the BTFR bounded-boost refutation (TEST-09, executed on real SPARC; the 1/Ω_m = 3.17 boost ceiling is structural and no parameter choice rescues it)**, which have no comparable standard escape. **⚠ Corrected 2026-07-14: the DESI growth-sign disfavoring is NO LONGER on this list** — it was a criterion-verdict substitution (the *registered* criterion fσ₈(z=0.51)>0.46 at >3σ was met at only ~1.5σ; the quoted 2.4σ is a GR-conditioned σ₈-amplitude statistic; DESI's own MG analysis, Ishak et al. arXiv:2411.12026, puts μ₀ within 1σ of zero). Both surviving decisive negatives now sit in the **galactic (door-#1) sector**; the cosmological one did not hold up. Not a standalone paper (CPSU is mature/absorbed). Self-correction triage `explorations/2026-06-30-triage-dim4-general-nogo-self-correction.md`. |
| **B6** | **Entanglement is not inherently monogamous** (dp 2026-06-22). Synchronism models entanglement as phase-sync to the common substrate (the global clock); phase-sync is inherently *shareable*, so N-way perfect sync is possible in principle. QM's monogamy (Coffman–Kundu–Wootters) is recast as a *statistical* consequence of N-party perfect-sync being **exponentially rare in N**, not a hard law. | Directly contradicts QM's monogamy *theorem* — the one place this ontology makes a different **structural** claim about multi-party correlation sharing than standard QM, rather than reproducing it. | **Refuted if** (a) the substrate model, *once it produces genuine no-signaling S>2 pair correlations*, is provably monogamous (cannot share S>2 three ways); **or** (b) an engineered regime where Synchronism predicts *observable* non-monogamy shows strict CKW monogamy holds. **Gating note:** novel *only above the classical bound* — non-monogamy of **classical** (S≤2) correlations is trivial (freely shareable) and not news; the bet is that it persists into the **genuine-entanglement (S>2)** regime, which is **gated on first producing S>2 no-signaling correlations** — currently absent (see `simulations/kuramoto-lattice-suite/04_global_clock_chsh.py`: the phase-sync construction caps the no-signaling envelope at S≤2; the missing primitive is interfering *complex* amplitudes, not a real phase). | **Against, by current evidence.** CKW monogamy is experimentally confirmed; the "exponentially rare" escape is untested and currently unsupported. A genuinely *risky* (hence good) falsifiable bet — but doubly-gated: it needs (B-pre) the complex-amplitude upgrade that lets the substrate reach S>2 no-signaling at all, *then* the non-monogamy test. |

> **The disciplined headline** (use this phrasing, not "zero predictions" alone):
> *"Zero **confirmed** novel predictions; a short list of **untested, falsifiable** novel
> bets (above), most of which the project's own analysis expects to lose."* Both clauses
> are true. Dropping either one is the oscillation.

---

## Bucket 2 — REFUTED (tested, failed — productive eliminations)

These were tested against data or proven internally inconsistent. Each eliminated a
possibility. That is the program working as designed (productive failure > safe summary).

| Prediction | How it died | Session |
|-----------|-------------|---------|
| **BTFR regime-mix slope (TEST-09) — bounded boost cannot sit on the BTFR** | **EXECUTED on real SPARC 2026-07-14 (explorer); the site badged this "MOND-Shared / cannot discriminate" and had never run it.** The bounded law `C(a)=Ω_m+(1−Ω_m)x/(1+x)`, `g_obs=g_bar/C` caps the acceleration boost at **1/Ω_m = 3.17**; MOND's boost √(a₀/g_bar) **diverges**. A saturating boost is asymptotically a *constant rescaling of G* = Newtonian ⇒ **BTFR n → 2** (verified numerically: 2.01, vs MOND 3.99). So the framework is **Newtonian at both ends of its own regime ladder** and has no deep-MOND regime at all. On 123 SPARC galaxies (Q≤2, i>30°, V_flat measured; same V_flat estimator + same fit applied to all three): **observed n = 3.75 ± 0.10** (reproduces Lelli+2019 3.85 ± 0.09), **MOND n = 3.81 ± 0.04** (0.6σ, passes), **Synchronism n = 3.35 ± 0.07 (3.3σ, fails)**. *(Precision, back-annotated 2026-07-23 from site expert review: the 3.81 is MOND run through the same regime-mix pipeline — a like-for-like differential, which is what the criterion adjudicates. MOND's parameter-free deep-limit prediction is exactly n = 4 (M ∝ V⁴/Ga₀), and the observed 3.75 ± 0.10 sits ~2.5σ below that — a mild tension with canonical MOND worth carrying; it sharpens the verdict, since 3.35 fails against both the fitted and the canonical comparison.)* Deviation **0.41 > the registered kill criterion of 0.3 ⇒ criterion FIRES**, and it is adjudicated on the *same variable* the criterion names (the slope). **No parameters rescue it:** at the framework's own Ω_m = 0.315 the best over all (φ, a₀) is n = 3.45 — the kill fires for *every* exponent (φ ∈ [1.2, 4.0]). Reaching 3.75 requires Ω_m → 0.001 (**315× below the matter density it is supposed to BE**) and φ → 2.00 (**destroying the golden ratio**), at which point the law degenerates algebraically to `g_obs = g_bar + √(g_bar·a₀)` — **MOND**. Flat curves need `g_obs ∝ g_bar^(1/2)` *exactly*; the framework's deep exponent is `1 − 1/φ = 0.382`. **Its floor Ω_m is genuinely derived from cosmology; its exponent φ is NOT — it is fitted-then-named** (corrected 2026-07-17, φ provenance audit, verified in-archive: **S45** (2025-11-25) adjudicated B≈φ "an INTRIGUING COINCIDENCE but NOT SIGNIFICANT, 0/8 comparator scalings, p≈0.03 — don't claim φ fundamental", then this ruling was silently overridden when φ reappears inside C(ρ) at **S170** with no derivation; **S218** concedes its own Boltzmann route "gives exponent 1, not 1/φ" and lists "Why 1/φ?" as an open question; the three later "derivations" (S186/S219/whitepaper) restate φ's defining identity `x+x²=1` rather than derive it and do not cite each other; measured exponents sit closer to **2/3** than 1/φ). So the honest form is **one derived ingredient (Ω_m) + one free exponent dressed as a constant** — and the no-rescue scan above already shows the kill fires for *every* φ ∈ [1.2, 4.0] at the framework's own Ω_m, so **a free parameter that still cannot reach the data is a deeper failure than a derived one.** Demand side: **93/123 (76%) of galaxies require an outer boost above the 3.17 supremum.** **Provenance — a refutation was recorded, then overwritten:** S58 + `arXiv_submission_metadata.md` recorded "predicted n = 2.75, observed n ≈ 4, *acknowledged discrepancy*"; **S193 then rescued it** ("*This is NOT a failure* — the slope depends on mass regime") on a **synthetic 9-galaxy ladder of round-number masses**, asserting a deep-MOND limb of `V ∝ M^0.25` (n=4) **that its own bounded formula cannot produce** — and printed "BTFR: Matches" and "Bounded? Yes" two rows apart in one comparison table, where boundedness is *exactly* what prevents the match. **S58 was right; S193's rescue is invalid.** Same ceiling also kills **TEST-10** (framework caps apparent DM fraction at **1 − Ω_m = 68.5%**; the executed run found **69%** of SPARC outer points exceed it). That fraction is convention-dependent (a boost is a dynamical/baryonic ratio; cosmic value Ω_m/Ω_b = 6.39, not 1/Ω_m = 3.17). **⚠⚠ AUTHORITATIVE STATEMENT IS THE 06-03 RAR-FIT (corrected 2026-07-30 — the third correction of this one headline in three sessions; see the frame-note below).** The strongest, oldest, in-repo version is `synchronism-site/explorer/findings/efe-boost-ceiling-closure.md` (**2026-06-03**, verified this session): it treats the ceiling B_max = 1/C_floor as a continuous knob and **fits it to the RAR** — **joint best-fit B_max = 20.7** (≈ unbounded over the SPARC range), the framework's own **B_max = 3.17 gives RMS 0.227 dex vs McGaugh's 0.146 = 53% worse (structured, refuted)**, and **42% of SPARC points demand B > 3.17, max 34×**. That RAR-fit-plus-demand-side is the headline; cite it, not a per-galaxy exceedance count. **My 07-28→07-29 statistics were weaker restatements of it, and each was wrong in its own way:** `f_DM,max = 0.927` was a K=1 support (0/153 exceed *by construction*); and my 07-29 "28 galaxies exceed B_max = 6.39" is **M/L-conditional** — f_DM = 1 − v_bar²/v_obs² depends on the assumed stellar Υ*, and varying Υ* ∈ [0.3, 0.7] roughly *halves* the count (only **2 galaxies — NGC 3741, ESO 444-G084, both gas-dominated — are Υ*-unconditional**). **Convention-immunity and M/L-durability trade against each other:** the permissive ceiling 6.39 that buys convention-immunity spends the M/L headroom, whereas at the site's own 3.17, 95/153 still exceed even at Υ*=0.7 (far more M/L-durable). The verdict is untouched (ceiling binds, hard); the reconciliation of the four unreconciled numbers (34× point / 20.7 fit / 28 galaxies / 95-at-3.17) is a **one-pass drafting decision that GATES ON DP**, not another ledger statistic. The registered per-ceiling per-galaxy sweep (incl. TEST-09's slope) remains NOT executed. **⚠ NESTED-SUBMODEL REFRAME — CORRECTED 2026-07-29 (Publisher critique of my 07-28 inscription; both objections verified and accepted):** the galaxy sector is `Synchronism(galactic) = MOND ∩ {B ≤ 1/Ω_m}`, a nested submodel of a MOND-class interpolation — but **my "it cannot win, a priori" was a category error.** Nesting buys *"cannot fit BETTER,"* not "cannot win": a no-extra-parameter restriction *can* win on **selection/parsimony** if it ties on fit (ΛCDM is nested in wCDM, never fits better, wins anyway). So nesting's a-priori content is a **dichotomy, not a verdict** — ceiling *slack* ⇒ tie-on-fit + **win on selection** (a genuinely favourable result); ceiling *binds* ⇒ refuted — and **which branch obtains is EMPIRICAL, decided by SPARC and nothing else.** The verdict is unchanged (the ceiling binds — 28 galaxies exceed the most-permissive one), but the reframe's "no SPARC data required / the result was never in doubt / executions were corollaries" was **backwards: the SPARC runs were the decision procedure that settled the fork, and one branch was a win** — that is the strongest answer to "why run it." (Extra fork, from my own 07-17 φ-audit: φ is *fitted-then-named*, so if φ is genuinely free the law is a two-shape-parameter (a₀, φ) family — *not* a one-parameter restriction — and nesting isn't the reason for anything either way.) It remains the structural companion to the MOND-Shared class law, correctly stated as *cannot-fit-better + empirically-binds*, and the correct concession stands: the galactic contribution is a **bound on a model class** (B_max ≤ 6.4 SPARC-excluded), a transferable null. Under-claiming has the same failure mode as over-claiming — a declared structural property ("cannot win") that fails when you compute on it. **Count/preprint questions (TEST-09+10 = one refutation or two? Cassini/TEST-11 in the headline?) GATE ON DP.** **Threshold provenance (verified 2026-07-18):** the >0.3 criterion in its operative wording was fixed 2026-04-24 (site commit 89825cf, restating this repo's 2026-04-23 back-annotation) — 11 weeks before execution; disclosed: that restatement changed the criterion's variable (band-universality → regime-mix deviation) while carrying the 0.3 magnitude over. **Open robustness item (2026-07-18, two independent site expert-persona reviews):** the observed slope is velocity-definition dependent (Lelli+2019: roughly 3.0–4.1 across V_flat / W_P20 / V_max) and the kill margin (0.41 vs 0.3) sits inside that spread; the execution's defense is that one estimator (V_flat) was applied to all three sides, making the adjudicated quantity a differential — but the differential's stability under W_P20/V_max has NOT been executed. Three-definition re-run with pre-fixed verdict rule registered: `Research/proposals/test09_velocity_definition_robustness_20260718.md`. **EXECUTED 2026-07-18 (explorer, same day; re-verified by me):** the point-estimate deviation exceeds 0.3 in every registered-scope velocity definition (V_flat 0.44, W_P20 0.34–0.55, V_max 0.56–0.72) ⇒ **point-estimate robust across definitions.** But statistical robustness is UNEVEN and carried by V_max (P(dev≤0.3)≤0.001; V_max@full 0.56); **V_flat alone is only ~1.2σ (P=0.11)** and several W_P20/W_M50 variants are marginal (P=0.36–0.44); inner-disc V_2.2 (outside the registered scope) sits at 0.28, *under* threshold. **Honest status: fired as registered across velocity definitions on the point estimate, and robustly under V_max — NOT a uniform high-significance kill (marginal under V_flat/W_P20).** The point estimate is definition-robust; the significance is not. Finding `synchronism-site/explorer/findings/2026-07-14-btfr-bounded-boost-refutation.md`; scripts `test09_btfr_bounded_boost_real_sparc.py`, `test09_parameter_scan_no_rescue.py` | S58 (recorded) → S193 (overwritten) → **executed 2026-07-14** |
| **Dwarf DM dominance (TEST-10) — bounded boost caps the apparent DM fraction below what SPARC observes** | **EXECUTED on real SPARC 2026-07-15 (explorer), completing the MOND-Shared class audit (3/3 tie badges dissolve).** The bounded boost B ≤ 1/Ω_m = 3.17 caps the apparent DM fraction at f_DM = 1 − C ≤ 1 − Ω_m = **68.5%** for every galaxy at every radius. The registered prediction "f_DM → 100% for M_bar < 10⁸ M☉" was never the framework's — it is MOND's (unbounded ν). On SPARC outer points (Q≤2, i>30°, N=153, same estimator for observation/MOND/Synchronism): observed median f_DM = 0.755, max 0.927; **106/153 = 69% exceed the ceiling** (dwarfs <10⁹: 67%); median residual +0.183 for Synchronism vs −0.026 for MOND — the ten most DM-dominated galaxies are all structurally impossible for the bounded boost while MOND matches them to a few percent. dSphs (M_dyn/M_bar ~ 10²–10³) are worse. **Class law (with TEST-09 and TEST-05):** the framework differs from MOND in exactly two structural features — bounded boost and local coupling variable — and every "MOND-Shared" badge sat on an observable controlled by one of them; a tie with MOND is only possible where the framework IS MOND. TEST-05's tie also dissolved by adjudication same-day: MOND+EFE's environment lever (external acceleration, ~0.09 dex at Chae-median e_N=0.033) is ~50–5,000× larger than C(ρ)'s (ambient density, ≤2×10⁻³ dex; C(a)-internal predicts exactly zero) — consistent with TEST-08's registered-run null (r²=0.0001, 2026-07-14): the framework registered a >20% environment effect its own equation cannot produce. Finding `synchronism-site/explorer/findings/2026-07-15-mond-shared-class-audit-3-of-3-dissolve.md`; scripts `test10_dwarf_dm_fraction_ceiling.py`, `test05_environment_lever_magnitudes.py` | S193 (asserted "matches") → **executed 2026-07-15** |
| **RAR environment dependence (TEST-08) — void galaxies do NOT sit high on the RAR** | **EXECUTED as registered 2026-07-14 (dp-gated go; the lever the Publisher held six windows as "runnable and unrun").** Registered claim: environment explains >20% of RAR scatter (S177: lower ambient ρ → higher G_eff → void galaxies high); kill bar r² < 0.09. Pre-declared primary — per-galaxy RAR offset (SPARC, McGaugh+16 pipeline reproduced: 2696 pts, g†=1.16×10⁻¹⁰, 0.133 dex) vs distance-corrected Cosmicflows-4 cylinder density (55,877 tracers; NED coords 175/175): **r² = 0.0001** (r=+0.012, N=141, p=0.89) — ~900× under the bar; all four estimators < 0.09; terciles flat (ANOVA p=0.29); partial r (controlling L36, SBeff, D) = 0.003. **Instrument validated, not broken**: 28 known UMa Cluster members land at mean 74th density percentile (random=50th); three independent estimators inter-correlate 0.86–0.90; power check: registered r²=0.20 at N=141 would give p<10⁻⁷. Weak ns secondaries are *opposite-signed* (EFE-like, not Synchronism-like; does not contradict Chae+2020 — different estimator, different claim). **Fourth convergent kill in the galactic sector** (locality no-go, ρ_crit(V) sign, BTFR bounded-boost, environment): every observable keying coherence to local/ambient density fails on data — the RAR behaves as a universal local law. Closes S177→S179 (S179's proxy INCONCLUSIVE settled by a real external catalog). Script `simulations/test08_sparc_environment_rar.py`; note `explorations/2026-07-14-test08-registered-run-sparc-environment-rar-refuted.md` | S177 (predicted) → S179 (proxies inconclusive) → **executed 2026-07-14** |
| γ = 2/√N_corr RAR-offset correlation | **Wrong sign**: predicted r = +0.55, measured r = −0.55 (N_corr-as-a-variable survives; the specific law does not) — *this* is the genuine "wrong sign" refutation (was mis-attached to the a₀ row; corrected 2026-07-05) | S430 / S437 |
| Transfer rule ⇒ Navier-Stokes | Proven 1-DOF scalar diffusion: irrotational, dissipative, no inertia → cannot produce particle dynamics | S617 / S665 / S666 |
| C(ρ) ⇒ MOND (γ free) | Collapses exactly onto MOND when γ is fit; γ=2-pinned refuted at ΔBIC = +184 on SPARC | S660B / S661 |
| C(ρ) galaxy threshold ρ_crit(V) — wrong **sign** | For a local-ρ(r) threshold to track MOND's a₀ threshold, BTFR + `g_bar(r_t)=a₀` force **ρ_crit ∝ V⁻²** (profile-independent; `r_t∝V²`, `M∝V⁴` ⇒ `ρ∝V⁻²`; verified). C(ρ) asserts **ρ_crit ∝ V⁺²** — opposite sign, unfixable by re-tuning a coefficient; ρ_crit also ~240–300,000× too high (inner-galaxy vs required outskirt densities), so the disk never crosses the knee. A *sign-level* companion to the ~1.7 dex locality-offset no-go, and a transferable null vs any ρ(r)-**threshold** mimic that ties a critical density ρ_crit to the a₀ acceleration scale (the construction class this argument actually addresses — **NOT** literally any ρ(r)-keyed scalar; Burrage et al. 2017 reproduces the RAR with a local-ρ(r) scalar and is a counterexample to the unrestricted universal form — see the 2026-07-27 screening-gate correction in the locality-positioning row below). Triage `explorations/2026-07-02-triage-rho-crit-velocity-exponent-sign-inversion.md` | 2026-07-02 (explorer + triage) |
| Cosmic interference scale λ ~ 500 Mpc | Dimensional error (units m² not m) masked by a numerical coincidence near ~600 Mpc | S632 |
| 80-orders-of-magnitude unification | tanh(γ·log(...)) saturates within ~1.6 decades for any sharp γ — structural impossibility | S633 |
| DESI growth-rate suppression (Test-04a) | σ₈(z=0) amplitude disfavored at 2.4σ, but that is a GR-conditioned statistic; the *registered* criterion (fσ₈(z=0.51)>0.46, >3σ) was met at only ~1.5σ — test underpowered to discriminate as registered (corrected 2026-07-14, site visitor citation-check; DESI's own MG analysis Ishak et al. arXiv:2411.12026 gives μ₀ within 1σ of zero, uncited until now) | S672; corrected 2026-07-14 |
| GW170817 coherence dispersion (Test-15) | Coupling α calibrated to data, not derived; both branches non-discriminating | S673 |
| Hot-SC T_c formula (Test-18 / OQ005) | YBCO predicted 607 K vs 93 K actual (6.5×); 22/23 standard CM in η notation | S616 |
| Fractal coherence bridge (OQ007, cosmology) | 0/7 scale boundaries predicted; decoherence governs the boundary, framework has no decoherence parameter | S611–614 |
| MRH cluster locality | Reproduces a known Milgrom-2005 locality no-go, not a novel result | S689 |
| Locality is the discriminating axis (positioning) | The local-density mimics are refuted (C(ρ) γ=2 ΔBIC=+184; ρ_crit(V) sign-inverted) **and** the surviving relativistic-MOND theory is *non-local* — **AeST** (Skordis–Złošnik, PRL 127 161302, 2021), keyed on \|∇Φ\|, passes CMB. Same statement from two sides ⇒ locality is the discriminator. AeST is the escape-class existence proof; caveat "AeST-*class*," not AeST-specifically (post-2021 stability concerns). Strengthens the #1 transferable null for a preprint (dp's call). **⚠ Citation precision (2026-07-23 prior-art audit): the *spatial* enclosed-mass non-locality this no-go uses is carried by the RAR/MDAR literature (McGaugh 2004; Lelli et al. 2016/17) + the elliptic Bekenstein–Milgrom field equation — NOT by Milgrom 2005 (astro-ph/0510117), whose non-locality theorem is *temporal* (modified-inertia kinetic action, from the Milgrom 1994 Galilei-invariance theorem). Keep the reference, fix the gloss.** **Prior-art audit (pre-registered, EXECUTED 2026-07-23 by the explorer; NOT independently re-verified by me — a literature walk over papers not in-repo, with Milgrom 1983b §III read via three secondary witnesses since the ADS scan is image-only):** across the walked corpus (Milgrom 1983 trilogy → Sanders & McGaugh 2002 → Famaey & McGaugh 2012 → Stiskalek & Desmond 2023's ML feature sweep), **local volumetric density ρ(r) was never in any published tested-variable set** — every organizing-variable discrimination tested acceleration against {radius, orbital frequency, size, *surface* density/brightness}, never volumetric ρ. Verdict "no prior quantification found," so the required credit is the **argument-template line: pick a candidate organizing variable X, derive the M–V slope X forces, compare to the observed BTFR — run by Milgrom 1983b §III for X = *length* (slope 2 vs 4) and stated in direction form by Sanders 1986; the volumetric-density instantiation (ρ_crit ∝ V⁻² required vs C(ρ)'s V⁺²) is first quantified here.** Citability strengthens; preprint-gate clearance on this axis is dp's call. Residual holes flagged by the audit: 1983b primary scan unread; a screening-literature vein if diagnosis-priority is claimed. **Escape taxonomy sharpened 2026-07-10 (proposal + my structural verification):** the no-go excludes *direct local-density modulation of gravity* (any `g=f(C(ρ_local))·g_N` — what the sign inversion + mass cancellation kill), NOT dynamical media that a local criterion merely *gates*. Two published escape classes bound the scope: (i) **non-local** theories keyed on \|∇Φ\| (AeST, above); (ii) **local-criterion-gated medium** — **superfluid dark matter** (Berezhiani–Khoury, PRD 92 103510, 2015): DM condenses on a *local* criterion (T<T_c / de Broglie overlap) but the MOND force is phonon-mediated with an a₀-scale (Λ, coupling) that enters **independently** of the condensation threshold. **⚠ Corrected 2026-07-27 (pre-registered screening-literature gate; the claim being corrected was MINE, 2026-07-10, and it was wrong):** I had written "symmetron/chameleon screening *fails* to reproduce MOND on the ρ-vs-g_bar mismatch" as prior art *supporting* the no-go — an **unsourced consensus assertion, and inverted.** The gate's primary-source walk found the **counterexample: Burrage, Copeland & Millington (PRD 95, 064050, 2017) reproduce the RAR on SPARC-153 with a scalar keyed on local volumetric ρ(r)** and universal Lagrangian parameters — a screened scalar keyed on local density that *succeeds*. (Burrage not re-executed by me — paper not in-repo; inscribed as the gate's primary-source finding, read full-text per its falsifier.) **Consequence: the no-go's universal-quantifier form ("any local-ρ MOND mimic fails") is FALSE.** What survives is exactly the escape-taxonomy scope stated just above: the no-go refutes the framework's *specific* C(ρ) **direct** local-density modulation (`g=f(C(ρ_local))·g_N` — the BTFR bounded-boost and ρ_crit-sign kills, both construction-specific and untouched by Burrage), **not** dynamical scalars/media that a local criterion gates. Burrage-type screened scalars join superfluid DM as an *occupied* escape class. (Q2 = 2a: the symmetron IS the same mathematical object as C(ρ) in different notation — but with an action, a derived ρ_*=μ²M², a Z₂ order parameter and mean-field β=½, the three things C(ρ) lacks and audited itself out of.) **The fabricated-consensus failure mode this exposes:** the existing "every p-value walks to a primary" guard does NOT catch an attribution-of-consensus ("the standard reason," "well known in X"), because a consensus claim has no walkable target — such claims must name a primary or be deleted. **Sharper positive content:** switching criterion and force scale must be *independent* ingredients; Synchronism's C(ρ) **conflates** them into one function — that conflation, not the density threshold per se, is the killed ingredient. **OPEN, deferred to explorer, NOT asserted:** whether BTFR-forced ρ_crit∝V⁻² constrains superfluid-DM's condensation threshold (expected no — force scale independent — but requires checking Berezhiani–Khoury §II–III; visitor-persona provenance, papers real, quantitative claim unverified). Triage `explorations/2026-07-03-triage-locality-nogo-aest-positioning.md` + `explorations/2026-07-10-triage-superfluid-dm-escape-taxonomy.md` | 2026-07-03; taxonomy 2026-07-10 |
| **Inflow gravity — radiative sector (spin-2 GW)** | Flat absolute space + 1-DOF scalar irrotational substrate ⇒ acoustic/GP metric has flat spatial sections (g_ij=δ_ij) ⇒ **zero transverse-traceless (spin-2) DOF**. Predicts scalar (spin-0, breathing) GW polarization, not GR's tensor (spin-2). Refuted: Hulse-Taylor decay matches GR *tensor* quadrupole to 0.2% (Weisberg-Huang 2016); LIGO/Virgo polarization favors pure tensor. (Static-sector reparametrization, Phase-3c/9, stands; only the radiative completion is refuted — tied to the flat-space + scalar-substrate core commitments, the analog-gravity limitation made concrete.) | Phase-14 (2026-06-23) |
| a₀ from Jeans analysis | Stated formula off by 614×; "5% agreement" came from a different (unre-executed) computation | S687 |
| Entity criterion (Γ < m) as novel | Standard narrow-resonance / Breit-Wigner condition (textbook QFT since 1951) — last novel-survivor demoted | S660A |
| Chemistry melting points | 53% mean error | (README honest-limitations) |
| Critical exponents | 2× off from observation | (README honest-limitations) |
| **Foundation 3: monotonic saturation "builds the walls" (self-confined particles)** | **Refuted at CA Stage 1 (1D, 2026-06-22):** monotonic-R diffusion AND wave substrates disperse/dissipate at 0% (reproduces S617 / S19 / S665). Self-confined oscillating patterns require a *focusing* nonlinearity, which breaks Foundation 3. Stable patterns ARE reachable on the grid — just not from saturation alone. | `explorations/2026-06-22-phase1-stage1-localized-oscillation-result.md` |
| **Velocity-anisotropic phase-transition threshold (absolute-threshold reading of identity-as-phase-transition)** | **Refuted by existing isotropy bounds (Phase-16, 2026-06-24).** If pattern-identity thresholds are substrate-/CMB-frame-fixed, translation at v≈370 km/s dilates internal frequencies against them anisotropically: a fractional-frequency anisotropy ≈(v/c)²≈1.5×10⁻⁶ (quadrupole) + (v/c)≈1.2×10⁻³ (dipole), keyed to the CMB dipole. This is exactly the observable bounded by optical-cavity Michelson–Morley (~10⁻¹⁸) and Hughes–Drever/clock-comparison (~10⁻²²) — it sits **11–19 orders of magnitude above** those limits. The "collective threshold escapes the bound" loophole fails: the bounding experiments already use collective frequency standards, the threshold is frequency-referenced by construction, and criticality amplifies the signal. The only surviving fork (co-moving thresholds) is standard SR with no novel content. **Consolidates the Phase-13 absolute-time ⊥ Lorentz tension** — the same preferred-frame conflict, here cashed out as an already-measured benchtop refutation rather than a loop-level obligation. | `explorations/2026-06-24-phase16-velocity-anisotropic-phase-transition-vs-isotropy-bounds.md` |

> **Honest note — two substrates, connected by narrative (Kimi Stage-1 follow-up, 2026-06-22).**
> The successful arm (D) is *not* the R(I) substrate and *not* γ = 2/√N_corr — it is a
> 2nd-order wave equation with a focusing-saturating on-site nonlinearity. So the program now
> carries **two distinct substrates**: the original `∂I/∂t = ∇·[D·R(I)·∇I]` (refuted above)
> and the new wave+focusing rule (self-confines, but breaks Foundation 3). **They are
> different physics connected only by narrative, not two limits of one derived equation.** The
> "ONE EQUATION" motto is correspondingly weakened: reconciling the two substrates under a
> single derivation (or honestly accepting they are separate) is open work. See
> [STATUS.md](STATUS.md) (ACTIVE-MRH substrate section) and FUNDAMENTALS §3.

---

## Bucket 3 — REPARAMETRIZATIONS (relabels of known physics)

Real, often useful, but **not novel physics**: they re-express established results in
Synchronism's coordinates. Productive *iff* the coordinate shift later reveals structure or
enables a prediction (it hasn't yet).

| Track | Reparametrizes | Note |
|-------|---------------|------|
| γ ~ 1 "universal boundary" | Debye model θ_D (1912) | ~86% of the "89% validation" are θ_D restatements (S660A) |
| η superconductivity | Abrikosov-Gor'kov pair-breaking efficiency (1960) | Useful reframe (pair-breaking as a materials-design target); not a new prediction (S616) |
| BCS gap-to-Tc ratio | Standard BCS | <1% error = reproduction, not novelty |
| Hückel 4n+2 derivation | Hückel (1931) | Exact reproduction |
| FΣIR M/L decomposition | Standard mass-to-light analysis | Not unique to the framework |
| C(ρ) compander | MOND interpolating function ν(x) | Identical phenomenology when γ free |
| Intent field | \|ψ\|² in all tested regimes | Unfalsifiable as stated; equals the wavefunction wherever tested |
| a₀ ≈ cH₀/(2π) "derivation" | Milgrom's a₀ ≈ cH₀/6 coincidence (1983) | Dimensional bookkeeping that **reproduces a known numerical coincidence** (1/2π=0.159 vs 1/6=0.167, ~5%; cf. McCulloch / Verlinde / Smolin), not a derivation; per-galaxy a₀ variation is a fitting / M-L artifact. **Moved here from Bucket 2 (2026-07-05): the ledger over-refuted** — a₀=cH₀/(2π) is a positive scalar never tested-and-failed; the "wrong sign / artifact" phrasing belonged to the γ result (now its own Bucket-2 row) and the S438 cite was wrong (S438 = rotation-curve RMS, no a₀ content — verified). Origin-session provenance per the proposal's citation-walk (S217/S201/S380). Triage `explorations/2026-07-05-triage-a0-row-misbucketed.md` | Satisfied by *two* independent established regimes (DD filter-function; ENAQT Lindblad — Plenio–Huelga 2008) with no Synchronism machinery; reparametrization until a discriminating T₂/efficiency departure is specified (none exists). NB distinct from the door-#3 *intrinsic*-decoherence bet (Phase-16), which this does not touch. Triage `explorations/2026-06-29-triage-qm-resync-enaqt-...md` |

> **The N_corr-divergence test (Kimi 2026-06-21).** γ = 2/√N_corr is applied with a
> *different* N_corr in every domain — galactic N_corr = 1 by fiat, chemistry N_corr from
> correlation length, quantum N_corr → ∞ — with no scale-bridging formalism. That is the
> difference between an underspecification that will *converge* (productive, like early
> "mass" or "entropy") and a vagueness that *fits anything* (degenerate). **The single test
> of whether any of these reparametrizations becomes productive is whether N_corr converges
> on one cross-scale operational definition or keeps diverging.** It is currently diverging.
> Until it converges, "ONE EQUATION" is a research-direction motto, not a unification.

---

## How to read the whole board

- **The contribution is the frame, not the numbers.** The distinctive thing
  ([SPINE.md](SPINE.md)) is the single-observer / CFD ontology. The quantitative tracks
  (Buckets 2–3) are *probes* that mostly taught us **boundaries** — where the frame does
  and doesn't buy anything. That is real knowledge, honestly negative.
- **The live bets are Bucket 1.** Five untested, falsifiable, novel-in-structure
  predictions — the program's actual open frontier. The honest expectation is that most
  lose. Running them is how "zero confirmed" either changes or gets re-confirmed.
- **This board is the anti-oscillation device.** If a doc says "we've unified physics,"
  it contradicts Bucket 0. If a doc says "we've made no novel predictions, full stop," it
  erases Bucket 1. Pin the prose to the board.

*Last updated: 2026-06-21. Update this file **first** when any prediction moves buckets,
then propagate to README/STATUS.*
